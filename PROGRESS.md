# Progress

마지막 업데이트: 2026-08-12 KST

## ✅ 2학기 개학 학교가 계속 1학기로 보이던 문제 — 구현 완료 (2026-08-12, 미커밋, ADR-046)

**요청**: "2학기를 시작했는데 쌤핀은 아직 1학기로 표시되는 곳들이 있다. 어떻게 2학기로 보이게 할까?"

**원인**: 학기 판정이 두 갈래뿐이었다 — ①`settings.currentTerm`(학년도 마무리를 **실행해야만** 생김) ②없으면 달력 파생(3~8월=1학기). 그래서 8월 중순 개학 학교는 **9월 1일까지** 앱 전체가 1학기. 게다가 표시를 바꾸는 유일한 방법이 마무리 마법사(기록을 보관함으로 옮기고 라이브를 비움)라 **"표시만 맞추기"가 원리적으로 불가능**했다.

**해결(3경로 + 축 분리)**: 새 필드 `settings.termStartDates`(학기→개학일)를 표시 축의 정본으로 두고 `currentTerm`(마감·동기화 가드)은 그대로 뒀다. 채우는 경로 셋 — ①설정에서 학기 선택 + 개학일 입력 ②**학사일정의 '개학식'을 찾아 자동 채움**(출처 행사명 표시) ③나이스 시간표 반대 학기 재시도 성공 시 불러오기 완료 화면에서 확인. 셋 다 **제안까지만** 하고 확정은 사용자가 한다(ADR-037 유지).

**함께 고친 것**: "이번 학기" 기간 계산이 **6벌** 복제돼 있었고(데스크톱 5·모바일 1), 그중 내보내기 2곳은 **1~2월에 시작일이 미래(그 해 3/1)** 라 결과가 늘 0건이던 기존 버그가 있었다. 전부 `resolveTermStartDate` 하나로 통합. 모바일도 같은 도메인 규칙을 공유하도록 설정 투영에 두 필드를 읽기 전용으로 추가.

**커버리지 메모(옆 세션 제보 반영)**: 시간표 신호만으로는 **나이스에 시간표를 아예 안 올리는 학교**(예: 경기북과학고)를 못 잡는다. 학사일정은 그런 학교에도 있고 날짜 자체를 주므로(하한선이 아니라) 실질 커버리지가 더 넓다 — 두 신호를 함께 쓴다.

**검증**: tsc 0 · lint 신규 0 errors(경고 133건 기존, 증감 없음) · 전체 테스트 365파일 4358건 통과(신규 60건) · regression 39/39.

**남음**: 실기기 확인 · 커밋 · `/docs` 사용자 가이드 문단 · 릴리즈 고지. **미해결(발견성)**: 학기가 틀린 줄 모르는 선생님은 설정에 들어올 이유가 없다 — 학사일정 후보가 있을 때 앱이 먼저 알리는 지점이 아직 없다(시간표 갱신 배너 영역은 다른 세션 작업 중이라 이번엔 손대지 않음).

## ✅ 위젯 새로고침에서 컴시간·압핀 변동 확인 — 구현 완료 (2026-08-12, 미커밋, ADR-045)

**요청**: "시간표 화면 새로고침처럼 위젯 새로고침에서도 컴시간 변동을 확인할 수 있으면 좋겠다"(2026-08-11 피드백, 챗봇 에스컬레이션).

**사실 확인**: 챗봇 답변이 맞았다 — 위젯 새로고침(`triggerRefreshAll`)은 저장 파일을 다시 읽을 뿐 서버 조회가 0회였다. 반면 변동 확인 로직(`checkComciganTimetableChange`)은 이미 앱 시작 훅과 시간표 화면 버튼이 공유하는 재사용 가능한 함수였고, 통신도 electron main IPC 경유라 위젯 창에서도 부를 수 있었다.

**구현**: 위젯 헤더의 기존 새로고침 버튼에 변동 확인을 병행. 켜져 있는 원천만(컴시간·압핀) 확인하고, 연동을 안 쓰면 조회도 안내도 없음(침묵). 결과는 위젯 하단 배너로 표시 — 변경 감지 시 "눌러서 검토하기" → 메인 창 시간표가 열리며 검토·적용까지 이어짐.

**지켜야 했던 3가지 (ADR-045)**: ① 확인은 **버튼 클릭 경로에만** — `useWidgetRefresh` 는 5분 타이머·창 활성화에도 도는 경로라 얹었으면 comci.net 상시 폴링이 됐다(별도 이벤트로 분리 + 소스 계약 테스트로 고정) ② 위젯 창엔 `ToastContainer` 가 없어 확인 함수가 **판정 결과를 반환**하도록 바꾸고(silent) 안내는 창이 그림 ③ 검토 대기 상태는 창을 못 넘고 `navigateToPage` 가 위젯을 닫으므로 **상태가 아니라 의도**(`timetable#sync-review`)를 넘김.

**구현 중 발견해 함께 고친 위험 2건**: ① 비교 기준(현재 시간표)이 로딩되기 전 확인이 돌면 **바뀐 게 없는데 "전부 바뀌었다"** 가 된다 → 두 경로 모두 `load()` 선행 강제 ② 재진입 가드가 첫 `await` 뒤에 있어 연타 시 두 번 조회 → 가드를 await 앞으로.

**검증**: tsc 0 · lint 신규 0 errors(경고 133건은 기존, 증감 없음) · 신규 테스트 19건 통과 · regression 39/39 · landing docs:check 통과. 사용자 가이드 '시간표 변경 자동 감지'에 위젯 경로 문단 추가.

**남음**: 실기기 확인(위젯은 별도 창이라 브라우저 모드 불가) · 커밋 · 다음 릴리즈 고지. 계획서 `docs/01-plan/features/widget-timetable-change-check.plan.md`.

## ✅ 진도 "다른 반에도 함께 기록" — 데스크톱 + 모바일 구현·push·배포 완료 (2026-08-11~12, main `35f14593`+`522d281a`, ADR-044)

**요청**: "진도 추가할 때 다른 반에서 복사하기나 목록에서 선택하기 기능이 있으면 좋겠다"(2026-08-11 피드백, 연락처 없음).

**진단**: 기능은 **이미 있었다**(진도 탭 "다른 반에서 불러오기"). 있는데도 요청이 온 이유는 쓸모가 없어서였다 — ① 방향이 pull이라 담당 반 수만큼 반복 ② **교시를 원본 그대로 복사**해(구 `ProgressTab.tsx:294`) 복사 후 거의 전부 손으로 수정 ③ 날짜도 반마다 수업 요일이 달라 "일괄 며칠 밀기"로는 안 맞음 ④ 중복 검사 0건. 정작 "이 반이 이날 몇 교시인지" 찾는 `getMatchingPeriods`는 이미 있었는데 **복사할 때만 안 쓰고 있었다**.

**구현**: 진도를 넣는 그 자리에서 다른 반을 골라 함께 기록(push 방향). 날짜·교시는 대상 반 시간표 기준 자동 배정(같은 시간 → 같은 날 다른 교시 → 가장 가까운 다음 수업, 최대 3주). 찬 자리는 건너뛰고 다음 후보로 이어감(덮어쓰기 없음). 저장 전 "어느 반 · 언제 · 몇 교시" 미리보기 + 저장 후 결과 안내. 복사본 상태는 항상 '예정'. 보관된 반 제외, 같은 과목 우선 정렬, 마지막 선택 기억(세션 한정).

**적용 지점 5곳**: 데스크톱 진도 탭 추가 폼 / 진도 캘린더 빠른 입력 / 시간표 진도 셀 + 모바일 학급 진도 탭 / 모바일 오늘 화면 진도 기록.

**배정 규칙은 `domain/rules/progressFanout.ts` 순수 함수**(시간표 조회·중복 판정은 콜백 주입) — 데스크톱·모바일이 같은 규칙 공유. 후보 목록(`buildFanoutCandidates`)·결과 문구(`describeFanoutResult`)도 도메인으로 올려 중복 제거.

**중간 발견·수정**: 모바일 진도 입력창이 **교사 시간표를 로드하지 않아** 오늘 화면 진입 시 매칭 0건 → 조용히 "원본 그대로 복사"로 떨어지던 문제(같은 커밋에서 수정). 매칭 실패가 곧 잘못된 배정이라 눈에 안 띄는 구조였다.

**검증 게이트 4/4**: tsc 0 · lint 신규 코드 0 errors · vitest 356파일 4250 passed(단독 재실행 포함 전건 통과 — 전체 병렬 시 `XlsxExporter.bundle.test.ts` 1건이 5초 제한 초과로 실패하나 단독 900ms 통과, 변경과 무관) · regression 39/39. 신규 도메인 테스트 **17건**(`progressFanout.test.ts`).

**배포 확인(실물)**: m.ssampin.com 번들 `mobile-DYw-0UFY.js`에서 신규 문구 4종(`다른 반에도 함께`·`이렇게 들어갑니다`·`다음 수업으로`·`이미 진도가 있어 건너뜁니다`) 각 1건 확인 · /docs `features/class-management`·`features/mobile` 200 + 신규 문구 라이브. landing docs:check 41문서 + next build 통과. **사용자 실기기 확인 완료.**

**남음**: 데스크톱은 다음 버전 릴리즈에 포함돼야 사용자에게 나감(현재 출시 v2.3.7). 릴리즈 고지(카드뉴스·Threads)도 그때.

## 🚀 v2.3.7 **출시 완료** — 위젯·아이콘 모드 업데이트 안내 + 바탕화면 모드 고착 해소 (2026-08-11, tag `v2.3.7` Latest, main `662bc093`, ADR-042 + ADR-043)

**배포 결과(전 지점 검증)**: GHA Windows(`31443929988`)·macOS(`31443932773`) 둘 다 success → 자산 8종 업로드 → **다운로드 URL 8종 전부 302** · `releases/latest` 피드 = **2.3.7**(해시·크기 실물 일치) · 랜딩 라이브 2.3.7 · 모바일 번들 `v2.3.7` · /docs 신규 2항목 라이브 확인 · 챗봇 KB **359/359**(실패 0) · landing docs:check 41문서 + next build 통과.

### 1) 바탕화면 아이콘 아래 모드가 한 번 실패하면 영영 재시도 불가 (ADR-042, `2fcaba4b`)

**신고**: 서울 중학교 교사 — "위젯 설정 → 바탕화면 아이콘 아래를 눌러도 아무 변화가 없다. 설정에는 눌려 있다. 백신(안랩)을 꺼도 동일." 오너 환경에서는 정상 동작 → 환경 종속 의심.

**진단(코드 근거)**: 실패 시 fallback 정정을 **renderer 훅 하나에만** 맡겨 저장값(`native-desktop`)과 실제 적용(`normal`)이 어긋난 채 고착될 수 있었고, 그 상태에서 재시도 경로 3곳이 전부 "값이 같으면 무시"로 막혀 있었다(라디오 change 미발생 · 저장 시 변경분 없어 IPC 미발신 · `from === next` 조기 반환으로 "다시 시도"가 죽은 버튼). **사용자가 UI로 빠져나올 방법이 0개.**

**수정**: main이 `settings.json`을 직접 정정(`persistDesktopModeFallback`) + force 재적용 경로 개방 + 설정에 "지금 다시 적용" 버튼 신설.

**미해결(대기)**: 1차 실패의 근본 원인. 후보는 ① 보안 프로그램의 koffi/후킹 차단 ② `WM_SPAWN_WORKER`를 `(wParam=0xD, lParam=0)` **한 조합으로만** 보냄(업계 통용은 Win10 `(0,0)`·Win11 `(0xD,0x1)`) ③ 다중 모니터. **사용자 로그(`%APPDATA%\ssampin\native-desktop-diag.log`) 회신 후 C 작업 진행 예정.**

### 2) 위젯·아이콘 모드에서 업데이트 안내가 아예 안 뜨던 문제 (ADR-043, `662bc093`)

**오너 관찰**: "릴리즈해도 자동 업데이트가 잘 안 되는 것 같다."

**측정 결과**: 피드는 정상(자산·해시·302 전부 확인). 막힌 곳은 앱 안 — 자동 내려받기 OFF + 실행 버튼이 **메인 창 전용** + 위젯 배너는 도달 불가능한 `downloaded` 상태에서만 표시 + 아이콘 모드엔 UI 없음 + 메모리 절약 모드(기본 ON)가 위젯 전환 시 메인 창을 해제. **주 사용 형태에서 업데이트 경로가 완전히 닫혀 있었다.**

**수정**: `useUpdateBanner` 훅으로 상태·트리거 단일화 → 위젯 하단 배너(available부터 → 진행률 → 재시작) + 아이콘 모드 핀 말풍선(버전당 1회) + 팝오버 실행 버튼. 자동 내려받기는 계속 OFF(290MB).

**게이트**: tsc 0 · eslint 신규 경고 0 · vitest **354파일 4229 passed / 0 failed** · regression 39/39 · CI success. 신규 회귀 8건(WidgetTab 2 + useUpdateBanner 6).

**남음**: 실기기 확인(위젯 배너·아이콘 말풍선은 실제 업데이트 이벤트로만 관측 가능) · 카드뉴스·Threads 고지(v2.3.5~v2.3.7 묶음) · 신고 교사 회신 + 진단 로그 회수.

## 🚀 v2.3.6 핫픽스 **출시 완료** — Drive 조건부 갱신이 **100% 실패**하던 상수 버그 수정 (2026-08-11, tag `v2.3.6` Latest, main `ebb8351e`, ADR-041)

**오너 실기기 확인 완료**(`electron:dev`로 직접 동기화 — 오류 소멸 확인) 후 릴리즈. **배포 결과**: GHA Windows(`31440235622`)·macOS(`31440239471`) 둘 다 success → 자산 8종 업로드 → **다운로드 URL 8종 전부 302** · `releases/latest` = v2.3.6 · 랜딩 `softwareVersion 2.3.6` · 모바일 번들 `v2.3.6` · 챗봇 KB **357/357**(실패 0) 후 실제 질의 정답 확인 · landing docs:check + build 통과.

**신고**: "클라우드 settings 파일이 동기화 중 변경되었습니다. 다시 동기화해 주세요."가 **동기화를 반복해도 사라지지 않음**.

**원인(직접 측정으로 확정)**: `getFilePrecondition`이 `res.headers.get('ETag')`를 못 읽으면 `null`을 반환했는데, **Google API는 CORS `Access-Control-Expose-Headers`에 `etag`를 넣지 않는다**(실측: 200·401 모두 `content-encoding,date,server,content-length,vary`). 렌더러는 webSecurity 기본 on + CORS 우회 없음 → `headers.get('ETag')`는 **항상 null** → 조건부 갱신이 **조건이 아니라 상수로 실패**. **v2.3.1~v2.3.3**엔 충돌 화면 "이 기기 유지"가 항상 실패했고(충돌이 해결되지 않던 진짜 이유), **v2.3.4~v2.3.5**엔 일반 업로드까지 CAS를 타면서 **클라우드에 이미 있는 어떤 파일도 갱신 불가**였다. 데스크톱·모바일 공통.

**왜 안 잡혔나**: `DriveSyncAdapterConditional.test.ts`가 모의 응답에 **ETag를 직접 넣어줬다** — 실제 Google이 주지 않는 값을 테스트가 공급해 검증력이 0이었다.

**수정**: 판정 기준을 응답 **본문**으로 읽는 `modifiedTime`으로 바꾸고, ETag는 읽히는 환경에서만 `If-Match`로 덤. 부재를 실패로 취급하지 않음(빈 If-Match 전송 금지 — 412 유발). 안전 확인(2회: 목록 + 신선한 GET)은 유지 — v2.3.1 이전엔 확인 자체가 없었으므로 그때보다 엄격하다.

**게이트**: tsc 0 · lint 0 errors · vitest **352파일 4221 passed / 0 failed** · regression 39/39. 신규 회귀 3건은 **수정 전 코드에서 2건 실패함을 실측 확인**(테스트가 진짜로 이 버그를 잡는다는 증거).

**남음**: 카드뉴스·Threads 고지(v2.3.5·v2.3.6 묶어서).

## 🚀 v2.3.5 핫픽스 **출시 완료** — 동기화 충돌 창 무한 반복 해소 (2026-08-10, tag `v2.3.5` Latest, main `c89f2e04`, ADR-039 + ADR-040)

**배포 결과(전 지점 검증 완료)**: GHA Windows(`31374177875`)·macOS(`31374182200`) 둘 다 success → 릴리즈 자산 8종 업로드 → **다운로드 URL 8종 전부 302** · `releases/latest` = v2.3.5 · 랜딩 라이브 `softwareVersion 2.3.5`(Vercel git 자동 배포) · 모바일 번들 `v2.3.5` · 챗봇 KB ingest **356/356 성공(실패 0)** 후 **실제 질의로 정답 확인**("v2.3.5에서 해결 + 업데이트 전엔 '이 기기 유지', '클라우드로 대체' 금지"). 푸시 CI success.

**남음**: 실기기 확인 · 카드뉴스·Threads 고지.

**신고**: v2.3.0~2.3.4 업데이트 후 "동기화 충돌 — 설정" 창이 계속 뜬다. 이 기기 시각이 `Invalid Date`로 표시.

**원인 (재현 테스트로 확정)**: v2.3.1 핫픽스(`81b58ab5`)가 넣은 "장부 체크섬은 같은데 실제 로컬 내용이 다르면 충돌" 판정이, 빈 봉투 유실뿐 아니라 **아직 업로드 안 된 정상적인 로컬 변경까지** 잡았다. 그런데 `useDriveSyncStore`는 동기화가 끝난 뒤 `settings.sync.lastSyncedAt`을 **장부 확정 이후에** 다시 썼다 → 매 동기화가 스스로 어긋남을 만들고 다음 다운로드가 그걸 충돌로 올리는 **자가당착 무한 반복**. 해결해도 다음 동기화에서 부활. 부팅·주기·창 포커스마다 동기화하므로 계속 떴다. `Invalid Date`는 판정이 시각 자리에 넣는 문자열 `'content-mismatch'`가 렌더된 것. 이 분기는 `conflictPolicy`도 검사하지 않아 기본값 `latest` 사용자까지 창을 봤다. **데스크톱 전용**(모바일은 settings에 시각을 덧쓰지 않음).

**수정 ①(ADR-039) 판정 교정**: 겹쳐 있던 두 상태를 가름 — ⓐ미업로드 로컬 변경 → 스킵(리모트 체크섬 == 내 장부 체크섬이라 **받을 것이 없음이 증명**되고, 이어지는 업로드가 올린다) ⓑ빈 봉투 + 리모트가 더 큼 → 충돌 유지(v2.3.1 보호 그대로). 판정식 `!hasSubstantiveContent(local) && remoteInfo.size > localSize` — **크기 비교 병용이 핵심**(settings처럼 배열이 부수적인 객체의 오판 차단). settings 전용 이스케이프 해치는 도달 불가가 되어 제거(매 동기화 settings.json 1회 추가 다운로드도 소멸).

**수정 ②(ADR-040) 원인 제거**: `sync.lastSyncedAt`을 동기화 대상 settings에서 빼내 **기기 전용 저장소**(`drive-sync-device-state`, SYNC_FILES 미등재)로 옮김. 이제 settings는 사용자가 실제로 설정을 바꿀 때만 올라간다 — 매 주기 업로드 낭비·기기 간 설정 LWW 핑퐁도 함께 소멸. 표시 3곳(사이드바 인디케이터·설정 백업 카드·Google 계정 카드)은 스토어 값 우선 + v2.3.4 이하 레거시 폴백, 앱 시작 시 `hydrateLastSyncedAt()`이 기기 저장소에서 복구하며 레거시 값 1회 승계. `Settings.sync.lastSyncedAt`은 `@deprecated` 읽기 전용으로만 남김.

**⚠️ 작업 중 발견**: 세션 시작 브랜치 `fix/sync-events-download-heal`가 **main보다 6커밋 뒤처져 있었다**(v2.3.1~2.3.4 전부 누락, 그 브랜치의 유일 커밋 `ca290dd1`은 main의 `81b58ab5`와 동일 내용). 또 `ssampin-hotfix-v2.3.1-sync-ui` **유령 워크트리**가 main을 점유하고 있어 전환이 막혀 있었다(`git worktree prune`으로 해소). 작업을 main 위에 재적용 — main이 그 사이 추가한 `convergeMergedFile`(병합본 체크섬 수렴)·CAS 업로드(`createSyncFileIfMissing`)와 정합 확인 완료. **교훈: 작업 시작 전 `git rev-list --count HEAD..origin/main`으로 베이스 신선도를 먼저 확인할 것.**

**게이트(main 기준)**: tsc 0 · lint 0 errors · vitest **352파일 4218 passed / 0 failed** · regression **39/39**. 신규 회귀 테스트 2종 — `contentMismatchPendingLocalChange.test.ts`(가짜 충돌 0·빈 봉투 충돌 유지·스킵분 실제 업로드·settings 재업로드 없음) + `driveSyncLastSyncedAtLocation.meta.test.ts`(시각이 settings로 되돌아가는 것을 소스 단에서 차단).

## 🚀 v2.3.0 릴리즈 완료 — **단, www.ssampin.com·m.ssampin.com 전면 다운(Vercel 402)** (2026-08-07, tag `v2.3.0` Latest, main `4ab58050`)

**출시 완료**: 오너 실검증 후 지시로 v2.2.14를 건너뛰고 **v2.3.0 단일 릴리즈**(v2.2.13 이후 65커밋 — 다른 세션 작업분 포함: 창 닫기 '완전히 종료' `26170d26`, nut-js 제거+Electron 43 `5fc323e7`/`04edefc5`, 설정 일정 카테고리 색 `2555f47c`, 라이트 모드 가독성 `8f395871`).

- **버전 6곳**(package.json·package-lock·landing config·layout.tsx·Sidebar·mobile/version.ts) + `public/release-notes.json`(하이라이트 4·변경 11) + **/docs 가이드 재삽입 완료**(보류분 3블록 + 신규 "학년도 마무리와 보관함" 섹션·FAQ 2건, 4개 문서 `lastUpdated` 2026-08-07).
- **챗봇 KB**: Q&A 5건 추가 → 키 로테이션·재배포·ingest **355/355 성공(실패 0)**.
- **빌드·배포**: GHA Windows(`31136465198`)·macOS(`31136467464`) 둘 다 success → 릴리즈 자산 8종 업로드 → **다운로드 URL 7종 전부 302 검증**, `releases/latest` = v2.3.0.
- 게이트: tsc 0 · regression 39/39 · landing docs:check + build 통과.

**🚨→✅ 별건 장애(릴리즈와 무관, 당일 해소)**: 출시 직후 **Vercel 팀 `pblsketchs-projects` 전체가 `DEPLOYMENT_DISABLED`(HTTP 402)** — www·apex·m·pblsketch.xyz 전부 402, 마지막 프로덕션 배포가 2026-07-23에 멈춰 있었고 **v2.3.0 push는 배포를 생성조차 못 함**. **Pro 업그레이드로 차단 해제**(결제 실패 아님 — 차단 중에도 `status=active`, Hobby 한도 사유). **⚠️ 이번 최대 교훈: 차단 해제 ≠ 재배포** — 차단 기간 push는 소급 배포되지 않아, 해제 직후에도 라이브는 07-23 빌드(2.2.13)였다 → **빈 커밋 `6482bdca` push로 재트리거**해 두 프로젝트 모두 배포. (배포는 git 자동연동이 살아 있음 — 옛 메모의 "CLI 수동 배포"는 정정됨.)

**배포 후 전 지점 검증 완료**: 랜딩 표시 `2.3.0`·schema.org `softwareVersion 2.3.0` · `/docs` 신규 4문구 전부 노출(수업반 보관하기·학년도 마무리와 보관함·FAQ 2건) · 모바일 번들 `v2.3.0`+학년도 전환 배너 문구 포함 · 다운로드 버튼은 `releases/latest` 링크라 버전 무관 · 챗봇 실제 질의 응답 확인(보관=삭제 아님 정확히 답변) · 앱 업데이트 카드는 `raw.githubusercontent`에서 읽어 Vercel 무관.

**남음**: 카드뉴스·Threads 릴리즈 고지 · 사후 실기기 확인.

## ✅ 학년도·학기 전환 · 보관함 — **오너 결정 반영(상시 마무리·복원 순환) + 적대 QA 6라운드 최종 GO** (2026-08-07, origin/main `f43dc29c`)

**오너 결정 2건 반영**: ①**"1년이 안 돼도 언제나 마무리 가능 + 한 번 더 팝업"** → 2학기 제한 해제(F9c), 중간 마감 시 확인 모달(통계 분할 경고·"수업반 보관" 대안·주 버튼은 수업 관리로 실제 이동), 유즈케이스는 `allowMidYearClosing` 플래그 요구 ②**"복원도 가능하게"** → 같은 학기 재보관을 **회차 디렉토리**(`2026-1-2`)로(F10a — term/archiveId 분리, Drive 불변 계약 무변경) → 마무리↔복원 **순환 성립**(기존엔 "이미 같은 학기" 거부로 막다른 길).
**그 대가로 필요했던 안전 재설계**: **①F9a — 부활 차단 기준을 "학년도"에서 "마감한 학기(lastClosedTerm)"로 교체**(이 교체 없이 제한만 풀면 QA BLOCKER B2 재발 — QA가 인과 증명: lastClosedTerm 제거 시 즉시 재현) **②F11a — 마감 시각(lastClosedAt) 병용으로 "중간 마감의 동기화 절벽" 구조 해소**(term은 date 파생이라 5월 마감 시 6~8월 신규 기록도 term='2026-1' → 학기 내내 3도메인 기기 간 전파 정지. 이제 "마감 학기 이하 AND 마감 시각 이전"만 차단, 마감 후 신규는 정상 병합. 시각 축은 도메인별 추출자 — observations만 ms) **③F11b — 보존 규칙을 "더 최신 학기 승"→"더 최신 결정 시각 승"(termGuardUpdatedAt)**으로 바꿔 복원(가드 해제)도 기기 간 전파 **④F11c** 상태 저장 스프레드에서 archiveId 소실 → 재개마다 회차 증식하던 버그 수정 **⑤F11d** 가드 필드 불일치 시 필터 비활성(fail-open).
**QA**: 적대 6라운드·누적 재현 **154건** → **최종 GO**(차단 0). 수용된 설계 대가 3건(마감 전 기록의 사후 편집 1건 부활·시계 스큐 1회성 경계·복원 LWW 역방향)은 §13.5 #24~26 등재. 최종 게이트: tsc 0 · 전체 vitest **4152 passed** · regression 39/39 · build:electron 성공. **실검증 문서 갱신 완료**(마법사 잠금 해제 절차 삭제, B-2a 확인 팝업·B-5 회차 재보관 신설).

## ✅ 학년도·학기 전환 · 보관함 — 적대 QA 4라운드 GO(1차 종결, 이후 오너 결정으로 5·6차 추가) (2026-08-07 아침, `61905b02`)

**궤적**: 구현 13/13 → 적대 QA 1차 NO-GO(BLOCKER 2·HIGH 3) → F1~F6(`e2a69dd5`) → 2차 NO-GO(B1 우회 3경로·마커 미검증·시계 축) → **F7 구조 수정**(`7aed9acd` — data:read 5바이트 휴리스틱 정밀화로 `[]` 유효화 → 리모트 정화 구조 + 전 분기 마커 게이트·시계 비교 제거) → 3차 조건부 GO(RT1 제품 결정·RT2·RM-a) → **F8**(`4df7412b` — 마커 해제에 리모트 정화 확인+DEFER 예외·seating 유효 빈 값(remove 키 0)·모바일 수신 고지 배너·즉시 정화 업로드) → **4차 GO**(재현 총 97건, 차단·상위 13건 전부 봉쇄 실증). 계약 QA(AC 90건)=차단 0·편차 §13.5 등재. 최종 게이트: tsc 0·전체 vitest **346파일 4108 passed**·regression 38/38. **핵심 교훈: ①빈 배열 파일(2바이트)이 "손상" 오판되는 5바이트 휴리스틱이 remove 리셋→리모트 미정화→부활 전 계열의 뿌리였다 ②마커 해제는 "로컬 내용" 아니라 "로컬 내용 AND 리모트 정화 확인" ③회귀 테스트는 수정 지점이 아니라 공격 표면 기준으로.** **남음(QA 밖)**: 오너 실기기 E2E(§8.4 P2 6항+P1 잔여)·실 Drive 2기기 왕복·릴리즈(v2.2.14 선출시→v2.3.0, landing docs.ts 커밋 보류분 포함).

## 🚨→🔧 학년도·학기 전환 · 보관함 — ~~QA-A 재검증 중~~ (위 항목으로 종결) (2026-08-06 밤)

**수정 완료**: F1 전환-remove 마커+치유 게이트(리모트 최신 예외·revert 정리·기록 실패=전환 중단) / F2 마법사 학년도 전환 전용(1학기 마감 차단, 이어하기·원복 보존 — UI 레벨) / F3 currentTerm "더 최신 학기 승" 보존(통파일 교체 3지점+수동 충돌, 수동 가져오기는 의도 경로라 미적용) / F4 삭제 문구 정직화(버튼 "이 PC에서 삭제") / F5 브릿지 교과 출결 create·delete 보관 거부 / F6 이어하기 소거 경고. 재현→영구 회귀 27건. 게이트: tsc 0·전체 vitest 4085·regression 38/38. **QA-A가 같은 공격+우회 경로로 재검증 중 — GO 후 릴리즈 실검증 안내.**

구현 13/13 완료 후 오너 지시로 깨끗한 컨텍스트 QA 2트랙 수행. **QA-B(계약 전수 90건)=차단 0**(편차 7건 §13.5 등재+AC-8 가드 3건 보강 `be22b189`). **QA-A(데이터 보존 적대, 재현 28건)=NO-GO**: **B1** 전환이 remove한 students/seating이 **자기 리모트 사본의 치유 다운로드**(ADR-024 경로)로 부활(qa3-D 재현) · **B2** 같은 학년도 학기 전환은 부활 필터(학년도 기준)가 무가드 — 지금 첫 실사용이 전부 이 케이스(qa3-A 재현) · **H1** currentTerm이 통파일 LWW settings라 미전환 기기가 벗기면 필터 영구 비활성(qa3-C 재현) · **H2** "영구 삭제"한 보관함이 Drive 사본에서 부활(qa4-⑤ 재현) · H3 구버전 감지=체크박스 대체(§13.5 수용) · M1 이어하기가 중단 후 입력분 소거 · M2 브릿지 출결 쓰기 보관 가드 부재 · M3/L 후속. **방어 확인도 다수**(크래시 주입 8종·아카이브 불변 8종·term 부작용 6종 전부 막힘). **수정 방향(F1~F6)**: B1=전환 removedKeys 마커(로컬 전용)+치유 다운로드 게이트 / B2=**마법사를 학년도 전환 전용으로 제한**(1학기 마감 차단 — 학기 정리는 수업반 보관, 개념 모델 §3 원복) / H1=settings 병합 시 currentTerm은 더 최신 학기 보존 / H2=삭제 문구 정직화("이 PC에서 삭제, 동기화 사본이 되돌아올 수 있음") / M2=브릿지 출결 가드 / M1=이어하기 경고 문구 → QA-A 재검증. **릴리즈 실검증은 이 수정 완료 후.**

## ✅ 학년도·학기 전환 · 보관함 — ~~ultragoal 13/13 전부 완료~~ **(위 QA-A NO-GO로 정정 — 수정 트랙 선행)** (2026-08-06, 원장 마감·품질 게이트 통과)

**P0~P4 전 구현 완료·push** (`f4d9f944`~`8c365668`, origin/main 동기). 최종: 전체 vitest **342파일 4062 passed**(트랙 시작 3828 대비 +234)·regression 38/38·슬롭 검사 reviewer-only 통과(차단 0, 경미 8건은 후속 후보). **P4 특기: ①아카이브 Drive 동기화=불변 3중 계약**(리모트 존재=업로드 스킵·로컬 존재=다운로드 스킵·archive:import 바이트 무변경 — 덮어쓰기 원천 봉쇄) **②구조·학생 승계=opt-in·기본 OFF**(자동 연결 금지, planImport 재사용) **③S4.2 모바일 보관함=별도 트랙 이연**(데이터 원천이 S4.1 Drive 산출물 — 온디맨드 읽기 설계 필요, 억지 구현 배제). **알려진 제약(후속 결정 필요): S3.3으로 삭제한 보관함이 Drive 사본에서 재다운로드될 수 있음**(Drive 개별 삭제 원시 부재 — (a)/docs 고지 (b)포트 확장 (c)툼스톤 중 택1, PC 교체 복원 관점에선 양날). **남음=오너 실검증·릴리즈**(v2.2.14 선출시→v2.3.0, 실 Drive 2기기 왕복·실기기·마법사 실행 포함). 원장: `.omc/ultragoal/`.

## 🚀 학년도·학기 전환 · 보관함 — **구현 착수 (ultragoal 13스토리, 2026-08-06)**

**[갱신 2026-08-06 저녁] 9/13 완료.** P1 전체(P0+S1.0~S1.6) 커밋·push + **실렌더 E2E §8.4 10항목 전부 PASS·결함 0**(스크린샷 41장 `docs/03-analysis/school-year-archive/e2e/`, CDP 레시피 포함 — dnd-kit 드래그는 CDP 합성 이벤트 미반응이라 동일 경로 직접 호출로 검증). P2 S2.1·S2.1b 커밋(`2d1852b3` — 아카이브 IPC 5종·{ok,error} 계약·term 불변·백업 archives 섹션[스키마 버전 유지, 검증 코드가 최상위 추가 키 비거부 실측]). **S2.2(레코드 term 스탬프) 진행 중.** **릴리즈는 오너가 직접 실검증 후 수행(2026-08-06 오너 확정)** — 에이전트는 출시 행위 금지, 준비물만 유지: v2.2.14 선출시→v2.3.0 순서, 노트 초안 docs/release-prep/, **landing docs.ts 갱신분은 라이브 배포 방지 위해 커밋 보류 중**(v2.3.0 출시 시 함께 커밋 — 워킹트리에 있음, 폐기 금지). P1.1 이월: 좁은 창 배지 줄바꿈·보관 토스트 Undo(현재 "보관함 보기") 등. **오너 지시 "/ultragoal로 모든 작업 진행"으로 착수.** 원장=`.omc/ultragoal/`(이전 동기화 하드닝 원장은 `.omc/ultragoal-archive-sync-hardening-2-complete/`로 보존). **§5.1 축소 게이트 기준일: 착수일 2026-08-06(1일차) → 5일차 = 2026-08-10(월) 18:00 — 이 시점에 S1.3 미완이면 P1-min 축소 발동+기록.** 릴리즈 공개(v2.2.14·v2.3.0 GO)는 오너 확인 후에만. **G001(S1.0) 완료**: ADR-027 계열 미커밋 8경로를 2커밋으로 정리(`48dc4331` 조용한 저장 실패 표면화 / `0f4bd85d` 모바일 PWA pendingWrites 배리어), 게이트 4종 전통과(tsc 0·lint 0에러/경고 132 기존·vitest 314파일 3828 passed 10 skipped·regression 38/38). 다음=G002(P0 academicCalendar).

## 📋 학년도·학기 전환 · 보관함 — **계획 v4 최종 확정(CONFIRMED→진행 중)** (2026-08-06, 코드 무변경)

**오너 결정 3건 반영(v4, 합의 유지)**: ①**시즌 배너 삭제**("학교마다 개학일이 달라 불필요, 기능 노출만 잘 되면 됨") — S1.5 삭제·S0.1 시즌 함수 2개 미생성·축소 게이트는 "착수 후 5일차 18:00" 상대 기준·개학일 질문 소멸 ②**그룹 보관 → 개별 보관 기본 + 체크박스 일괄 보관 + 보관함 그룹화 표시**(㉒ 쓰기 가드는 혼합 상태가 정상 경로가 되어 오히려 더 필수) ③모바일 PC 동시 반영 확정. Critic r2 잔여 R3·R4·R5도 반영 = **잔여 0**. P1 산정 7일 유지(배너 -0.5, 일괄 UI +0.5). ④⑤(시간표 안내 문구 포함·무기한+용량 표시)는 권장 기본값 채택. **오너 지시: "계획만 확정, 착수는 보류."**

**합의 루프**: Planner v1 → Architect r1(APPROVE_WITH_CHANGES — **파일 루트 epoch는 fail-open, 레코드 스탬프로 재설계** + 함정 ㉑ 과대평가 반박=일정 리스크 제거) + Critic r1(ITERATE — 화이트리스트 테스트가 부정형이라 필터 누락 원리적 미탐지·S1.4 목록 양방향 오류 7누락/2과잉·**v2.2.14는 이미 대기 중인 릴리즈**·개학일 미질문 등 15건) → v2(P1-min 기본 승격·§12 이원화) → Architect r2(조건부 APPROVE — 업로드 트리거 고아 완화책·term 파생 `date` 고정·ADR-034 선결 검증 4건 등 8건) → v3(전건 반영) → **Critic r2 최종 APPROVE**. 잔여 6건 전부 비차단(R1·R2·R6 5줄은 main이 즉시 수정 완료, R3=S1.6 시·R4=P2 시·R5=D-5 시).

**산출물**: 계획 `docs/01-plan/features/school-year-archive.plan.md`(v3, 1072줄, **PENDING APPROVAL**) · 검토 기록 `docs/03-analysis/school-year-archive/reviews/`(architect r1/r2·critic r1/r2) · 조사 `docs/03-analysis/school-year-archive/research-and-proposal.md`(v0.2). **구조**: P0 학기 규칙 → P1 수업반 보관(v2.3.0, 개학 전, 5~7일, P1-min 기본) → P2 마법사+스냅샷+레코드 epoch(∥S2.1, S2.4는 3중 게이트) → P3 뷰어(P2와 강제 묶음) → P4 확장. ADR-029~036(034는 APPROVED(방향)+선결 검증 4건). **선행 조건: v2.2.14 선출시(대기분 4건+미커밋 8경로 — 이 계획의 S1.0).** **남음**: §12-A 오너 5문항(①개학일[S1.3 전 필수] ②그룹 형제 함께 보관 ③모바일 동시 출시 ④시간표 안내 문구 ⑤보관함 용량 정책) + 실행 승인 → 구현 착수.

## 🔍 학년도·학기 전환 · 보관함(아카이브) — 4트랙 조사 완료, 설계 제안 v0.2 (2026-08-06, 코드 무변경)

**v0.2 갱신(사용자 요구 "학기별로도 바뀔 수 있어" 반영)**: 시간 축을 2단으로 분리 — **담임 축(명렬·출결·누가기록)=학년도 연속**(출결 통계·생기부가 학년도 단위 집계라 학기에서 끊으면 안 됨) vs **교과 축(수업반·진도·관찰·성적)=학기 수명**(고교학점제 학기 재편성). 전환 장치도 2개: **(A) 수업반 단위 보관**(TeachingClass.archived 플래그 — Memo/Notebook 선례, 파일 이동 없어 동기화 부활 문제 없음, epoch 불필요, **2학기 개학 전 P1 목표 — 가장 가까운 전환 이벤트가 내년 3월이 아니라 2026-09 개학**) + **(B) 학년도 마무리 마법사**(스냅샷 `data/archives/{2025-2}/`+epoch 가드, 2027-02 전 P2). epoch은 처음부터 학기 입도 `'2026-1'`(useAiBridgeConsentStore 선례 승격). 시즌 배너 2회(8/25~9/15, 2/15~3/15).

**배경**: 새 학년도(3월)에 학생·과목·담임 반이 바뀜 — 기존 기록은 삭제 아닌 보관함 보관 + 언제든 열람 + 새 환경에서 시작. **조사(서브에이전트 4트랙 병렬 — 분석만 병렬, 구현 아님)**: ①내부 구조 전수 ②해외 8개 서비스(공식 문서) ③국내 6개 + 법령·개인정보 훈령 ④아카이브 UX 패턴(NN/g·YNAB 등). **결정적 발견: 쌤핀에 학년도 개념이 0** — 수업반 studentKey(`학년-반-번호`)가 새 학년에 다른 학생과 충돌, grade-analysis는 semester만 있어 2년차에 붕괴 → **2년차 사용 필수 인프라**. **업계 표준**: "작년 반 재사용 금지, 보관+새로 만들기" + 수동 트리거+시즌 안내 + 읽기 전용 보관함+복원 + 삭제는 보관 뒤에만(2단계). 가장 닮은 모델=YNAB Fresh Start(구조 승계, 기록만 리셋, 옛 데이터 통째 보존). **제안**: 스냅샷 아카이브(`data/archives/{year}/` 파일 복사, 엔티티 필드 방식 기각) + 학년도 마무리 마법사 4단계(2/15~3/15 시즌 배너) + 읽기 전용 보관함 뷰어 + **동기화 epoch 가드**(출결·학생기록·관찰은 병합형이라 그냥 비우면 타 기기서 전부 부활 — P1 전제조건). 산출물: `docs/03-analysis/school-year-archive/research-and-proposal.md` (함정 9종·로드맵 P0~P3·미결정 5항 포함). **남음**: 사용자 방향 결정(§10 미결정 5항) → .plan.md 작성 → 구현.

## ✅ 아이콘 모드 — 호버 잔상 + "345분 후" 표기 + 드래그 고착 (2026-07-23, 2라운드, **사용자 실기기 해결 확인**, main `2252e4d9` push완료)

**신고(사용자 직접)**: ① 커서를 올리면 아이콘이 이전에 있던 자리에서 잔상이 깜빡임 ② 남은 시간이 "345분 후"로 표시. **1차 수정의 실기기 반증(같은 날)**: 잔상 잔존 + "처음 몇 번만 이동 성공, 이후 안 됨" 악화 신고 → 1차의 `setOpacity(0)` 은폐는 **transparent 창에서 setOpacity 무효(Windows)**라 효과 0(기존 fadeIn/fadeOut도 사실상 무동작이었던 것), 축소 250ms 디바운스는 확장 상태 체류를 늘려 **B-1 잔재(아래)를 더 자주 밟게 함** → 둘 다 철회.

**최종 수정 3파일**: ① `pinPresence.ts` — `formatMinutesUntil`(60분 미만 "M분 후"·정각 "H시간 후"·그 외 "H시간 M분 후") + 말풍선 3곳 적용, 테스트 4개 ② **드래그 고착(B-1 잔재) 근본 수정** — 원인: 확장 창의 마우스 통과 재동기화가 `interactiveCountRef` 카운트 기반인데, 축소 시 0으로 리셋된 채 **커서가 핀 위에 머문 채 재확장**(드래그 직후가 대표)되면 mouseenter가 재발화하지 않아 카운트 0=통과로 굳음 → 핀 클릭이 바탕화면으로 샘. 수정: (a) `electron/main.ts` `expandIconWindow`가 확장 순간 **커서가 핀 사각형 안이면 즉시 인터랙티브로 시작**(IPC 왕복 비의존, diagLog `overPin` 추가) (b) 렌더러 재동기화를 카운트 대신 **실제 DOM `:hover`** 기준으로 판정 ③ **잔상** — `IconWindow.tsx` 확장 요청 직전에 핀 스프라이트만 CSS `opacity:0`으로 숨겨 **빈 프레임을 그려 둔 뒤**(rAF 2회) 리사이즈 → 비치는 낡은 프레임에 아무것도 없음. hit-test 유지(visibility 아님), 250ms 안전망·effect cleanup에서 복원(영구 숨김 불가). 축소는 낡은 프레임 해당 영역이 빈 곳이라 즉시.

**게이트(2차)**: tsc 0 / prettier 통과 / Icon 폴더 eslint 0 / 아이콘·지오메트리 테스트 47 통과 / `build:electron` 정상 / 전체 vitest **3828 passed**·10 skipped(314파일, `--maxWorkers=4`) / regression 38/38(1차에서 통과, 이후 대상 파일 무변경). **주의: electron main 변경 — dev 검증은 `npm run build:electron` 후 electron 재시작 필요.** **함정: transparent 창에서 `win.setOpacity`는 Windows에서 무효 — 은폐·페이드 목적으로 쓰지 말 것. 잔상류는 렌더러 CSS로 빈 프레임을 만들어 해결.** **남음**: 실기기 확인(잔상 소멸·드래그 연속 10회·호버 시 핀이 ~50ms 사라지는 부작용 체감)·커밋·릴리즈 고지(v2.2.14). 미흡하면 다음 단계=오버레이 별도 창 분리(핀 창 리사이즈 자체 제거).

## ✅ 학생 기록 삭제 툼스톤 — 기기 간 삭제 전파 (2026-07-23, ADR-028, main `38b01215` push완료)

**배경**: v2.2.14 릴리즈 전 QA 잔여 HIGH 1건 — "학생 기록을 지웠는데 다른 기기에서 다시 생겨요." student-records만 삭제 표식(툼스톤)이 없어(출결·관찰기록은 이미 있음) A기기에서 지운 기록이 B기기 사본과의 병합에서 부활. 핸드오프 `docs/01-plan/features/student-records-delete-tombstone.handoff.md` 그대로 구현. **수정 3파일+테스트 2파일**: ①`StudentRecord.ts` — `StudentRecordTombstone`(**deletedAt은 ISO 문자열** — 관찰기록의 ms 숫자를 복사하면 updatedAt(string)과의 부활 비교가 조용히 항상 오판, §4-① 최대 함정)+TTL 90일 상수+`StudentRecordsData.deleted?`(optional=과거 파일 호환) ②`ManageStudentRecords.ts` — `buildStudentRecordsSaveData` 조립 함수(사라진 id→툼스톤, 재등장→걷힘, TTL GC) 신설 + **저장 6경로 전부 경유**(add/update/updateMany/delete/카테고리/cascadeTagChange — 한 경로라도 우회하면 그 저장이 툼스톤을 통째 소실) ③`SyncFromCloud.ts` `mergeStudentRecords` — 툼스톤 id별 최신 합집합 병합+부활 규칙(`(updatedAt ?? '') > deletedAt`만 생존, 동률·스탬프 부재=삭제 승=의도된 기본값·테스트 고정)+`deleted` 비었으면 미직렬화. **스토어 무변경**(데스크톱·모바일 삭제 전부 usecase `delete()` 하나로 수렴 실측)·**entity-samples 무변경**(notMirrored — 메타테스트는 등재 인터페이스만 검사, ObservationTombstone 미등재 선례 확인). `MigrateStudentRecordsSubcatToTags`는 봉투 스프레드 저장이라 deleted 자동 보존(미수정). **게이트 전통과**: tsc 0 / lint 0에러(경고 132 기존) / 전체 vitest **3828 passed**·10 skipped(314파일, `--maxWorkers=4`) / regression 38/38 / prettier 통과. **신규 테스트 17**(병합 9+저장 조립 8 — §5 필수 시나리오 6종 전부 포함: 삭제 전파·정당한 부활·스탬프 없는 옛 기록=삭제 승·TTL GC·하위 호환·툼스톤 병합). **남음**: 릴리즈 노트에 "지운 기록이 다시 생기던 문제"로 고지(/docs는 사용자 행동 무변경이라 갱신 불필요)·실기기 2대 왕복 확인.

## ✅ 피드백 #147 B-4 — 출결 이중 장부: 삭제가 한쪽에만 기록되던 문제 수정 (2026-07-22, ADR-027, 미커밋)

**신고**: "모바일에서 지웠는데 여전히 살아있거나, PC 홈화면에서 지웠는데 담임 메뉴에서 봤을 때는 여전히 살아 있는 경우". **원인 3건(핸드오프 확정 그대로)**: ①PC `deleteRecord`가 사본(`student-records`의 `att-<studentId>-<date>`)만 지우고 **원본 출결부(`attendance`)를 안 건드림** — 수정은 양방향인데 삭제만 단방향. 게다가 그리드가 그 날짜를 재저장하면 브리지가 사본을 **재생성**해 되살아남 ②모바일 `bridgeAttendanceRecord`가 **미로드 스토어의 빈 records**에서 existing을 못 찾아 present 되돌리기 삭제를 조용히 건너뜀(+비-present는 같은 id 중복 append) ③"PC 홈화면"=대시보드 담임 메모장 위젯 **확대 상태**(`DashboardStudentRecords`가 담임 업무와 같은 `StudentRecordsEditor`를 통째 렌더) — 두 진입점이 같은 `deleteRecord`를 써서 한 수정으로 함께 해결. **수정**: ①출결 브리지 기록 삭제 시 `upsertStudentAttendanceEntries`+**빈 recordsByPeriod**로 원본에서 그 학생 엔트리 제거(다른 학생은 락 안 fresh 보존) ②**원본 먼저, 실패하면 사본도 남김(fail-closed)**+오류 토스트+예외 전파 ③`upsertStudentAttendanceEntries` 반환을 `records | null`로 바꿔 `ensureWritable` 차단(조용한 no-op)을 실패로 표면화 ④학급·번호 특정 불가 시엔 정리만 건너뛰고 사본 삭제는 진행(기록을 영영 못 지우는 상황 방지) ⑤원천 조회는 **스토어가 자체 해결**(className=useSettingsStore, 명렬표=studentRepository fresh 읽기) — 호출처가 핸드오프 기재 5곳이 아니라 **실제 8곳**(`StudentRecords.tsx` 3곳 누락)이라 params 전달 방식이면 누락 진입점이 조용히 옛 버그로 남는다. `useStudentStore` import는 **순환(useStudentStore→collectExternalRefs→useStudentRecordsStore) 실측 확인**돼 회피 ⑥모바일은 스토어 진입부 로드 보장 ⑦**출결 기록 삭제의 "되돌리기" 제거**(기존 undo는 새 UUID 사본만 만들어 원본 복원 불가 → 유령 사본 발생) + 전파 고지 문구. 비출결 undo는 그대로. **명시적 비변경**: mergeAttendance(정상)·replaceDayForClass 미사용·사본 툼스톤 미신설·InputMode 기존 안내 유지. **게이트**: tsc 0 / lint 0에러(경고 132 기존) / 전체 vitest **3799 passed**·10 skipped(310파일, `--maxWorkers=4`) / regression 38/38. **신규 테스트 13**(데스크톱 10+모바일 3) + **돌연변이 검증**(수정 전 소스에서 데스크톱 8/10 실패·회귀 0용 2건만 통과, 모바일 2/3 실패). **실화면**(실제 저장 바이트 확인): 진입점 ①기록 조회 삭제→원본에서 2번만 빠지고 3번 상태·사유 보존+그리드 즉시 반영 ②**위젯 확대**에서 동일 동작 ③상담 삭제는 출결부 바이트 무변경+되돌리기 유지 ④모바일 결석→출석 되돌리기 시 사본 삭제. **미실행**: 실제 Drive 동기화 왕복(계정 필요 — 양쪽 로컬 바이트까지 확인). **함정: 한 교시가 비면 레코드 자체가 사라지고 툼스톤이 전 기기 전파(되돌릴 수 없음) → 토스트로 고지함. 전체 vitest는 브라우저·dev 서버 동시 실행 시 PDF/lesson-repo 5s 타임아웃 flaky(131파일 실패로 보이지만 정리 후 재실행 전건 통과).** **남음**: 커밋·push·릴리즈 고지·`/docs`(출결 삭제 전파 안내)·실기기.

## ✅ 피드백 #147 B-3 — 모바일 담임 출결 학생 명단 미표시 + 빈 명단 덮어쓰기 차단 (2026-07-22, ADR-026, main `01772cac`)

**신고**: "첫화면 우리반 출결 체크를 누르면 학생 있어도 명단 안 뜸"(smile837@naver.com). **원인(핸드오프 확정 그대로)**: `AttendanceCheckPage`가 담임인데도 학생을 **수업 학급 명부**(`getClass(classId)`)에서 찾는데, 홈이 넘기는 `classId`는 `settings.className`("3-5" 문자열)이고 `TeachingClass.id`는 UUID라 영원히 매치 실패 → 빈 배열 → "학생 명단이 없습니다." 같은 화면의 출결 통계 탭·PC 담임 출결·모바일 학생 탭은 전부 담임 명렬표를 쓴다(이 화면 하나만 어긋남). **함께 막은 미신고 유실 경로(§3)**: 명단이 빈 채로 "완료"를 누르면 그날 담임 출결이 `students: []`로 교체되고 `updatedAt`이 갱신돼 **동기화 LWW로 PC의 그날 출결까지 사라진다**(여러 날은 최대 30일 동시). **수정**: ① `type === 'homeroom'`만 원천을 담임 명렬표로 전환(`type === 'class'`는 코드 무변경) ② 매핑은 `number: studentNumber` + **grade/classNum 미부여**(`studentKey`가 `String(number)`여야 기존 기록이 붙는다) + `isStudentActive` 필터 + 번호 없는 학생 제외·안내 ③ **`doSave()`/`handleMultiDateSave()` 빈 명단 가드**(완료·자동저장·언마운트 flush·교시 전환 전부 이 경로) + 완료/여러 날/텍스트 버튼 비활성 ④ 명렬표·출결 **로드 완료 전 판정 금지**(로드 전 빈 배열·null 기록을 사용자 의도로 오인하지 않도록, 로딩 스켈레톤) ⑤ 빈 화면 문구를 원인별로 분리(명렬표 비었음 / 번호 있는 학생 없음 / 수업 명단 없음) ⑥ 카운터를 현재 명단 기준으로 계산("출석 3 · 전체 0" 오해 제거). **명시적 비변경**: `getTodayRecord(classId, selectedPeriod, teachingClass?.groupId)` 인자(그룹 공유 레코드 계약, ADR-023 QA2 B2)·`classId` UUID 정규화 금지(저장된 레코드 전량 고아화)·신규 저장 경로 0. **게이트**: tsc 0 / lint 0에러(경고 132 기존) / 전체 vitest 3786 passed·10 skipped(308파일, `--maxWorkers=4`) / regression 38/38 / landing docs:check 통과. **신규 테스트 11**(`AttendanceCheckPage.homeroomRoster.test.tsx`) + **돌연변이 검증**(수정 전 소스로 되돌리면 11개 중 8개 실패, 수업 경로 3개만 통과 = 결함 포착과 회귀 0 동시 증명). **실화면**(모바일 dev + IndexedDB 시드): 명단 4명 표시·저장 바이트 확인·재진입 유지·통계 탭 수 일치·**명렬표 비운 뒤 완료해도 그날 레코드 updatedAt까지 무변경**·텍스트/여러 날/수업 출결 회귀 없음. **함정: 이 파일의 기존 테스트 2개(multiDate·quickText)는 소스 원문을 grep하는 계약 테스트 — `'await saveRecord(record)'`, `setStudentStatuses(nextStatuses)` 등 고정 문자열과 "applyText 본문에 `saveRecord(` 없음" 부정 계약을 깨지 않도록 가드는 해당 줄 앞에 추가했다.** **남음**: 릴리즈 고지(v2.2.14)·**신고자 회신 — "명단이 빈 채로 완료를 눌러 저장된 날이 있는지" 확인 안내 필요(Drive 웹 '버전 관리' 리비전으로 30일 내 복구 가능)**·실기기(iOS/Android) 확인·트랙 C(출결 이중 장부 삭제 — 같은 파일 뒷부분이라 이 커밋 이후 진행).

## ✅ 피드백 #147 B-2/B-1 — 아이콘 모드 코치마크 고착 + 드래그 실패 수정 완료 (2026-07-22, ADR-025, main `857566d7`)

**신고**: B-2 "'쌤핀이예요! 클릭, 오늘 요약~' 멘트가 계속 살아 있어 화면을 자꾸 가립니다" / B-1 "쌤핀 닫았을 때 생기는 아이콘이 때때로 움직이지 않습니다"(smile837@naver.com). 핸드오프 `docs/01-plan/features/icon-mode-coachmark-stuck.handoff.md`가 **두 신고의 원인을 하나로 확정**(원인 재조사 없이 §4 수정부터 착수).

**원인**: `IconWindow` 코치마크 effect가 마운트 직후 스토어 초기값(`DEFAULT_SETTINGS...showCoachMark=true`)을 보고 말풍선을 켜고 5초 타이머 무장 → `loadSettings()` 완료로 저장값 `false`가 들어오며 dep이 뒤집히면 cleanup이 **타이머만** 정리하고 표시 상태는 `true`로 고착(끄는 코드 없음) → `wantExpanded` 상시 true → 창 340×480 확장 유지 → 확장 창은 마우스 통과가 기본이라 핀 위 IPC 왕복이 늦거나 유실되면 pointerdown 미도달 = "때때로 안 움직임"(B-1). 대상 = **코치마크를 한 번이라도 본 기존 사용자 전원**(신규는 저장값도 true라 dep 불변으로 정상 — 헤비 유저가 신고한 이유).

**수정**(4파일, 전부 `src/adapters/components/Icon/` — `electron/`·스토어 diff 0): ① 수명주기를 `useCoachMark` 훅으로 분리하고 **`loaded` 게이트** 적용(로드 완료 후 딱 한 번 판정) ② cleanup에서 타이머와 표시 상태를 **항상 함께** 내림(고착 방어) ③ **[데이터 보호]** 저장 페이로드를 `useSettingsStore.getState()`로 **발사 시점에** 읽음 — 기존 코드는 effect 생성 시점 `settings.widget`을 통째 스프레드해서, ①만 고치면 로드 전 기본값이 5초 뒤 저장되어 **사용자 위젯 설정(크기·투명도·데스크톱 모드·표시 섹션)이 기본값으로 덮어써지는 유실 경로**가 살아났을 것(update가 widget을 얕은 병합하므로 stale 스프레드가 전 필드를 민다) ④ 수동 닫기(×) 추가 — 본체는 `pointer-events-none` 유지, 버튼만 `pointer-events-auto` + 팝오버와 동일한 `interactiveEnter/Leave` 래퍼로 클릭 도달.

**검증**: 게이트 4종 — tsc 0 / lint 0에러(경고 132 기존, Icon 폴더 무경고) / 전체 vitest **3773 passed**·10 skipped(307파일; 실패 2건 JsonInteractiveLessonRepository는 부하 flaky, 단독 18/18 통과) / regression 38/38. **신규 테스트 5**(`useCoachMark.test.tsx`, 수정 전에 먼저 작성). **돌연변이 검증**으로 테스트가 공허하지 않음을 실증 — `loaded` 게이트+cleanup 복원을 빼면 테스트 1·2 실패(고착 재현), 저장 시점 읽기를 무장 시점 캡처로 바꾸면 테스트 4 실패. **실화면**: (a) 브라우저 `?mode=icon` 실렌더 — 기존 사용자 미표시(진입 직후·7초 후 모두)·신규 사용자 표시→5초 소멸→저장 시 width 999·opacity 0.42·desktopMode native-desktop **보존**·× 클릭 즉시 소멸+저장 (b) **Electron 실기동**(`npm run build:electron` → `electron:dev:fresh` 격리 프로필) — 기존 사용자 상태에서 아이콘 모드 진입 시 창이 **66×66 compact 유지**(확장 340×480 없음, 20초 관찰), PrintWindow 캡처로 핀만 표시 확인, settings.json width 999/opacity 0.42 무변경.

**함정**: PowerShell `Set-Content -Encoding utf8`은 **BOM**을 붙여 앱의 `JSON.parse`가 실패 → 시드한 설정이 통째 무시되고 기본값으로 재작성됨(`[System.IO.File]::WriteAllText`+`UTF8Encoding($false)` 사용). dev Electron은 Vite 최초 컴파일 때문에 창 표시까지 **5~6분** 걸림. `MainWindowTitle`로 프로세스를 찾으면 **설치판 실앱**이 잡힘(격리 dev 인스턴스는 electron.exe) — 창 핸들을 직접 지정할 것.

**남음**: 실기기 확인(핀 드래그 10회·단일/더블/우클릭 — 사람 손 필요, §5-3 잔여 3항목)·커밋·릴리즈 고지(v2.2.14 "아이콘 모드 안내 말풍선 고착 + 드래그 실패 수정")·신고자 회신. 형제 트랙 2개(모바일 담임 출결 명단·출결 이중 장부 삭제)는 미착수.

## ✅ 모바일 동기화 안 됨 신고(문혜인) — 원인 규명 + 수정 완료 (2026-07-21, ADR-024, 미커밋)

**신고**: PC의 할 일·일정이 폰에 안 내려옴(폰 "업로드 결과 변경 없음 (28)", 다운로드 눌러도 무반응, mhi0826@gmail.com 개인 계정 연결 정상). **원인(재현 테스트로 확정)**: `SyncToCloud`가 업로드 0건(no-op)이어도 매니페스트를 무조건 재작성하며 `{...remote.files, ...local.files}` 병합본을 리모트/로컬 양쪽에 저장 → ① 받은 적 없는 리모트 항목이 폰 로컬 장부에 "받았음"으로 승계 → 이후 다운로드가 checksum 동일 판정으로 **영구 스킵**(재로그인·재시도로 절대 안 풀림) ② 낡은 로컬 항목이 리모트의 더 새 항목을 되돌림 ③ 리모트 deviceId가 no-op 업로더로 찍혀 "내가 올린 데이터" 스킵 오판. 모바일 자동 동기화가 업로드 전용(useSyncTrigger interval→syncTo만)이라 오염 업로드가 거의 확실히 끼어듦. **v2.0.4(`f0d9402c`)부터 있던 설계 결함**(최근 하드닝 회귀 아님). **수정(데이터 파일 쓰기 규칙 일절 무변경 — 데이터 유실 0 제약)**: ① no-op이면 매니페스트 미작성 ② 리모트 장부=리모트 기존+이번 업로드분, 로컬 장부=로컬 기존+이번 업로드분(장부 병합 금지) ③ `DriveSyncFileInfo.uploadedBy`(optional) 파일별 작성자 스탬프 — 다운로드 스킵 판정은 uploadedBy 우선, 부재 시 deviceId 폴백(안전 방향) ④ 오염 자가 치유 — checksum 동일이어도 로컬 파일 부재면 다운로드(정적·동적·바이너리 3루프, 로컬 존재 시 종전대로 스킵=덮어쓰기 없음). **기각 가설**: 레지스트리 누락(28개 전부 등재)·모바일 UI 부재(TodoPage/SchedulePage+reload 존재)·학교 계정 차단(개인 전환 완료). **게이트**: tsc 0 / lint 0에러(경고 132 기존) / 전체 vitest 3770 passed·10 skipped(306파일, --maxWorkers=4) / regression 38/38 / 신규 테스트 7(syncManifestLifecycle.test.ts — 신고 시나리오 치유+데이터 보존 잠금). **함정: 신규 기기 첫 업로드의 whole-file 클로버 경로(deferred 미발동)는 의도적 미변경(별도 PDCA — 방향 전환도 트레이드오프)·미사용 ResolveSyncConflict 유즈케이스는 동종 결함 보유 데드 코드(재사용 시 ADR-024 정합 필수).** **남음**: 커밋·push(사용자 확인)·선생님 안내(임시 조치: 폰 자동 동기화 끄기→PC에서 할 일/일정 수정→업로드→폰 다운로드, 또는 PWA 갱신 후 다운로드 한 번)·릴리즈 고지(v2.2.14)·실기기 확인.

## ✅ 동기화 2차 하드닝 QA2(데이터 보존) — Codex NO-GO 3건 수정 완료 (2026-07-14 오후)

**구현 완료본에 "기존 사용자 데이터가 사라질 수 있는가" 단일 관점의 Codex(gpt-5.6-sol) QA**(6개 시나리오, 인라인 tsx 실행 재현, 아티팩트 `.omc/artifacts/ask/codex-*sync-hardenin-2026-07-14T05-06-*.md`) → **NO-GO(BLOCKER 2·HIGH 2·MED 1)** → 코드 교차 검증 후 3건 수정, 2건 잔여 등재(plan §12.5/R6/R7). ① **[B1] deleteByClass 공유 그룹 출결 오삭제**(그룹=같은 교실의 여러 과목 — 다른 과목이 남아도 물리 classId 필터가 공유 레코드 삭제+툼스톤 전파, 베이스에도 있던 결함을 본 트랙이 재구현하며 승계) → fresh 학급 목록의 "살아남는 그룹" 집합으로 그룹 레코드는 그룹 생존 여부로만 판정(판정 불가 시 전체 보존 fail-closed). ② **[B2·신규 회귀] 모바일 교차 반 저장이 그룹 레코드를 1명짜리로 교체**(모바일 4곳이 classId로만 기존 기록 조회 → 다른 과목 명의 공유 레코드를 놓침, 스윕 S4 groupId 주입이 교체를 엶) → 도메인 헬퍼 `findAttendanceRecordForClass`(그룹 키 우선) 신설·모바일 조회 4곳 전환 + usecase `upsertRecord` 교체 시 기존 classId 승계(키 불변=툼스톤 0) + S4 주입은 레거시 비그룹 매치 없을 때만(이중화 차단). ③ **[H3] 어댑터 3종(Electron/LocalStorage/IndexedDB)+electron main data:read가 읽기 오류를 null("파일 없음")로 위장** → intent 저장 경로가 빈 파일로 오인해 편집분만으로 덮어쓰기 가능(락 fresh-read 전환이 노출면 키운 본 트랙 소유) → **null=부재만, 오류=예외 전파**로 계약 변경(읽기 실패 시 쓰기 0회 fail-closed, main은 원본·백업 복구 실패 시 throw). **잔여**: R6 멀티탭/멀티컨텍스트 무직렬화(v2.2.13에도 락 없음 — 미악화, Web Locks 후속)·R7 미래 필드 roundtrip 계약(현행 필드는 전수 보존 확인). QA가 실행으로 확인한 보존 경로: 구 스키마 왕복·첫 동기화 합집합·mapless/혼합 버전=record-LWW 동일(P4 바닥). **테스트 +9**(deleteByClass 3·upsertRecord 2·읽기 실패 fail-closed 1·어댑터 계약 6[신규 파일]). **주의: electron main 변경 — dev 검증 시 build-electron+재시작 필요.** 게이트/커밋 해시는 아래 참조.

## ✅ CI 46일 연속 실패 원인 규명 + 수정 (2026-07-14)

**증상**: GitHub Actions CI 워크플로가 2026-05-29(`7045480c`)부터 46일간 전 커밋 실패(마지막 성공 05-28). 알림은 "최근 6회"였으나 실제 스트릭은 훨씬 김. **원인**: `XlsxExporter.bundle.test.ts`(번들 격리 게이트 vitest leg)가 빌드 산출물 부재 시 throw 하도록 설계됐는데(silent skip 금지), ci.yml verify 잡은 빌드 없이 vitest만 실행 → `dist/assets`·`dist-student/assets`가 CI에 존재한 적 없음 → 매 런 동일 3건 실패. 로컬은 과거 빌드 잔존물로 항상 통과라 46일간 미발견. 번들 격리 자체는 무결(정본 게이트 `check-bundle-isolation.mjs`는 Build 워크플로 postbuild에서 계속 통과). **수정**: CI 환경(+산출물 부재)에서만 `it.skipIf`로 사유 출력 후 skip, 로컬 부재는 기존대로 throw 유지. 3시나리오 실검증(로컬+dist=3통과 / CI+무dist=3스킵 exit0 / 로컬+무dist=3실패). **게이트**: tsc 0·lint 0(경고 132 기존)·vitest 전체 3750/3760 통과(10 skip, 기본 병렬은 부하 시간초과 flaky → `--maxWorkers=4`로 클린 통과, 실패분 단독 재실행 전건 통과)·regression 38/38. **함정: 전체 vitest 기본 병렬은 머신 부하 시 PDF·grid 테스트 5s 타임아웃 flaky — maxWorkers 축소나 단독 재실행으로 판별.** 남음: push 후 CI 초록 확인.

## ✅ 일정 카테고리 색 수정 버그 3종 세트 — 수정·검증·**push 완료** (2026-07-14, main `e19a5717`+`2555f47c`+`8f395871`)

**① 모달 색 수정 불가(신고)**: 일정 > 카테고리 관리에서 색상 팝업의 색을 클릭해도 반응 없음 — 실렌더 100% 재현. 원인 = 공용 `Modal`의 FocusTrap이 `allowOutsideClick` 미설정(기본 false)이라 body로 포털된 색상 팝업 클릭을 트랩 밖 클릭으로 차단(capture 단계 preventDefault+stopImmediatePropagation → React onClick 미도달). 수정 = focusTrapOptions `allowOutsideClick: true` 1줄(`e19a5717`). 백드롭/ESC 닫기 회귀 없음. **함정: 모달 안 body 포털 드롭다운은 이 옵션이 전제 — 제거 금지.**

**② 설정 캘린더 탭 색 수정 UI 부재(후속 지시)**: 일정 모달의 색상 팝업을 공용 `CategoryColorPicker`(common/)로 추출, 설정 > 일정 탭 카테고리 행에도 장착(`2555f47c`). 두 입구가 같은 부품·같은 저장 경로(updateCategory) 사용, Settings shared COLOR_MAP 중복 제거.

**③ 라이트 모드 옅은 색 텍스트 가독성(신고+근본 개선 지시)**: NEIS 안내문(text-yellow-200/80)이 라이트에서 노랑 위 노랑 — 원인 = alpha 변형이 정확 매치 override를 빠져나감. 전수 감사로 amber-400(164곳 override 부재)·sky 계열 등 동종 구멍 확인. 수정(`8f395871`) = index.css 3세대 보정을 유채색 17종 통합 구역으로 재작성 + **선택자를 앞경계 고정 쌍(`[class^='text-X']`+`[class*=' text-X']`)으로 전환**(alpha 변형 포착, hover:/group-hover: 제외 — 단순 `[class*=]`는 삭제 버튼 hover:text-red-400 평상시 색까지 오염) + 누락 8조합 추가. **MT-D2 메타테스트를 유채색 17종 × 100~400로 확장** — 이후 보정 없는 옅은 텍스트는 테스트 실패로 차단(재발 방지 구조화). 중성색(slate 등)은 상시 어두운 배경 사용이 많아 의도적 제외.

**검증**: 실렌더 라이트/다크 각각 — 색 변경·영속(파일 바이트)·안내문 #92400e 회복·amber-400→#b45309·다크 원색 유지·hover 무오염. 게이트 tsc 0 / lint 0에러(경고 132 기존) / vitest 3726 passed(303파일) / regression 38/38. **남음**: 릴리즈 노트 고지(v2.2.14 — 색 수정 복구·설정에서도 색 변경 가능·라이트 모드 가독성)·선생님 회신·실기기 확인. 푸시 시 타 세션 동기화 하드닝 A1~B1 커밋 6개 동승 게시됨.

## ✅ 동기화 2차 하드닝 — **구현 완료** (2026-07-14, ultragoal 8스토리, main `f78b2df6`~, ADR-023, 미푸시)

**rev.5 CONSENSUS 승인 → ultragoal 8스토리 완주.** A1 락 프리미티브(`f78b2df6`: usecases/shared/fileWriteLock+SYNC_FILE_KEYS 정본+유닛 8) → A2a 락 배선(`d27b1c2d`: SyncFromCloud 병합 6지점·ManageObs/Att 본문·MSR 전역 락·cascade intent 이관·migrate 락 내부화+통합 테스트) → A2b 출결 intent(`c04e92c0`: upsertRecord/replaceDayForClass/upsertStudentEntries[F3 부분 갱신]/deleteByClass, saveAll·saveDayBatch 삭제=K1) → A2c 관찰 intent(`a781d934`: add/removeCustomTag·addCustomCategory, saveCustom\* 삭제) → A3 실측(`f9659e77`: 보조 창 3도메인 미기록 확정→CAS 불채택) → B1 스키마(`34759e86`: FieldUpdatedAt 별칭+notMirrored+픽스처) → B2 원자(`95a98cec`: 3분기 병합+H3 materialize+H4 derive+{before,after} 의도 스탬프+호출부 10곳+field.test 15종) → **최종 게이트**: 전체 vitest 3742(기지 flaky 2 단독 통과)·tsc 0·lint 0·regr 38·**실렌더**(그리드 연속입력 저장칩 정상·파일 바이트 무유실·타 반/타 날짜 보존·나이스 토글 fieldUpdatedAt 스탬프 실확인)·**브릿지 레포 게이트 579**. **최종 xhigh 코드리뷰**(파인더 7앵글+검증+스윕, 후보 40여) → **수정 20건 반영**: ①브릿지 재구성→existing 스프레드 승계(truthy 탈락·미승계 필드가 "삭제 의도"로 오인돼 위조 스탬프 찍던 버그 — 7앵글 수렴) ②no-op 재저장 무스탬프(updatedAt 인플레이션 차단) ③맵 신설 백필(업그레이드 경계 P4) ④**mapless 페어=record-LWW 폴백**(스윕 S2: 백스톱이 LWW 승자 항목을 뺏는 P4 위반 → (b)분기 교체, 계획서 §5.1 구현 노트) ⑤upsertStudentEntries period 충돌 병합(그룹+레거시 공존 유실) ⑥모바일 saveRecord→upsertRecord(그룹 파괴)+usecase groupId 폴백 주입 ⑦deleteByClass fresh fail-closed 판정 ⑧cascadeTagChange applyRecordChange 경유(스탬프 규율 단일화) ⑨stable stringify·lockKey 파라미터 제거·TRACKED_GROUP_FIELDS 정본·mergeAndWriteLocked 헬퍼(filename=락 키 겸용)·ensureWritable·빈 하루 비우기 warn·공허 mock 갱신 등. **함정 공유**: 리뷰 픽스가 테스트 가짜 저장소의 미구현 메서드(getClasses)를 밟음→가짜에 추가·백필 도입 시 기존 "무관 그룹 미스탬프" 기대가 낡음→계약 갱신. **잔여(후속)**: update 대상 부재 무음 no-op·updateRecord before=폼 스냅샷 미전달·ObservationForm 태그 실패 무표면·load 읽기 락 밖 화면 되돌림·wedge(영구 pending)·R5(teaching-classes/progress·타 세션 소관 2건은 리포트 전달). **남음**: push(사용자 확인)·실기기·릴리즈 고지·/docs.

## 📋 (이력) 동기화 2차 하드닝 — QA NO-GO 반영 재합의, rev.5 CONSENSUS (2026-07-14 오전, 계획 단계)

**(최신) rev.4~rev.5 재합의 라운드 완료**: QA 8건(B1·B2·H3·H4·H5·M6·M7·L8) 반영한 rev.4 → Architect 재검토(APPROVE_WITH_CHANGES — **B2 before/after 화면-화면 diff·H3 결과맵 materialize 2단계 수렴을 증명으로 확인**, 잔여 F1~F6) → Critic 재평가(조건부 APPROVE — F1~F6 전부 타당 확인 + 신규 K1~K3) → rev.5 마감(9개 지시 전부 반영, 오케스트레이터 계획서 실물 검수 완료). **핵심 설계 전환(P6): "저장은 통째 스냅샷이 아니라 변경 의도(intent)로 표현하고, 의도를 락 안의 fresh 상태에 적용한다"** — 스토어 whole-array 저장은 스토어-대면 도달 불가(K1 필수), update는 {before,after} 시그니처, 출결 단일 학생은 per-student upsert intent(F3), 스코프는 "record-merge 3도메인+우회"로 정직화(F1 — teaching-classes/curriculum-progress는 1급 잔여 R5·후속 PDCA), P6 반환값 갱신의 그리드 UX 회귀는 실렌더 게이트(F4). 스토리 8개: A1(락+SYNC_FILE_KEYS)→A2a(락 배선)→A2b(출결 intent)·A2c(관찰 intent)→(조건부 A3 CAS)→B1(스키마)→B2(병합+스탬프 원자)→(선택 C정렬). 두 리뷰어 합의: 계획-단계 추가 QA 불요, **구현 후 §8 통합테스트+F4 실렌더+릴리즈 전 E2E가 최종 안전망**. 합의 아티팩트: scratchpad architect-review-3·critic-review-3·planner-revision-3/4. **다음: 오너 승인 → 구현 세션.**

**(이력) Codex QA(gpt-5.6-sol, xhigh) 계획 QA 결과 = NO-GO** (아티팩트 `.omc/artifacts/ask/codex-read-the-utf-8-korean-qa-brief-*2026-07-14T00-10-56*.md`, 브리프 `.omc/qa-brief-sync-hardening-2.md`). 합의 3자(Planner·Architect·Critic)가 놓친 구조 결함 2건을 실행 재현으로 확증: **[BLOCKER-1]** A2가 유스케이스 본문만 잠가도 스토어가 락 밖 in-memory 스냅샷으로 저장 페이로드를 만들어(useTeachingClassStore:659~694 saveAll·:724~753·useObservationStore:110~133) 낡은 배열이 병합본을 덮음 — 실증: 저장소 c1,c2에 낡은 [c1] saveAll → c2 소실. 락 위치가 아니라 **"통째 스냅샷 저장 → 변경 의도(patch) 저장" API 전환**이 필요. **[BLOCKER-2]** B3의 current/input diff(시그니처 불변)는 "디스크와 다름=사용자 변경"으로 오인 — 동기화 직후 낡은 화면 레코드(reportedToNeis:true 잔존)로 무관 편집만 해도 체크 부활+새 스탬프까지 찍혀 9ce4c1cf 회귀를 강화. {before,after}/typed patch 수용으로 시그니처 변경 필수. **[HIGH×3]** 병합 결과 fieldUpdatedAt 맵 출력 규칙 부재(2단계 병합 퇴화)·documentSubmitted 재계산은 deriveDocumentSubmitted 정본 사용(빈 배열 [].every()===true 함정, attendanceDocumentPolicy.ts:106~112)·경로②(map-drop) 구버전 무관 편집도 체크 부활 가능(P5 "완전 보존" 과대). **[MED×2]** SYNC_FILES는 readonly string[]라 개별 키 정본 없음(SYNC_FILE_KEYS as const 필요)·B2/B3 원자 출시 vs 스토리별 커밋 규칙 충돌(한 스토리로 병합). **[LOW]** SyncFromCloud.ts:402 인용 오류. 계획이 옳게 잡은 것(QA 확인): type 별칭·notMirrored·fresh-read·비재진입·B2+B3 동시 출시 원칙. **계획서 상태 ⛔ QA NO-GO로 변경(승인 보류), 다음: rev.4 개정(두 BLOCKER의 공통 뿌리 = 저장을 변경 의도로 표현) 후 재합의.** 함정: BLOCKER-2의 낡은 화면 덮어쓰기는 **오늘 코드에도 있는 잠복 버그**(전 기록 교체형 update가 원인) — rev.4는 이것까지 닫는 설계여야 함.

핸드오프(`docs/01-plan/features/sync-hardening-2.handoff.md`, `2cccac0d`) 기반 /ralplan 합의 2라운드 완주(Planner→Architect→Critic ×2, deliberate 모드). 산출물 **`docs/01-plan/features/sync-hardening-2.plan.md` rev.3 — 상태 CONSENSUS(pending approval)**. **채택안**: A-트랙=`src/usecases/shared/fileWriteLock.ts` 공용 파일별 async 락(withFileLock, 락 키 정본=`@usecases/sync/syncRegistry` SYNC_FILES)으로 전 쓰기 유스케이스+SyncFromCloud 병합 쓰기+우회 경로(cascadeTagChange는 락 내부 fresh-read 재계산 필수·migrateStudentRecordsOnLoad)의 read~write 임계구역 직렬화, ManageStudentRecords는 락 키 DI 주입으로 전역 키 통일(비재진입 규율 -Unsafe 승계), electron main CAS 백스톱은 "위젯이 공유 파일을 실제 쓰는가" 실측 선결 게이트로 조건부. B-트랙=StudentRecord에 최상위 `type FieldUpdatedAt` 별칭(notMirrored, 인라인 객체 금지 — 메타테스트 첫-brace 절단) 추가, 병합은 맵 유무 3분기 유효시각(양측 맵→항목 스탬프만/한쪽 부재→record.updatedAt 백스톱/키 없음→createdAt)+record-LWW 승자 BASE에 추적 항목만 오버레이+documentSubmitted 재계산, 스탬프는 update/updateMany 내부 old-new diff 중앙화(시그니처 불변·맵 부재 입력이 기존 맵 미소거), followUp 3필드=한 그룹. **B2+B3 원자 출시 필수**(bridge 맵 드롭으로 B2 단독=LWW 퇴화). 구현 순서 A1→A2→(B1→)B2+B3원자→(조건부 A3)→(선택 C정렬). 잔여 1급 R1(구×신 좁은 교차 "낡은 승자" — 손실 아님·의도적 교환)+OQ 5건([backup-import] main 직접 쓰기 경로 포함). 합의 아티팩트: scratchpad architect-review-1/2·critic-review-1/2·planner-revision-1/2. **다음**: 오너 계획 승인 → 별도 구현 세션(ADR-023 승격 포함).

## ✅ 모바일 담임 출결 텍스트 입력 + v2.2.13 동기화 변경 모바일 영향 검토 (2026-07-14)

**① 영향 검토(Explore 2병렬, 코드 근거 확보)**: v2.2.13 동기화 하드닝(mergeObservations/mergeCategories·툼스톤·updatedAt 스탬프·시작 스냅샷)은 모바일과 **usecase·병합 코드 공유**(useMobileDriveSyncStore가 동일 SyncFromCloud/SyncToCloud 인스턴스화, 3도메인은 conflictPolicy 무관 레코드 병합)라 **회귀 없음·순 이득**. 모바일 저장은 ManageObservations/ManageStudentRecords/ManageAttendance 경유라 봉투 필드(customTags·customCategories·tombstones) 보존 — AI 브릿지 계열 유실 버그 재현 안 됨. updatedAt은 UI가 생략해도 usecase가 스탬프(출결 실측: 저장 바이트에 updatedAt 확인). **잔여(비차단)**: PWA 버전 스큐(구 SW 번들이 구 병합으로 덮을 수 있음, 자기치유·MEDIUM)·모바일 로컬 시작 스냅샷 대응물 없음(LOW)·**담임기록(records) 툼스톤 부재로 모바일 브릿지 삭제가 병합에서 부활 가능(사전존재, 동기화 2차 하드닝 트랙과 동일 계열)**·obs 첨부 바이너리 모바일 미동기(사전존재).

**② 모바일 텍스트 출결 입력(담임 전용)**: 데스크톱 출결 그리드의 텍스트 입력을 모바일 담임 출결(AttendanceCheckPage)로 이식. (a) `parseAttendanceQuickText`에 `QuickTextParseOptions.requirePeriod`(기본 true=데스크톱 불변) 신설 — 모바일은 하루 단위 상태 1개라 교시 생략 허용(requirePeriod:false, 기준=조회), 계약 테스트 14→19. (b) 헤더 `edit_note` "텍스트" 버튼(담임 출결 전용, type==='homeroom') → 바텀시트(문법 안내+textarea+미리보기+N명 적용). 적용은 **기존 상태맵(setStudentStatuses/Reasons/Memos)+scheduleSave 경유 — 신규 저장 경로 0**(비고 생략 시 기존 메모 비움=데스크톱 행 재작성 의미 동일). 미리보기는 교시 미노출 재조립(`이름 — 종류(사유) · 비고`). (c) designer 리뷰 반영: actionToast 개명+톤 해요체 통일·sr-only 파싱 요약(role=status). (d) 메타테스트: multiDate 소스 계약 갱신(useBottomSheet 3합성)+`AttendanceCheckPage.quickText.test.ts` 신설(파서 공유·담임 전용·기존 저장 경로 강제 6케이스). **실렌더 검증**(Playwright, IndexedDB 시드 명단 6명): 미리보기 정상4/오류2(명단 없음·동명이인)→4명 적용→상태·사유·메모·카운터 정확→2초 자동저장 후 attendance 파일 바이트+updatedAt 확인, 라이트/다크 스크린샷 3장(`docs/03-analysis/mobile-attendance-quick-text/`). **게이트**: tsc 0 / lint 0에러(경고 132 기존) / vitest 전체 3702 passed(실패 2=multiDate 계약 갱신으로 해소+FillFormFields 기존 flaky 단독 통과) / regression 38/38 / build:mobile. **남음**: 실기기 확인(iOS 키보드 ↔ 시트 가시성 — designer MED 지적, visualViewport 미대응)·다음 데스크톱 릴리즈 고지.

## 🚀 v2.2.13 릴리즈 완료 (2026-07-13, tag `v2.2.13` Latest, main `8177b158`)

**현재 출시 v2.2.13.** 헤드라인=출결 그리드 전면 개편(팔레트·자동 저장·좌석 뷰, 기록 탭 기본=출결)·기록 조회 검토 모드·동기화 데이터 보호(수업 기록/카테고리 유실 수정+삭제 전파+매일 스냅샷+AI 브릿지 유실 수정)·출결 통계(생기부 집계·개근·내보내기·증빙 체크리스트). **SOP 8단계 전부 수행**: 버전 7곳→release-notes.json(highlights 4·changes 13, 위치 이동·부활 정책 고지 포함)→/docs 3문서(google-sync에 기록 소실 복구 안내 신설)→KB Q&A 5건+재임베딩 348/348(키 로테이션)→게이트(tsc0/lint0/regr38/vitest3698, RenderTemplate flaky 단독 6/6)→릴리즈 커밋 `8177b158` 푸시(타 세션 출결4종·조회 QA 후속 동승)+브릿지 `dd8fd92` 푸시→GHA Win/Mac→자산 8개 업로드→**URL 302 6/6·랜딩/docs 라이브 확인(랜딩은 push 자동 배포 확인됨)**. 실기기 확인은 오너 판단으로 릴리즈 후 진행. **남음**: 박창현 선생님 복구 지원(업데이트 안내→Drive 버전 수령→복구본), 사후 실기기 확인(팔레트·좌석·검토 모드·backups/startup), 후속 트랙(동기화 2차 하드닝=쓰기 직렬화+필드 충돌 HIGH·인앱 복구·카테고리 툼스톤·브릿지 npm publish).

## 🚨 사용자 데이터 소실 신고 — 원인 규명 + 재발 방지 + 삭제 전파(툼스톤) 구현·커밋 완료 (2026-07-13, main `d5a115c8`+`72c65870`)

**(추가) 수업 기록 삭제 전파 툼스톤(`72c65870`, goals 4/4, architect APPROVED)**: 레코드 병합 전환의 트레이드오프("지운 기록이 병합으로 부활")를 attendance 툼스톤 패턴 이식으로 해소 — ObservationTombstone{id, deletedAt ms}+TTL 90일, `buildObservationSaveData`(사라진 id 툼스톤·재등장 제거·TTL GC·봉투 명시 재조립, ManageObservations 6개 저장 경로 전부 경유 — 데스크톱/모바일 스토어 공용, aiBridge는 add 전용이라 전 삭제 경로 커버), `mergeObservations` 툼스톤 병합(updatedAt>deletedAt만 부활·동률 삭제 승). 계약 메타테스트는 ObservationRecord만 검사(봉투 필드라 ENTITY_FIELD_CONTRACT 갱신 불필요 확인). 테스트 +10(스위트 72/72). 게이트 tsc0/lint0/vitest3666(실패 2파일=autosave·RenderTemplate 기존 병렬 flaky 단독 통과)/regr38. **리뷰 비차단 메모**: updatedAt 스탬핑 불변식은 주석 가드로 명문화(새 write 경로 추가 시 필수)·혼합 버전 전환기엔 구버전 삭제가 전파 안 됨(attendance 도입 때와 동일 성질). **릴리즈 고지 대상 축소**: 이제 부활 가능성은 카테고리·커스텀 태그·담임 기록(records)에만 해당(수업 기록은 삭제 전파됨).

**Codex(gpt-5.6-sol, xhigh) 외부 QA 완료** (아티팩트 `.omc/artifacts/ask/codex-electron-react-*2026-07-13T07-14-43*.md`): SyncFromCloud 3분기 병합·6개 저장 경로·10만건 성능(80ms)은 실행으로 정상 확인. 블로커 판정 4건 트리아지 — ① **AI 브릿지 닫힌 앱 직접쓰기가 record.category·customCategories·툼스톤을 떨굼(기존 잠복 유실 버그) → 수정 완료: 브릿지 `dd8fd92`(master)+본체 번들 `2edf0d77`** — appendObservation만 정규화 재조립이었고(나머지 직접쓰기는 이미 verbatim), directWrite readListRoot 계약으로 통일(raw 봉투 보존 append+fail-closed 2종, 미러 category 미노출은 의도 계약 유지). 게이트: 브릿지 build/typecheck 0·vitest 579(신규 3)·격리 스모크 connected(stdio) 2회. 함정: 번들 한글은 \uXXXX 이스케이프(grep 직접 안 잡힘)·`git show|Set-Content` 복사 손상(Copy-Item)·결정적 번들이라 diff 16줄 정상. npm 게시본은 다음 publish 때 반영 ② 시계 오차 부활 = attendance 동일 기존 구조(수용) ③ 동시 저장 경쟁(sync 병합 write vs 사용자 저장) = 전 도메인 기존 구조 — 타 세션 발견 bulk 원자화(P1)와 같은 계열, **파일 쓰기 직렬화/CAS 통합 트랙 후보** ④ 당일 재시작 스냅샷 오염 → **즉시 수정 `9550e6fb`**(하루 1회 first-write-wins+로컬 날짜). 주의 5건은 기존 정책/후속 후보로 분류.

**재발 방지 구현(goals 5/5, architect APPROVED)**: ① `mergeObservations` 신설 — observations(수업 기록)를 파일 통째 교체(LWW)에서 id 단위 병합(updatedAt ms 최신 우선·동률 preferRemote·customTags/customCategories 합집합)으로 전환, 충돌·매니페스트 미등록 가드·최초 다운로드 3지점 연결 ② `mergeCategories` 신설 — categories를 id 합집합(같은 id는 remote name/color+subcategories 합집합, 빈 배열 가드)으로 교체 ③ electron `createStartupBackups`에 student-records·observations 추가 + `data/backups/startup/{파일}.{날짜}.json` 날짜별 스냅샷(14일 보존 GC) 신설 — 1세대 `.backup.json`이 재시작 시 오염본으로 덮이는 위험 해소 ④ 신규 테스트 14케이스(유실 시나리오 재현·방지). **게이트**: tsc 0 / lint 0에러 / vitest 3623 passed(FillFormFields 기존 flaky 단독 10/10) / regression 38/38 / build-electron OK. **선생님 회신은 사용자가 발송 완료(답 대기)**. **남음**: v2.2.13 릴리즈 포함(9ce4c1cf도 미출시라 함께 나감) + 릴리즈 노트 고지("지운 기록·카테고리가 동기화로 되살아날 수 있음" 무-툼스톤 정책, 복구 안내) + 실기기에서 backups/startup 폴더 생성·GC 1회 확인. **후속 후보(비차단)**: observations 툼스톤(attendance식 deleted[]) 별도 PDCA·obs-attachments 메타 JSON 여전히 통째 교체·병합 출력 정렬(무해한 재업로드 진동 제거)·인앱 복구 기능(Drive 리비전 — drive.file 스코프로 가능 확인).

박창현 선생님(용인한국외대부고, cavempt@hafs.hs.kr, 7/13 11:38 메일) 신고: 업데이트 후 ① 담임 기록 커스텀 카테고리 소실 ② 수업 관리 수업 기록 학생별 메모 전부 소실. 학교 노트북↔집 데스크탑 2대를 구글 동기화로 사용. **원인(코드 확정, Explore 4개 병렬 조사)**: ① 카테고리 = `student-records.json`의 `categories` 배열인데 `mergeStudentRecords`가 records는 id 병합·categories는 **remote-first 통째 교체**(`SyncFromCloud.ts:68`, v2.0.4부터) + `RecordCategoryItem` 타임스탬프 부재 → 기본 카테고리만 아는 기기가 카테고리 관리 동작 한 번이라도 하면 기본 4종 배열을 업로드해 커스텀 전체 소거(records는 생존 — 신고 증상 일치). ② 수업 메모 = `observations.json`(ObservationRecord, updatedAt 있음)인데 레코드 병합 특례가 student-records·attendance 둘뿐이라 **파일 단위 LWW 통째 교체**(`SyncFromCloud.ts:258-275`) + 툼스톤 없음 → 구/빈 파일이 mtime 역전으로 이기면 전량 소실. ③ 9ce4c1cf(updatedAt 병합 수정)는 **v2.2.12(4644e707) 미포함**(merge-base 확인). "빈 로그인 업로드"는 신규기기 가드+null 스킵으로 차단됨 — 사용자 가설은 방향만 맞고 경로는 LWW/카테고리 교체. **복구**: Drive 동기화가 일반 폴더 `쌤핀 동기화`에 files.update(PATCH)라 **Drive 웹 "버전 관리"로 이전 리비전 복구 가능(30일/100리비전 시한)** + 로컬 `%APPDATA%\쌤핀\data`의 `*.backup.json`(1세대·저장 시 덮임 — 앱 실행 금지 안내 필요). **다음**: ① 선생님 회신(보존 수칙+Drive 버전 확인 유도 — 초안 작성됨) ② v2.2.13 핫픽스 후보: observations 레코드 병합 신설(SyncFromCloud.ts:221·338 두 지점, attendance 패턴)+categories 항목 병합/합집합+9ce4c1cf 출시+startup 백업 대상에 observations·student-records 추가+빈/축소 업로드 가드.

## ✅ 담임 기록 조회 탭 리디자인 안 B — ralph 전 구간 구현·architect APPROVED (2026-07-13, main `f09e2c66`~`e4f6aed8`, **push 완료** `9ce4c1cf..83f044d0`)

오너가 안 B(찾아보기/검토 모드 분리) 전체 승인 → /ralph로 5단계+검증 완주. **1단계**(`f09e2c66`): 배너 2종→접힘 스트립(variant, 통계 탭 무회귀)·ActionDashboard 학생 선택 시 유지·학생 드롭다운 lg미만 전용·비출결 저장 토스트. **2단계**(`7c681fbe`): RecordCompletionBadge 공용화(중복 JSX 2곳 제거)·로컬 StudentJumpList→공용 RecordStudentJumpList(+isVacant 화이트리스트 정리, isStudentActive 사용)·공용 점프리스트 bg-sp-accent/10 투명버그 정정·useRecordInlineEdit 훅(prop 30→11). **3단계**(`349c2168`): 필터 상시 4종+RecordFilterPopover(활성 개수 배지)·useRecordFilters 훅. **4단계**(`dab9ec4e`, 안 B 핵심): useReviewQueue 단일 계산·[찾아보기|검토] 세그먼트+배지·ReviewMode(진행 막대 3종+전체 처리 승계+종류 필터+체크박스 다중 선택 일괄 3종+행별 처리+인라인 편집기 재사용+빈 상태)·1단계 스트립/ActionDashboard는 검토 모드로 흡수(배너 원형 원복=git checkout 블롭, ActionDashboard 삭제). 저장은 기존 스토어 액션만(신규 경로 0). **5단계**(`dfea690f`): 이모지→Material(task_alt/push_pin/calendar_today)·sp-fade-in 유틸·focus-visible 보강 + **실렌더 검증**(브라우저+Playwright 실데이터: 세그먼트/팝오버/큐 유지/2건 선택 일괄 confirm→반영/저장 토스트, 다크+라이트 스크린샷 5장 → analysis/screenshots). **검증**: 64개 기능 전수 점검 기록(ralph progress.txt)+architect(Opus) 리뷰 VERDICT: APPROVED(표본 14건 일치·배지 byte-동일·onEditSave setter 순서까지 동일·메타 가드 무접촉·tsc 독립 재실행 0). deslop 패스(사이드바 JSX 추출·document 카운터 개명). SearchMode 725→약 460줄·useState 23→4. **게이트 매 단계**: tsc 0/lint 0에러/스코프 vitest 1251/전체 3610/regression 38 (전체 실행 시 인프라 타임아웃 flake 간헐 — pdf 3종·JsonInteractiveLessonRepository, 전부 단독 통과 확인). **함정**: 서브에이전트 wedge 2회(무진행 executor 정지 후 직접 구현)·PS 커밋 메시지 큰따옴표→`git commit -F`·vitest v4 `--reporter=basic` 없음·`git show|Set-Content`=BOM 삽입(→`git checkout <sha> -- <path>`)·소스의 유니코드 이스케이프 리터럴이 Edit 매칭 실패(줄범위 삭제로 우회). **codex QA(gpt-5.6-sol) 후속 발견 — ⚠️ 전 세션 공유**: `bulkMarkNeisReported/DocumentSubmitted`(useStudentRecordsStore:352·372)가 기록별 `manageRecords.update()`(파일 전체 읽기→1건 교체→전체 쓰기)를 `Promise.all` 병렬 실행 → 동시 저장 시 **마지막 쓰기만 디스크에 생존**(메모리는 전부 완료로 보임 → 재시작 시 미반영 부활). **9ce4c1cf 이전부터 존재하는 잠복 버그**(리디자인 무관, 통계 탭 배너 전체 처리도 동일 경유). 건별 토글 빠른 연속 클릭도 같은 계열(set이 await 뒤). 정석 수정=usecase `updateMany`(1회 읽기→전체 교체→1회 쓰기)+쓰기 직렬화. QA 상세: `.omc/artifacts/ask/codex-*qa-git-9ce4c1cf-e4f6aed8*.md` (7건 중 critical 1·절반오탐 1·기존/경미 5).

**codex QA 후속 수정 완료 (`f0732fb0`, 미푸시)**: ① ManageStudentRecords **쓰기 직렬화 체인**(모든 변이 순차화, 실패 격리)+**updateMany**(1회 읽기→전체 교체→1회 쓰기) — bulkMark 2종이 Promise.all 대신 사용(M6 documents 불변식 유지), 경합 회귀 테스트 5종 신설(원자 일괄/병렬 update 직렬화/연속 변이/실패 격리/빈 목록) ② ReviewMode 견고화 — 성공 토스트·선택 해제를 저장 완료 후로+실패 토스트+busy 잠금, **후속조치 전체/선택 일괄 신설**(신규 스토어 액션 bulkMarkFollowUpDone, updateMany 경유), 빈 상태 구분(기록 0건="아직 검토할 기록이 없습니다") ③ useTodayStr 훅(자정+5초 자동 갱신) — 검토 큐 기한·경고 점 자정 고착 해소 ④ 출결 기록 인라인 편집에서 저장 안 되는 입력(카테고리 전환·후속조치) 숨김(UI 정직화, 저장 계약 불변). 게이트: tsc 0/lint 0에러/스코프 346(신규 5 포함)/전체 3695(1 flake 단독 57/57)/regr 38.

**남음**: 실기기 확인(검토 모드 일괄·빈 상태 2종 실렌더 포함)·릴리즈 노트 고지(검토 모드 신설/찾아보기 기본/우측 패널·상단 배너가 검토 모드로 이동)·/docs 사용자 가이드 조회 섹션 갱신. (푸시 전부 완료 — 리디자인 본체 `9ce4c1cf..83f044d0`, QA 후속 `f0732fb0`은 타 세션 푸시에 편승해 원격 반영 확인 2026-07-13)

## 📋 (계획 단계 기록 — 위 구현으로 완료됨) 담임 기록 조회 탭 리디자인 분석·계획 (2026-07-13)

담임 업무→기록→조회 탭 UI/UX 리팩토링(기능 전부 유지 + 확인·수정·검토 워크플로우 개선). 코드 전수 탐색(Explore)으로 **기능 64개 인벤토리** 확보 후 designer 에이전트 협업으로 리디자인 제안서 작성. **진단**: 콘텐츠 도달 전 5단 수직 비용 · 나이스/서류 정보가 화면 5곳 중복(탐색용 3곳이 진짜 중복) · 필터 상시 15개 과밀 · 학생 선택 시 우측 진행 현황 패널 소실(SearchMode.tsx:709) · 학생 선택 컨트롤 이중화(드롭다운+사이드바) · 저장 토스트 비대칭(출결만 있음) · prop drilling 실측 30개. **제안 2안**: A=보수(접힘 스트립+필터 팝오버+패널 유지), B=구조(찾아보기/검토 모드 분리, 나이스·서류·후속조치 통합 처리 큐+체크박스 부분 일괄) — designer 추천 B, 단 1~3단계는 양안 공유 저위험 정리라 3단계 후 재판단 가능. **제약**: ADR-021/022 출결 단일 기록자(새 입력 경로 금지·메타 가드 수정 금지), sp-\* 알파 수식 금지, AttendanceStatusBanners는 통계 탭 공유. 산출물: `docs/03-analysis/records-search-redesign/uiux-redesign-proposal.md`(진단+2안+64개 매핑 표+시각 가이드) · `docs/01-plan/features/records-search-redesign.plan.md`(5단계 실행 계획+보존 계약+검증). **다음**: 오너 방향 결정(A/B/3단계 후 재판단) → 1단계 착수.

## ✅ 출결 그리드 v2 — 팔레트 입력 모델 재설계, ralplan 승인→ralph 구현 완료 (2026-07-11, main `8d5d1b70`~`49679d3a` 6커밋, ADR-022, **미푸시**)

v1(ADR-021) 실기기 피드백 8건 + 나이스 출결 매뉴얼 분석으로 담임 출결 입력을 팔레트 모델로 재설계. 계획서 `docs/01-plan/features/attendance-grid-v2.plan.md`(§3.10 실행 안전 계약) 승인본을 P7.1~P7.6 순차 구현. **P7.1**(`8d5d1b70`): 기록 탭 서브탭 [출결|누가기록|통계|조회] 기본=출결, 오늘 출결 그리드를 InputMode→전용 `AttendanceMode`로 분리, 공용 `AttendanceGridView` table-fixed+colgroup(교시 열 균등)+헤더/식별 열 sticky+출석 빈칸 opt-in, DateNavigator 과거 배지, 메타 가드 리타깃. **P7.2a**(`8cabb86d`): 팔레트(종류5×사유4+비고), 칸 클릭=computeAutoPeriods 기준 교시 전-행 재작성(§3.10-5), 지우개, 공용 뷰 opt-in 구분/사유 열, 순환·팝오버·선택모드·일괄바 제거. **P7.2b**(`879c98fb`, 최대 위험): 자동 저장(dirty-gate 주+자기저장 canonical 서명 보조, 800ms 디바운스, 플러시 3종=날짜/언마운트/blur, 조용한 저장됨✓·성공토스트 제거), undo/redo(행 스냅샷 50·Ctrl+Z·오늘 비우기), `parseAttendanceQuickText` 순수함수+미리보기. **fake timer 런타임 회귀 7종(§3.10-2 4종 포함)+파서 계약 14종.** StrictMode `mountedRef` 미복원으로 저장칩 고착 버그를 실렌더로 발견·수정(타입/테스트로는 안 잡힘). **P7.3**(`ca7182ea`): 여러 날 출결을 `MultiDayAttendancePanel`(팔레트+기간/여러날+주말제외+학생다중선택)로 출결 탭 이관, 명령 팔레트 라우팅 변경, InputMode 출결 완전 제거(2024→1357줄, 날짜모드는 비출결용 존치), 메타 가드 강화(출결 입력 경로 부재+AttendanceMode 유일). **P7.4**(`8f0aaf22`): 좌석 배치 뷰(grid 우선) — 신규 읽기 전용 `SeatAttendanceView`(편집 스토어/액션 import 0), id↔번호 매핑(AttendanceMode가 useSeatingStore 읽기+명렬로 주입, 그리드 셸 스토어 미import 유지), 팔레트 기준 교시 선택기, [명렬|좌석] 토글 localStorage 영속. **P7.5**(`49679d3a`): 요약 칩 클릭→행/좌석 하이라이트(opt-in highlightStatus), 사유 툴팁. **P7.6**: 실렌더 검증 게이트(Playwright) — 열 정렬·팔레트·텍스트·여러날·좌석·누가기록 정화·다크 모드 확인. **§3.10-8 수업관리 무회귀**: 공유 뷰 소비처 AttendanceTab이 Phase5 UX 정리로 desktop UI에서 언마운트됨(ClassRecordTab가 대체) → 공유 AttendanceGridView의 유일 라이브 소비처는 담임 그리드(검증 완료), 수업관리 AttendanceMatrixCore는 컴파일·정적 테스트 통과하나 라이브 회귀 표면 없음.

**게이트**: 매 단계 tsc 0 / lint 0에러(경고 132 기존) / vitest 3604 / regression 38. 신규 테스트: 파서 14 + 자동저장 회귀 7 + 메타가드 7(리타깃·좌석 격리). **남음**: v1(9커밋)+v2(6커밋) **전체 미푸시** — 사용자 푸시 확인 필요 · 실기기(팔레트·텍스트·좌석·다크) · 다음 릴리즈 노트 고지 3종(카드 여러날 출결→출결 탭 이동·기록 탭 첫 화면 출결로 변경·레거시 이질 상태 행 첫 편집 시 평탄화) · /docs 사용자 가이드 출결 섹션 · 모바일 행 모델 이관·모둠/자유 좌석 렌더(후속). 자동저장 테스트는 병렬 부하에서 간헐 flaky(단독 7/7 통과, 마이크로태스크 추가 플러시로 완화).

## ✅ 출결 개선 6종 — 사용자 건의 반영, ralplan 합의→ralph 구현 완료 (2026-07-10, main `529faffb`~`4df843e9` 9커밋, ADR-021)

출결 탭 헤비유저 담임교사 건의(smile837@naver.com, ssampin_escalations `c4484974`) 6건을 ralplan 3라운드 합의(Planner→Architect→Critic, 최종 APPROVE)로 계획 확정 후 ralph로 전 구간 구현. **규정 검증 선행**(사용자 지시): 2026 기재요령(훈령 제555호 별표 8 §3) 원문 확인 — 초안의 "지각 자동 결시 토글"이 규정 바(같은 날 지각·조퇴·결과=한 가지만)와 충돌해 **폐기**, 지각 자동 채움은 건의 원문대로 "조회~등교 교시를 지각 상태로"(`docs/03-analysis/attendance-regulation-2026.analysis.md`). **구현(전략 1 순서, 단계별 게이트+커밋)**: ① P1a `computeAutoPeriods` 도메인 순수 함수(결석=전일·지각=조회~등교·조퇴=하교~종례·결과=해당 교시, 비-present 빈 Set 금지) ② P2 모바일 좌석/명단 보기 반별 localStorage 영속(+좌석 없으면 명단 폴백) ③ P3 모바일 명단 스와이프에 조퇴 추가(담임·수업반 대칭) ④ P4.1 headless 그리드 코어 추출(`adapters/components/attendance/shared/`, 수업관리 동작 보존 52/52) ⑤ P4.2 담임 오늘 출결 그리드 장착 — **단일 기록자**(단일 날짜 카드 출결 제거, 여러 날은 카드 유지)+렌더 게이트(번호 충돌 시 그리드 차단)+미러 재매핑+스토어 승리 재시드+메타 회귀 가드 6종 ⑥ P4.3 이름 클릭 교시 자동 채움(전일 결석 2클릭) ⑦ P4.4 그리드 40px 격자 리플로우·글씨 기본 상향 ⑧ P5 다중 학생 일괄 적용 바(선택 모드 배타)+기간 주말 자동 스킵 ⑨ P6 `summarizeNeisAttendance`(인정 사전필터→일 단위 대표 접기 합성 고정)+ProgressMode 생기부 기준 집계 표+사유 드릴다운+A4 인쇄 2종. **게이트**: 매 단계 tsc 0/lint 0에러/regression 38/38, 전체 vitest 3580 passed(실패 2건은 단독 통과 기존 병렬 flaky — FillFormFields·RenderTemplate). **남음**: 실기기 실렌더 확인(그리드 입력·자동채움·일괄·인쇄 A4·다크/라이트, 모바일 영속·조퇴 스와이프) → 다음 릴리즈(v2.2.13 후보, 릴리즈 노트에 카드 출결→그리드 이동 고지 필수) + /docs 사용자 가이드 갱신. 건의 7(시간표 전문가 연동)·8(설문지 열기 버그)은 별도 트랙.

## ✅ 챗봇 플랫폼(OS) 문맥 이탈 오답 수정 — Mac 사용자에게 Windows 해결법 주던 버그 (2026-07-08, main 커밋·edge function 배포)

macOS 베타 표기 작업 중 발견한 별개 챗봇 버그를 조사·수정. **증상**: Mac 사용자가 "알려준대로 해도 실행이 안되" 같은 짧은 후속 질문을 하면 봇이 Windows 해결법(Setup.exe·백신 V3/알약·스마트 앱 컨트롤)을 답함. **재현**: 실제 대화(Mac history + 애매한 후속)를 라이브 엔드포인트에 isTest로 replay → **6회 중 4회 Windows 오답**(sources 전부 Windows 문서). **근본 원인(투 트랙)**: ① 짧은 후속 질문은 자체 플랫폼 신호가 없는데 `reformulateQuery`가 대명사 패턴(그거/이거)만 히스토리를 붙여 → 임베딩·HyDE 검색이 KB 다수(Windows) 문서로 쏠림(`classifyQuery`는 null 반환 — 오분류 아님을 확인). ② 최종 답변 `temperature: 1.0` → 같은 질문에 Mac/Windows를 오가는 비결정성. **수정(edge function `ssampin-chat`)**: (a) `reformulateQuery` 확장 — 30자 이하 짧은 후속도 직전 사용자 질문 부착; (b) `detectPlatform()` 신규 — 메시지→히스토리→appContext 순 OS 탐지 후 검색 쿼리에 "(macOS 환경)" 앵커링 + 시스템 지시에 [확인된 플랫폼] 못 박기; (c) SYSTEM_PROMPT에 플랫폼 일관성 규칙(반대 OS 안내 금지, OS 되묻기는 설치·실행·보안 문제로 한정) + macOS 트러블슈팅 절차(그래도 열기·칩 불일치·xattr, Control+클릭 폐기) 추가; (d) `temperature 1.0 → 0.3`. **클라이언트**: 앱(useHelpChat)은 appContext에 navigator.platform 병기, 랜딩(useChatbot)은 navigator 기반 OS를 appContext로 전달(첫 메시지부터 OS 인지). **검증(전/후)**: 문제 시나리오 Windows 오답 4/6 → **0/8**(Mac 정상 7·에스컬 1), sources가 Mac 문서로 전환, Windows 회귀 없음(Win 사용자 Win 답 유지). 전체 27케이스 스위트 실패 6→4(남은 4건은 #6 테스트 특성·#8 지연·#17/#18 answer-first 기존 성향으로 회귀 아님). 게이트: 앱 tsc 0·lint 0 / 랜딩 tsc 0·lint 0 / deno check 8(기존 rpc 타입노이즈, 신규 0). **후속(비차단)**: 오늘 추가한 Mac KB 항목(칩 불일치 등)은 다음 릴리즈 재임베딩 때 반영(이 수정은 재임베딩 없이도 동작 — 검색이 이미 Mac 문서 회수).

## ✅ macOS 베타 지원 표기 + 실행·업데이트 안내 전면 교정 (2026-07-08, ADR-020)

macOS Tahoe 사용자 실행 실패·이탈 사례 조사(근거: v2.2.11 GHA 빌드 로그 — 서명 skip·ad-hoc 폴백·공증 skip 확인) 후 사용자 결정: **Apple 개발자 프로그램 미가입 확정 → macOS "베타 지원" 솔직 표기**. **① 랜딩**: Hero 배지 macOS(베타), DownloadButton 캡션 베타 표기+칩 확인 안내(🍎 이 Mac에 관하여)+FAQ 재작성(macOS 15+ [그래도 열기] 절차, 칩 불일치 오류 케이스), InstallGuide mac 트러블슈팅 A(보안 경고)/B(칩 불일치 "이 버전의 macOS에서 작동하는지…")/C(xattr) 3케이스+Control+클릭 폐기 명시, FAQ.tsx Mac·업데이트 답변 교체. **② /docs**: install-macos(베타 공지+칩 고르기+새 절차), macos-security(칩 불일치·xattr·수동 업데이트 섹션, lastUpdated 2026-07-08). **③ 챗봇 KB**(ingest-chatbot-qa.mjs): Tahoe 항목에 베타·게이트키퍼 절차 추가, v1.10.5 "자동 업데이트 곧 정상화" 거짓 약속 제거→수동 설치 안내로 교체, 신규 2건(칩 불일치 오류 전용 Q&A·macOS 15+ 그래도 열기 절차) — **다음 릴리즈 재임베딩 때 반영됨**. 오프라인 FAQ 4항목 갱신+1신규. **④ 앱**: mac에서 업데이트 모달이 "다운로드 중 0%"에 영구히 갇히던 버그 수정 — main.ts `update:available`에 `manualOnly`+`update:download`가 칩(process.arch+runningUnderARM64Translation) 맞는 DMG 직접 브라우저 다운로드, UpdateNotification·AppInfoSection에 `manual` 상태(수동 설치 3단계 안내). universal 빌드는 실기기 검증 불가로 보류(ADR-020). **게이트**: tsc 0 / lint 0에러(경고 132 기존) / vitest 3492 passed·1 flaky(FillFormFields 단독 10/10) / regression 38/38 / landing docs:check(문서 41)+build 통과. 남음: 실기기(Mac) 확인 불가 상태 지속 — 다음 mac 사용자 문의 시 새 안내로 검증, 다음 릴리즈에 앱 변경분 포함+KB 재임베딩.

## 🚀 v2.2.11 릴리즈 진행 (2026-07-07)

v2.2.10(`3fc6f71d`) 이후 8개 커밋 묶음을 v2.2.11로 릴리즈. **헤드라인**: ① 진도 캘린더(시간표 진도 오버레이 + 수업 관리 반별 진도 달력, `39568392`/`c3cf7961`) ② 시간표 불러오기(압핀 sgpap.com 학급·교사 `cbb7bfd8` + 나이스 교사 시간표 재조합 `b20303af` + 통합 '불러오기' 드롭다운 + 압핀 자동 변경감지) ③ AI 브릿지 수업 진도 도메인(외부 AI 진도 CRUD, `ed92a595`) ④ 백그라운드 리로드가 노트·메모 편집을 지우던 문제 수정 + 조용한 갱신 전환(`af5b8e1b`/`4e52cd72`) ⑤ 이번 달 일정 목록(지난 일정 접기 + 주 퀵점프, `59f93f70`). **릴리즈 작업**: 버전 6곳(package.json/lock·landing config/layout·Sidebar·mobile version.ts) + release-notes.json(v2.2.11 highlights 4·changes 6) + KB Q&A 4건 추가·재임베딩. **게이트**: tsc 0 / lint 0에러(경고 132 기존부채) / vitest 3493 passed·10 skipped / regression 38/38 / 랜딩 docs:check+build 0. 스플래시 마스코트 렌더링(index.html) 포함. **제외**: 에스컬레이션 스크린샷(landing/supabase, 별도 트랙 — 릴리즈 후 마무리 예정). 다음: 커밋·푸시 → Windows/macOS GHA → GitHub 릴리즈 8자산 + URL 302 검증.

## ✅ AI 브릿지 수업 진도(progress) 도메인 — 조회+쓰기(CRUD) 추가 (2026-07-07, 본체+브릿지 양 레포, 커밋 대기)

진도 캘린더 출시에 맞춰 외부 AI(Claude/Codex/Antigravity)가 수업 진도 기록을 조회·추가·수정·삭제할 수 있게 확장. **본체**: 쓰기 계약 SSOT(`scripts/contract/aiBridgeWriteContract.def.mjs`)에 `progress` 도메인+필드(classId/date/period/unit/lesson/status/note)+상한(200/500/500)+status enum(planned|completed|skipped) 추가 → `gen:contract` 3산출 재생성(check:contract-sync PASS). electron 검증(`checkProgressFields`+op별 허용필드 — **update 는 classId 변경 불가**), 렌더러 적용(`applyProgress`: create=classExists 404·update/delete=exists 404·complete 거부), `useAiBridgeLiveSync`에 useTeachingClassStore 배선+선로딩, `addProgressEntry`에 optional status 인자(기본 planned, 기존 호출 불변). **브릿지**(ssampin-ai-bridge): 벤더 계약 JSON 동기화, `curriculumProgress` 엔티티 미러(관대 파서), `readCurriculumProgress`, `prog:` 신원+`prg_` 토큰, `appendProgressDirect`(락/CAS/백업/멱등 상속, 앱 스키마 전 필드 채움), MCP 도구 4종(`get_progress` 읽기 — 단원·차시·메모는 content 게이트+탈식별 / `ssampin_create_progress` 하이브리드 / `ssampin_update_progress`·`ssampin_delete_progress` 앱 실행 중에만). 도구 49→53개. **번들 재생성**: `electron/ai-bridge/index.mjs` 완전 자기완결 재번들 + node_modules 없는 격리 폴더 스모크(`connected (stdio)`) 통과. **게이트**: 본체 tsc 0/lint 0에러/vitest 3493/regression 38 · 브릿지 typecheck 0/테스트 576(core 302·mcp 227·cli 47)/build 클린. 남음: 커밋(리뷰 후)·실기기(Claude 연결 후 진도 왕복).

## ✅ 모바일 출결 통계·반 전체 기록에 달력 '직접 설정' 기간 필터 (2026-07-06, main `aa82ed7c`, m.ssampin.com 배포)

프리셋(전체/이번 학기/이번 달/이번 주)만으론 임의 기간을 못 봐서 달력으로 시작~종료를 직접 지정하는 옵션 추가(사용자 요청). **공용 `DateRangePickerSheet`(신규)**: 이미 모바일이 쓰던 `@adapters/common/MultiDatePicker`의 range 모드를 바텀시트로 감싼 재사용 컴포넌트. **함정: range `maxCount` 기본 30일이 기간 길이를 제한**(MultiDatePicker.tsx:121-124)→`maxCount={366}`로 학기 단위 허용. useBottomSheet+메타테스트 등록. **출결 통계(담임+교과)**: 공용 `AttendanceStatsTable`에 '직접 설정' 칩(선택 시 "M/D~M/D" 라벨)→시트→custom 범위 집계. `AttendancePeriodFilter`에 'custom' 추가, 소비자는 filter==='custom'이면 customRange로 dateRange 계산. **반 전체 기록**: '기간' 필터 칩→같은 시트, 설정 시 1차 필터로 적용해 요약·주의 학생·카테고리 카운트·타임라인 전부 그 기간 기준. 신규 오버레이 시트 1(등록), 신규 코드 sp-\* 알파수식 0. **게이트 4/4**: tsc0 / lint0에러 / vitest 3432 passed / reg 38/38. 커밋=7파일 명시 add·배포(라이브 번들 `mobile-DGrEOD2x.js` 200·1850433B, 모듈 1366→1367). **⚠️ 푸시 함정: GitHub 계정이 이메일 노출 차단(email privacy)을 켜서 pblsketch@gmail.com 커밋 푸시 거부(GH007)→저장소 로컬 user.email을 `239486537+pblsketch@users.noreply.github.com`으로 변경+amend 후 푸시 성공. 이후 커밋도 이 저장소는 noreply로 나감.** 남음: 실기기 확인.

## ✅ 모바일 학급 단위 조회 2종 — 담임 반 출결 통계 + 반 전체 기록 모아보기 (2026-07-06, main `5fcf5141`, m.ssampin.com 배포)

위 3종 배포 후 사용자 피드백: "학생별로는 보이는데 학급별로 한 번에 확인이 안 된다"(AskUserQuestion으로 "둘 다" 확정). /team 후속(designer §6 → builder 2명 병렬 → 게이트). **① 담임 '우리 반' 출결 통계**: 지금까지 교과 학급(수업 관리)에만 있던 학급 전체 출결 통계표를 담임 반에도. 홈 "담임 출결하기" → 상단 `[출결 체크|출결 통계]` 세그먼트(기본 체크, 회귀 0). 기간 칩+연인원 요약+번호·이름 sticky 표를 교과와 공용화(신규 `components/common/AttendanceStatsTable`로 추출, `ClassAttendanceStatsView`는 소비형으로 리팩터 — 렌더 동일). 담임은 학생 소스가 `useMobileStudentStore`라 `student.id`를 row key, `sa.number===studentNumber` 매칭. **② 반 전체 기록 모아보기**: 담임=학생 탭 헤더 아이콘(`history_edu`)→신규 `HomeroomRecordsOverviewPage`(풀스크린, 분류 요약 digest+주의 학생 줄 `getWarningStudents`+카테고리·학생 필터+월별 타임라인, 행 탭=학생 필터). 교과=특기사항 탭에 `[학생별|전체]` 세그먼트 추가(전체=학급 전원 관찰 월별 나열, 읽기 전용, 행 탭=학생별 편집 점프). `getCategorySummary/getWarningStudents/sortByDateDesc` 재사용. **배치 함정**: 담임 출결 체크 위 세그먼트 바가 `AttendanceCheckPage` 비embedded 헤더의 `safe-area-inset-top`과 이중 여백 → `headerTopInset` optional prop(default true, 회귀 0) 추가로 해소(세그먼트 바가 노치 여백 흡수). 신규 오버레이 시트 0(풀스크린/세그먼트만), 신규 코드 sp-\* 알파수식 0. **게이트 4/4**: tsc0 / lint0에러(경고 132 기존) / vitest 3431 passed / reg 38/38. 커밋=10파일 명시 add(타세션 제외)·푸시·m.ssampin.com 배포(`Aliased` 확인, 라이브 번들 `mobile-C3gQYet1.js` 200·1846868B, 모듈 1362→1366). 남음: 실기기 확인(4개 뷰 실데이터 필요), 다음 데스크톱 릴리즈 포함.

## ✅ 모바일 학생 기록·출결 조회/통계 화면 3종 — /team 구현·커밋·m.ssampin.com 배포 완료 (2026-07-06, main `3da32de6`)

모바일 격차 분석 후속 — "기록만 되고 조회·통계가 없다" 해소. /team 파이프라인(scout 정찰 → designer 설계서 → builder 2명 병렬 → 리드 검수 → 게이트)으로 진행. **S1 학생 기록 전체 모아보기**: `RecordsSubTab`에 "전체 기록 보기 (N건)" 진입 → 신규 `StudentRecordsFullSheet`(85vh 풀시트, 카테고리 칩 필터+카운트, 월별 그룹 타임라인, 1줄 초과 시 행 펼치기). **S2 담임 학생 출결 요약·내역**: `AttendanceSubTab` 하단 담임 전용 `AttendanceHistorySummary`(domain `getAttendanceStats` 재사용 — 결석·지각·조퇴·결과·칭찬) → 신규 `StudentAttendanceHistorySheet`(상태 칩 필터, 재탭 해제, 사유 표기). 교과 학생은 bridge 데이터가 원천 부재라 완전 숨김(설계 결정). **S3 수업 학급 출결 통계**: `ClassAttendanceTab`에 SegmentedControl(출결 체크/출결 통계, 기본 체크 — 기존 AttendanceCheckPage 호출 바이트 동일 보존) → 신규 `ClassAttendanceStatsView`(기간 칩 전체/이번 학기/이번 달/이번 주 — PC `ClassRecordStatsView` getFilterRange 로컬 이식, 학급 연인원 요약, 번호·이름 sticky + 상태 5칸 가로 스크롤 표, period 무시 합산 PC 동일 알고리즘). 설계서 `docs/02-design/features/mobile-records-stats.design.md`. 신규 4 + 수정 4(메타테스트 SHEETS_TO_REGISTER 2건 등록 포함). **함정 처리**: 디자이너 산출물의 `bg-sp-card/95`(sp 알파 수식 무효) 리드 검수에서 적발→solid 정정, 신규 파일 sp-\* 알파 grep 0건, 신규 시트 2종 useBottomSheet+메타테스트 등록. **게이트 4/4**: tsc 0 / lint 0에러(경고 132 기존부채) / vitest 276파일 3431 passed·0 fail / regression 38/38. **실화면 검증 미수행**: 4개 뷰 모두 명단·수업반 데이터 필요 — 브라우저 모드는 명단 미영속 → 실기기(동기화 계정) 확인 남음. **커밋·배포 완료**: main `3da32de6`(9파일 명시 add — 타세션 landing/supabase/Settings/PROGRESS 제외, lint-staged 자동 정리 통과)·푸시 → m.ssampin.com 배포(링크 ssampin→ssampin-mobile 전환→`vercel --prod`→원복, `Aliased: m.ssampin.com` 확인, 라이브 번들 `mobile-cvowvvIQ.js` 200·1834186B=빌드 출력 일치 검증, sw.js 200). 남음: 실기기 확인, 다음 데스크톱 릴리즈에 포함.

## ✅ 설정창 카테고리 재정리 — 17개 평면 탭 → 5개 그룹 (2026-07-06, main 미커밋)

사용자 요청 "설정창 카테고리를 사용자 입장에서 재정리, 기존 설정 기능 제거 금지". 17개 탭 전수 조사 후 **탭 id·라벨·내용물 전부 보존**(딥링크 `settings#widget`·/docs 가이드 "Google 연동 탭" 참조 안전), 사이드바만 그룹 헤더 5개로 재구성: ① 기본 정보(학교 정보·교시 시간) ② 화면 구성(디스플레이·위젯·사이드바) ③ 기능별 설정(일정·할 일·좌석·날씨·도구·단축키) ④ 연동·백업(Google 연동·AI 연결·백업/복원) ⑤ 시스템(보안·시스템·앱 정보). `SettingsSidebar.tsx`에 `TAB_GROUPS` 신설(기존 `TABS`는 flatMap 파생으로 유지), 모바일 `SettingsTabBar`는 그룹 순서+그룹 사이 세로 구분선. **부수 수정 2건**: ① PeriodTab 안내문이 존재하지 않는 "일정·연동" 탭을 가리키던 것 → "일정"으로 정정 ② 좁은 창(<768px)에서 상단 탭바 고유 폭이 flex 컨테이너를 밀어내 **설정 본문 전체가 화면 밖으로 사라지던 기존 버그** → SettingsLayout 래퍼에 `min-w-0` 추가로 해소(실브라우저 700px 재현→수정 확인). 게이트: tsc 0 / lint 0에러(경고 132 기존부채) / vitest 3429 passed / regression 38/38 + 실브라우저 스크린샷(와이드 상·하단 그룹, 좁은 폭 탭바·본문). 남음: 실기기 확인, 커밋(사용자 요청 시 — Settings 4파일 명시 path).

## ✅ 학교 알리미 편제표 3건 수정 — 검색 학교 미반영 + 전체 문서 쏟아짐 + PDF 줄바꿈 뭉개짐 (2026-07-06, main `b1f28659` 커밋·푸시)

**(추가) 원인 ③ — PDF 셀 줄바꿈 소실**: 경기북과학고처럼 PDF로 올린 학교는 kordoc 변환 시 같은 행에서 과목명 셀은 `<br>` 보존, 학점("44444455")·과목유형("공통공통") 셀은 값이 붙어서 나옴(실문서 다운로드로 재현·확인 — 학교알리미 b14 스크레이프를 스크래치 스크립트로 재현). **수정**: `curriculumTableExtract.ts`에 행 문맥 복원 `repairMashedCells` — 셀당 항목 수 ctx = max(행의 br 최대 줄 수, **셀 자신의 rowspan**). rowspan이 결정적: 과목명 줄까지 붙은 행(18과목인데 과목명 17줄)에서도 rowspan=18이 진짜 과목 수. 숫자 셀은 **1~8 숫자만**일 때(0/9 포함 = 진짜 합계 109·192·연도 2026 → 절대 미분리) 자릿수==ctx(정렬 복원) 또는 자릿수 4+·≤ctx(학기열 목록 복원)만 분리. 과목유형은 어휘 토큰(공통/일반/진로/융합/전문/교양) 완전분해+토큰≥2일 때만. 중첩표 행 제외. PDF 안내 문구 추가. **실문서 검증: 문제 셀 6종 전부 해소**(44444455·18자리 과학·공통공통 포함). **(추가) 교과(군) 뭉침 복원**: "국어수학영어한국사"·"사회과학"·"체육예술"은 교과군 어휘(한국사/국어/수학/영어/사회/역사/도덕/과학/체육/예술/음악/미술/기술/가정/정보/한문/교양/제2외국어)로 완전분해될 때만 분리(`tokenizeByVocab` 전부-아니면-무 — "국어문학독서수학"처럼 과목명 뭉침은 어휘 밖이라 미접촉). 실문서 교과군 3종 해소. **미복원 잔여(정보 소실)**: 3자리 학기열("363" — 교과 총 이수학점 174와 구별 불가)·다자리 합계 뭉침("1430166" — 경계 미상)·글자 뒤섞임("공통일선"·"정치와 법통합과학"). 테스트 +11(총 16). 게이트: tsc0/lint0/reg38/제 영역 테스트 전부 통과 — 전체 vitest의 4fail은 타세션 진행 중 작업(sync mergeAttendance 툼스톤, 미커밋)으로 무관.

사용자 버그 제보: "다른 학교 검색"으로 어떤 학교를 골라도 교육과정 탭 편제표가 항상 우리 학교(온양여고) 것만 나옴. **원인 ①**: `CurriculumEvaluationSection`이 공유 컨텍스트(`useSelectedSchool`) 대신 설정(`useSettingsStore.schoolInfo`)을 직접 읽음 — 검색 모달이 `shlIdfCd`(학교알리미 고유번호)를 검색 결과에서 받고도 버려서(`identifyFromAddressKind`에 미전달) 컨텍스트에 고유번호가 없었기 때문. **수정**: `SchoolDisclosureIdentity`에 `shlIdfCd?` 추가 → 검색 모달·`useSchoolIdentity`(설정 경로) 양쪽에서 실어줌 → 편제표 섹션이 컨텍스트를 따라가도록 변경. **원인 ②**(수정 후 발견): 학교마다 문서 양식이 달라 "운영계획 전체 문서"(효자고·경기북과학고형)를 올린 학교는 표지 이미지 자리표시(`![image]`)·목차 잔해가 통째로 렌더됨. **수정**: 신규 순수 도메인 서비스 `domain/services/curriculumTableExtract.ts` — 최상위 `<table>` 깊이 스캔(중첩 택N 박스 보존) 후 편제표 후보(교과/과목+단위/학점)만 추출, 표 앞 "N학년도 입학생" 캡션 보존, 후보 없으면 이미지 잡음만 제거+안내 문구 폴백. 변경 파일 6: schoolIdentify.ts(+test)·SchoolSearchModal·useSchoolDisclosure·CurriculumEvaluationSection·curriculumTableExtract.ts(+test 신규). **게이트**: tsc 0 / lint 0에러(경고 132 기존부채, 변경파일 0) / vitest 3401 passed·**1 failed는 타세션 미커밋 작업**(`entitySampleContract.meta` — 타세션이 `Attendance.ts`에 `updatedAt` 추가 중, `ENTITY_FIELD_CONTRACT` 미분류; 본 작업과 무관, 그 세션 영역이라 미수정) / regression 38/38. 남음: 실기기(여러 학교 검색→편제표 전환·추출 품질) 확인, 커밋(사용자 요청 시 — 명시 path로).

## ✅ v2.2.9 릴리즈 완료 (2026-07-06, tag `v2.2.9`, main `8ff83430`)

사용자가 "Claude 연결" 시도 중 구글 계정 연동 실패 캡처(액세스 차단됨: invalid_client)를 보여줘 진단 후 정식 핫픽스로 진행. **① 구글 OAuth invalid_client**: 콘솔에서 "쌤핀"(데스크톱, `...eji9...`) 클라이언트가 실제로는 살아있었으나, GitHub Secrets(`VITE_GOOGLE_CLIENT_ID/SECRET`, 2026-05-26 이후 미변경)에 등록된 값이 어긋나 있었음 — 사용자가 콘솔에서 값 재확인 + 비밀 재발급 후 `gh secret set`으로 갱신. **② AI 브릿지(Claude/Codex/Antigravity 연결 실패)**: `electron/ai-bridge/index.mjs`(app.asar 바깥 `resources/ai-bridge/`에 배치)가 `@modelcontextprotocol/sdk`·`zod`를 esbuild `external`로 빼뒀던 것이 원인 — 패키징된 앱에선 그 경로 위로 올라가도 app.asar 안 진짜 `node_modules`를 만날 수 없어 즉시 `Cannot find module`로 죽음(개발 환경만 우연히 동작, `aiBridge.ts`의 `buildEntry()`를 세 클라이언트가 공유하므로 동일 증상). 두 패키지를 인라인 완전 번들(265KB→~1MB)로 재생성 후 node_modules 없는 격리 폴더에서 단독 실행 스모크 테스트로 확인(`[ssampin-mcp] connected (stdio)`). 버전 6곳(package.json/lock·landing 2·Sidebar·mobile version.ts) + release-notes.json 2건. **게이트 전부 PASS**: tsc 0 / lint 0에러(경고 132 기존부채) / vitest 3373 passed / 랜딩 docs:check+build 0. `regression-check` npm 스크립트는 현재 package.json에 없음(CLAUDE.md 기재가 오래된 듯 — 회귀 테스트 파일들은 이미 vitest 본 실행에 포함되어 함께 통과 확인). Windows/macOS GHA 빌드 + GitHub 릴리즈 8자산 + URL 6/6 302 확인. 남음: 실기기(구글 재연동, Claude/Codex/Antigravity 재연결) 확인.

## ✅ 모바일 시각적 전문화 패스 1차 — /ralph 구현·검증 완료 (2026-07-03, main 미커밋, Opus)

계획서 `docs/01-plan/features/mobile-design-uplift.plan.md`(ui-ux-pro-max 분석) 승인 후 /ralph로 "보이는 인상" 슬라이스 6종 구현. designer 협업 + 실화면 grounding. **US-001** 교실도구 11 이모지→Material Symbols 통일 중립 칩(bg-black/5 dark:bg-white/10 + text-sp-accent, 관리도구 raw색도 통일, 섹션헤더 이모지 제거, 뒤로가기 44px). **US-002** 우선순위 🔴🟡🟢→flag 아이콘+sp-error/warning/success, calcDDay raw→토큰. **US-003** EmptyState 확장(mascot·hint·optional action, 하위호환) + 4블록(할일·좌석=쌤핀이 마스코트, 일정·보관함=아이콘). **US-004** 온보딩 히어로: 환영/로그인=마스코트, 정보/동기화/학교/교시=Material Symbol 메달리온, ✓→check_circle, 💡→lightbulb. **US-005** 설치배너 bottom-20→calc(tab-bar+5.5rem)(FAB 위로), 📲→add_to_home_screen. **US-006** 탭 라벨 9px→11px, 홈 날짜 text-xl→2xl(단일 primary), 😢/💡→Material Symbol. **핵심결정**: 11색 무지개 대신 통일 중립 칩(.impeccable 절제 방향), sp 알파수식 무효라 black/white 알파만, 신규 raw 팔레트 0. **게이트 전부 PASS**: tsc0 / eslint0(14파일) / vitest 3370 passed / regression 38/38 / build:mobile 0 / 실화면 8장(light+dark, `after/`). 14파일 +194/-137. **후속 분리**: 149건 전면 토큰 스윕·M2 헤더/모달 통합·M4 모션 시스템·라우터. 남음: architect 리뷰 진행중→deslop→커밋(사용자 요청 시, index.html 타세션 분리). 보고서 `docs/04-report/features/mobile-design-uplift-pass1.report.md`.

## ✅ 모바일 현장 도구 3종 (Track A) — /ralph 구현 완료·미커밋 (2026-07-03, Opus 4.8)

격차 분석 후속. ralplan 합의(Architect+Critic APPROVED) 통과 후 /ralph로 Track A 구현 완료. 계획서 `docs/01-plan/features/mobile-field-tools.plan.md`. **M1 배점 계산기**: `ToolScoreAllocator`를 App.tsx MORE_LAZY_TOOLS 레지스트리+CLASSROOM_TOOLS 타일로 무수정 드롭인. **M2 모둠 편성기**: 신규 `pages/ToolGroupingPage.tsx`(공유 ToolGrouping.tsx 미수정, 순수 `@domain/rules/groupingRules`만 재사용, 명단=useMobileStudentStore/useMobileTeachingClassStore 매핑은 데스크톱과 동일, 1차 범위=코어(인원/방법/결과/복사)만, 고급옵션·내보내기 제외). **M3 루브릭 채점**: 신규 `stores/useMobileRubricStore.ts`(PC useRubricStore:144-199 시그니처+가드 본문 미러 — classId 파라미터·결시생 채점 차단·undefined 가드, 쓰기마다 triggerSaveSync) + `di/container.ts` rubricRepository 등록 + reloadAllStores 두 곳 등재 + 신규 `pages/ToolRubricPage.tsx`(수업반→평가지→학생 3단계 드릴다운 + GradingSheet 바텀시트, studentId=studentKey 정합) + ToolsOverviewPage 관리도구 버튼. rubrics는 SYNC_REGISTRY 기등록이라 syncRegistry 미수정. **함정 대응**: lint exhaustive-deps 1건(rubrics를 allRubrics 구독 useMemo로) 수정. 바텀시트 메타테스트에 ToolRubricPage 등록. UI 2종은 designer(frontend-design) 협업 작성·검수. **게이트 4/4**: tsc 0 / vitest 270파일 3370 passed(신규 스토어 테스트 6 포함) / regression 38/38 / lint 변경파일 0. **Architect APPROVED + deslop 클린(수정 없음)**. **Track B**(후속·미착수): 과제 수동 체크(제품 결정 게이트)·자리 뽑기(seating 쓰기 경로 선행). **남음**: 실기기 확인(동기화 계정으로 명단·평가지 표시), 커밋(사용자 요청 시 — 다른 세션 미커밋 파일과 명시 path 분리).

## ✅ 모바일 홈/일정 UX 3종 (2026-07-03, main 미커밋)

사용자 요청 3건. **① 앱 아이콘 → 쌤핀이 마스코트**: PWA 홈화면/즐겨찾기 아이콘(`public/icons/icon-192·512·apple-touch-icon.png`)을 단순 압정에서 얼굴 있는 쌤핀이(`public/floating-pin.png`)로 교체 — 크림 배경(#FAF7F0, 사용자 선택) 위 중앙 합성, 마스킹 안전영역 고려(192/512는 0.70, apple-touch 0.80). sharp로 생성(trim→contain→composite). **② 홈 상단 카드 좌우 스와이프 캐러셀**: `HomeScheduleCarousel`(무라이브러리 CSS scroll-snap + 점 인디케이터) — 슬라이드 [오늘 현황(기존 CurrentClassCard) → 주간 교사 시간표 → 주간 학급 시간표(우리 반, 내용 있을 때만)]. `WeeklyTimetableCard`(요일×교시 table, 오늘 열 헤더 강조 + 현재 교시 셀 채움). 캐러셀은 벤토 그리드 밖 풀너비로 배치. 학급 시간표 위해 `useMobileScheduleStore`가 `getClassSchedule()`도 로드하도록 확장. (마침 같은 날 [모바일 기능 격차 분석] 1차 추천 "주간 시간표 보기"와 일치.) **③ 일정 페이지 전체 스크롤**: `SchedulePage` 루트를 `flex flex-col overflow-hidden`(내부 목록만 스크롤) → `h-full overflow-y-auto` 단일 스크롤(달력+다가오는 일정이 하나로 상하 스크롤). **함정 대응**: sp-_ 토큰은 Tailwind 투명도 수식(`bg-sp-accent/20`) 무효 → `black/white` 알파·`opacity-_`만 사용. **frontend-design(designer) 협업 리뷰 반영**: 오늘 열 셀별 `ring-1 ring-sp-accent`제거(3중 강조 완화), 과목명`truncate`, `text-[11px]/[9px]`→`text-detail/text-tiny`토큰화, 점 인디케이터 탭영역`py-2` 확대. **미반영(리스크·후보)**: 캐러셀 슬라이드 높이 JS 동기화(높이 다른 슬라이드 가로 스와이프 중 잘림 위험 → auto 유지), gap 스냅 좌표 오차(슬라이드 4개+ 시 재검토). **게이트 4/4**: tsc 0 / lint 0(변경파일) / vitest 269파일 3363 passed / regression 38/38 + 정적 렌더 스크린샷으로 그리드 밀도 확인(390px). **남음**: 실기기 확인(스와이프 감·아이콘 홈추가·전체 스크롤), 커밋(사용자 요청 시).

## ✅ 모바일 기능 격차 분석 (2026-07-03, 분석만 — 구현 없음)

데스크톱 vs 모바일 기능 전수 비교 → 추가 우선순위 제안. 산출물 3건: `docs/03-analysis/mobile-feature-gap/`(격차 분석 본문 + 데스크톱/모바일 전수 목록 2건). 핵심 발견: 동기화 30개 도메인이 이미 모바일 IndexedDB에 전부 내려오므로 격차 대부분은 "화면 부재" 문제. **1차 추천(읽기 전용 3종): 주간 시간표 보기 · 상담 예약 조회 · 즐겨찾기(북마크 — DI 리포지토리는 이미 등록됨)**. 2차: 과제 제출 체크(쓰기, 소형) → 루브릭 채점 입력(쓰기, 별도 PDCA). 3차: 모둠 편성기·자리 뽑기·배점 계산기 도구 이식(MORE_LAZY_TOOLS 패턴). 부적합 확정: 실시간 협업 개설(Electron 터널 서버)·명단/시간표 편집(PC=원본 설계)·파일시스템 의존 도구. 다음: 사용자 우선순위 결정 → 항목별 계획(.plan.md) 작성.

## ✅ 모바일 리팩토링 (2026-07-03, main `5fa0c3e6` 커밋·푸시)

`src/mobile/` 전면 정리 — **동작 완전 보존(순수 리팩토링만), 코드 변경 0건 원칙 준수**. ① 중복 통합: utils/date·haptic, common/ActionSheet·ConfirmDialog·Spinner·EmptyState, AttendanceSummaryCard(출결 카드 2종 통합), hooks/useLongPress, version.ts(표시 버전 단일화). ② 구조: 스토어 3개 `stores/useMobileXxx` 규칙으로 이동·개명, StudentsPage 1,815→484줄(pages/students/ 9파일), TodoPage 398→102, ToolSurveyPage 455→153, App.tsx 도구 라우팅 레지스트리화. ③ 화이트리스트 승계 2건(eslint 래칫 + isVacant 메타 테스트 — ADR-018). 게이트: tsc 0 / lint 0 err / vitest 3,363 passed / regression 38/38. 보고서 `docs/04-report/features/mobile-refactor.report.md`, 보류·최적화 메모 포함. 남음: 커밋(사용자 요청 시), 실기기 확인.

## ✅ v2.2.6 릴리즈 (2026-07-02, tag `v2.2.6`)

v2.2.5(2026-06-27) 이후 묶음. **컴시간알리미 교사 시간표 자동 불러오기**(신규 — 나이스 미제공 교사 시간표를 컴시간 공개 데이터로 구성, 2스텝 위저드·미리보기 적용·비파괴) + **나이스 성적/전과목 파일 업로드 개선**(`sheetGrid`/`htmlTableGrid` 공용 파서 신설 — 나이스가 흔히 주는 'HTML 표를 .xls로 저장'한 파일을 EUC-KR·병합셀 포함 폴백 파싱, 실패 시 .xlsx 재저장 안내) + 모바일 로그아웃·계정변경 노출 + PIN 잠금 위젯 내용 가림 + AI 브릿지 동봉 번들 재생성(쓰기 도구 17종 `ssampin_` 개명·읽기 게이트 안내 복원).

검증 게이트 4/4: tsc 0 / lint 0(경고 131 기존부채) / vitest 3226 passed(1 flaky=FillFormFields 단독 10/10 통과) / regression 38/38 / 프로덕션 빌드 + postbuild 게이트(번들격리·계약동기화) PASS. 버전 8곳(package.json·lock·landing 2·Sidebar·모바일 2) + release-notes.json.

## ✅ v2.1.1 릴리즈 완료 (2026-06-11, tag `v2.1.1`, main `62ba7de`)

v2.1.0(2026-06-01) 이후 미배포 커밋 묶음. 검증 게이트 4/4 통과(tsc 0 / lint 0 errors(MemoFocus 1 기존 부채) / vitest 2097 passed / regression 34/34). **8단계 릴리즈 워크플로우 전부 완료**: 버전 6+lock / release-notes.json / KB Q&A 6건 ingest(289 임베딩) / 노션 메인 callout / 커밋·푸시 / Windows 빌드 / macOS GHA / GitHub 릴리즈 8자산 + URL 8/8 302.

**빌드 트러블 2건 복구**: (1) electron-builder 1차 실패 — `app-builder-lib/@electron/get/node_modules/fs-extra`(v8.1.0) lib/ 본체 누락(중단된 설치 잔재) → 동일 v8.1.0 최상위 사본 복사로 국소 복구(npm install·Electron 종료 불필요). (2) 2차 EPERM rename `win-unpacked.tmp`→`win-unpacked`(Defender 추출물 일시 잠금) → tmp 정리 후 재시도로 해소. **dev Electron 5프로세스는 node_modules/electron/dist 실행이라 종료 불필요했음**. 빌드 가드 `990268309712` app.asar 1건 박힘 확인.

**사용자 노출(release-notes.json 7건)**: ① 서명받기(등록부) 5단계 마법사 전면 개편 + 수업반 명렬 + 임포트 열 매핑 ② 서명 공개 페이지 이름 찾기(초성 검색·소속·가나다) ③ 서명 일시 KST + 공개 링크 정정 ④ 협업 보드 실작동 복구(충돌 해소·도형 12종·교사 [보드 열기]) ⑤ 복합 설문 v2 미리 체험(베타, 옵트인 배너) ⑥ 위젯 카드 크기 조절(대시보드+위젯 모드+발견성) ⑦ "교실 약속 정하기" 진입 차단 해소.

**노출 제외(사용자 결정)**: AI 챗봇 안정화·보안 취약점 12건 패치 — 내부 작업이라 release-notes에서 제외. 챗봇 대화 원문 전체 보기(관리자 analytics)도 교사 노트에서 제외.

**버전 동기화**: package.json·package-lock·landing config/layout·Sidebar·모바일 Settings/More 6+lock = 2.1.1. KB Q&A 6건 신규(서명·협업보드·복합설문·위젯·교실약속). 다음: 커밋 → Windows 5단계 빌드 → macOS GHA → GitHub 릴리즈 + KB ingest + 새 웹 사용자 가이드(`/docs`) 갱신.

## In progress (PDCA)

- 📌 **아이콘 모드 0~4순위 개선 — 창 구조 재설계(버그픽스)+능동 브리핑+오늘 요약 팝오버+노출 개선+분석 이벤트 (2026-07-02, main 미커밋, v2.2.7 후보)** — 진단 세션(같은 날, 실화면 검증)에서 발견한 **잠복 버그**: 아이콘 창이 64×64 고정인데 말풍선(호버 요약·능동 알림 v2.2.3)·코치마크·우클릭 메뉴가 전부 창 밖/잘림 렌더 → **실기기에서 한 번도 보인 적 없음**(v2.2.5가 알림 링을 제거하며 "말풍선으로 확인"이라 안내했으나 그 말풍선이 비표시). **S1 창 구조 재설계(ADR-017)**: UI 필요 시에만 main 이 창을 340×560으로 확장(핀 화면 위치 불변 — `electron/iconWindowGeometry.ts` 순수 기하+테스트 8, 4방향 anchor 플립), 빈 영역은 `setIgnoreMouseEvents(+forward)` 클릭 통과(렌더러가 인터랙티브 요소 hover 참조카운트로 토글), 드래그·화면이탈·재시작 전부 "핀 사각형" 기준(icon-bounds.json 의미 불변). IPC 신설 `icon:set-expanded`/`icon:set-mouse-ignore`/push `icon:layout`·`icon:shown`. **전역 투명 CSS 수술**: `body.ssampin-icon-popup *` 규칙이 말풍선 bg-sp-card 까지 지워 유령상자를 만들던 것 → 구조 요소(html/body/#root)만 투명 강제. `check-icon-alpha.mjs` 검사 목록에 실사용 `sprite-pin.png` 추가(누락 발견 — 검사 결과 잔상 0 확정). **S2 능동 브리핑**: pinPresence 확장 — 아침 첫 수업 30분 전 "오늘 수업 N개" 브리핑 + 점심 60분 전 급식 메뉴(mealSchool→neis 폴백, 모바일 TodayHub 규칙) + 우선순위(수업5분>아침>급식>할일>일정, 시간창 알림이 상시 할일보다 우선). **S3 오늘 요약 팝오버(클릭 재정의)**: 단일 클릭 = `PinPopover`(오늘 수업 목록(교시 시간 포함)·마감 할일 최대 12개 완료토글(스크롤)·한 줄 빠른추가(오늘 마감, 엔터 후 연속 입력)·위젯/전체앱 버튼) 토글, 더블클릭 = 전체 앱(기존 restore 는 팝오버 버튼으로 이동). 코치마크 문구 갱신("쌤핀이에요! 클릭: 오늘 요약…"). **S4 노출**: 핀 이름 상수 `PIN_NAME='쌤핀이'`(pinName.ts 한 곳, 사용자 결정), WidgetTab 표시 모드 섹션에 아이콘 모드 승격 카드+[지금 접기], 모드 코치투어 4번째 슬라이드(+`mode-preview/icon.svg`), landing `/docs` widget-mode 에 아이콘 모드 전용 섹션(docs:check 통과). **S5 측정**: AnalyticsEventName +5(icon_mode_enter/expand·icon_popover_open/quick_add/todo_toggle) — 진입은 main `icon:shown` push 로 기록. **게이트**: tsc 0 / lint 0 errors(경고 131 기존부채) / vitest 258파일 3256 passed + 신규 34(기하8·pinPresence 확장·오버레이 메타5) / regression 38/38 / build-electron 재번들+심볼 3종 grep / docs:check 통과. **실화면 재관찰 불가(환경)**: 저녁 시점 이 PC에서 모든 Chromium(시스템·내장)의 loopback 접속이 먹통(아침엔 동일 방법 성공, node fetch 는 정상) — 브라우저 자동 검증 대신 소스 메타테스트(`iconOverlayLayout.meta.test.ts`)로 버그 부류 재발 차단. ⚠️ 함정 2건: 백그라운드로 띄운 vite 가 "ready" 후 요청에 무응답(자기완결 스크립트로 우회), `cd landing` 후 셸 cwd 잔존으로 dev/regression 오실행. **남은 것(실기기)**: ① 핀 호버→요약 말풍선 ② 능동 알림(수업 전) ③ 클릭 팝오버(수업/할일/빠른추가) ④ 우클릭 메뉴 4항목 ⑤ 드래그·모서리 4방향 플립 ⑥ 화면 배율 150% ⑦ **불투명 잔상 재확인**(렌더러·스프라이트 원인 배제 완료 — 남은 용의자 DWM, 창 확장 시 변화 관찰) ⑧ 급식 브리핑(학교 설정 기기). 반영엔 `node scripts/build-electron.mjs`+앱 재시작 필요(main/preload 변경).

- 📏 **rubric-grading Phase 1~4 + 양식 개선 + 미리보기 + HWPX 표 수술 (2026-06-12~13, main `8302238c`+`eb13d253`+`50f2c99a`+`a599307e`+후속)** — **HWPX 표 3종 결함 수정(사용자 신고 — 한글 출력만 깨짐)**: ① **표 너비** — `addTable`에 너비 미지정이라 기본값(좁게)으로 그려져 한쪽으로 치우침 → 본문 폭 `RUBRIC_HWPX_CONTENT_WIDTH=42520`(skeleton page 59528−좌우 여백 8504×2) + `setColumnWidth`로 열 합=42520 지정(인적사항 19134/10630/12756, 본문 4열 7000/24760/5380/5380, 점수숨김 2열 8500/34020). ② **행 높이** — `cellSz height` 기본 3600(~12.7mm)이 너무 길어 한 쪽 초과 → `RUBRIC_HWPX_ROW_HEIGHT=1800`로 전 셀 축소(한글이 2줄 셀은 자동 확장, 최소 높이 역할). ③ **글자처럼 취급 해제 + 쪽 분할** — `pos treatAsChar="1"`이 표를 한 덩어리로 묶어 다음 쪽으로 통째 점프 → `configureRubricFlowTable`이 treatAsChar="0"+pageBreak="CELL"(이미 기본)+본문 표 repeatHeader="1"(쪽 넘어갈 때 헤더 반복) 설정. 신규 헬퍼 `collectDescendantsByLocalName`(표 element 재귀 수집). **검증**: tsc 0 / lint 0 / vitest 2392 / regression 35/35 + **HWPX 생성→XML 검사**(esbuild 번들+setSkeletonHwpx 주입, 6학생 길게): 셀 너비 합=42520·height 전부 1800·treatAsChar 전부 0·본문 repeatHeader=1 확정. ⚠️ hwpxcore `mmToHwp` 미export(JSDoc만) — 직접 환산(7200 hwpUnit=1inch). **이전 라운드**: **3차 라운드(사용자 신고 "점수 해제 시 출력 오류" + 미리보기 요청)**: ① 오류 재현 — Node(도메인→PDF 풀 파이프라인 ON/OFF)·실브라우저(PDF+HWPX 양쪽 ON/OFF) 전부 정상, **원인 = 편집 중이던 작업 트리를 사용자 dev 앱이 그대로 서빙하다 중간 상태 모듈 동적 import 실패**(콘솔에 RubricFeedbackModal vite 500 기록 확인 — dev 환경 한정, 재시작으로 해소). 회귀 가드로 점수 숨김 PDF 테스트 2건 추가(총 6) + **출력 catch가 실제 오류 메시지를 토스트·콘솔에 표면화**(차후 신고 시 즉시 진단 가능). ② **평가지 출력 모달 미리보기 패널**: 모달 md→xl 2단(좌 설정/대상, 우 종이 미리보기) — 선택 1번째 학생의 평가지를 도메인 `buildRubricFeedbackDocs` 동일 데이터로 흰 종이 HTML 표 렌더(rowSpan 병합·●/○·메모 행·합계·총평), 점수 토글/대상 선택에 실시간 반응. **실브라우저 검증**(vite 5199+playwright, 스토어 시딩→수업 관리→수행평가→채점 화면→모달): 압축 채점 화면·미리보기 ON/OFF 스크린샷 확인 + **점수 OFF '1명 출력' 실행 → PDF 다운로드 성공**. ⚠️ 함정: `useTeachingClassStore.addClass`는 위치 인자(name, subject, students,...) — 객체로 넘기면 name에 객체가 박혀 ClassList 크래시. 게이트: tsc 0 / lint 0 errors / vitest 2384 / regression 35/35. **이전 라운드**: **개선 라운드(사용자 피드백 2건, 같은 날)**: ① 채점 화면 밀도 — "모든 평가 요소가 한 눈에": 요소별 큰 카드 → **한 카드 안의 압축 행 목록**(행 = 번호·요소명(w-36 고정폭 정렬)·수준 버튼(text-xs)·점수·메모 토글), **메모 입력칸 기본 접힘**(edit*note 아이콘 토글, 내용 있으면 자동 표시·아이콘 accent), **합계를 학생 헤더로 승격**(스크롤 없이 상시), 총평 2줄 압축+하단 합계 카드 제거. ② **'피드백 출력' 명칭 폐기 → '평가지 출력'**(버튼·모달 '학생 평가지 출력'·파일명 `*평가지`). ③ **PDF/HWPX 양식을 현장 수행평가 채점기준표 양식으로 전면 재설계**: 가운데 제목+'수행평가 평가지' 부제 → 인적사항 괘선 행(수업반|번호|이름) → 본문 괘선 표 **[평가 요소 | 평가 기준 | 배점 | 받은 점수]**(평가 기준 칸에 수준 목록 ●/○+성취 설명+메모, 요소별 가변 행 높이 D7, 페이지 넘침 시 표 머리글 재출력) → **합계 행**(만점/받은 점수, 결시는 '결시') → **총평 괘선 칸**(비어도 손글씨 기입용 빈 칸 출력). 점수 숨김이면 배점/받은 점수 열 자체 생략. HWPX는 단일 평면 표(요소명은 첫 수준 행에만 — hwpxcore 셀 병합 미지원 대응)+동일 구성. 게이트: tsc 0 / lint 0 errors / vitest 2380+(JsonInteractiveLessonRepository 2건은 타 세션 기록된 부하 한정 flaky — 단독 18/18 통과) / regression 35/35 / PDF 라운드트립 3건 통과. **본 기능 최초 구현**: **Phase 4 (같은 날)**: 채점 화면 헤더 [피드백 출력] → `Rubric/RubricFeedbackModal.tsx`(대상 선택 1명/다중/전체 체크박스 명단 — 결시 기본 해제, 형식 PDF/HWPX 라디오, **점수 포함 토글** 기본 ON) → 도메인 `buildRubricFeedbackDocs`(테스트 4건, 총 50) — **점수 토글 OFF면 배점·합계·만점이 데이터 단계에서 제거되어 렌더러가 점수를 그릴 수 없는 구조**(계획서 §10 점수 숨김 보장). 렌더러 2종: ① `pdf/RubricFeedbackPdf.ts`신설(A4 세로, 학생당 1페이지 기본+넘침 시 이어짐, 요소 단위 블록 D7 — 수준 ●/○ 텍스트 마크(✓류 서브셋 글리프 누락 위험 회피)+체크 수준 성취 설명+메모+총평+합계, 한글 char 단위 wrapText, createPdfContext 공용 헬퍼 재사용) + **라운드트립 테스트 3건**(실폰트 fs fetcher로 캐시 선워밍 → %PDF 매직+재로드 페이지 수 — 전체 스위트 병렬 부하 타임아웃 1회 발견→테스트 30s 명시+본문 축소로 해소). ②`HwpxExporter.exportRubricFeedbackToHwpx`(R-1 스파이크 결론: exportStudentRecordsToHwpx의 문단+소형 표+셀 스타일 패턴 재사용 — 요소당 3열 표(●/○|수준(배점)|성취 설명)+체크 행 볼드, hwpxcore 페이지 브레이크 API 부재로 학생 구분은 ─ 구분 문단, PDF가 1페이지 보장 담당). 렌더러는 모달에서 dynamic import(pdf-lib/hwpxcore 청크 지연 로드). Phase 4 게이트 4/4: tsc 0 / lint 본 범위 0 errors / vitest 185 files 2382 passed / regression 35/35. **Phase 3 (같은 날)**: ① 루브릭 카드 케밥 메뉴 "다른 반에 복사" → `Rubric/RubricCopyModal.tsx`(다중 선택 체크박스 — 과목·학생 수·루브릭 N/10 표시, 한도 도달 반 비활성+안내) → `planRubricCopy`(같은 호출 내 누적 한도 계산) + `ManageRubrics.copyRubric`(허용 반만 `copyRubricToClass`독립 복사, 건너뛴 반 토스트 보고). ② 채점 화면 헤더 [엑셀 내보내기] →`Rubric/RubricExportModal.tsx`(메모 포함 체크 기본 OFF — 나이스 입력용 점수만 권장 카피) → 도메인 `buildRubricExportRows`(번호순, 미채점·결시 null — 0점 강제 금지, 비고 '결시'/'부분 채점') → `ExcelExporter.exportRubricToExcel` 신설(번호/이름/요소별 점수/합계/[메모]/비고, 시트명 금지문자 제거+31자, UnifiedExportModal saveFile 패턴 — electronAPI showSaveDialog+writeFile / 브라우저 Blob 폴백). 테스트: rubricRules +6(행 빌더 4·복사 계획 2, 총 46) + **엑셀 라운드트립 3건**(`ExcelExporter.rubric.test.ts`— 버퍼를 ExcelJS로 재로드해 셀 값 검증, 결시·부분 빈칸 보존 확인 = 실제 파일 바이트 검증). Phase 3 게이트 4/4: tsc 0 / lint 본 범위 0 errors(ExcelExporter import 경고 1건은 UnifiedExportModal과 동일 기존 패턴) / vitest 184 files 2375 passed / regression 35/35. **Phase 2 (같은 날, 사용자 Phase 1 실기기 확인 후)**: 루브릭 카드 클릭 →`Rubric/RubricGradingView.tsx`신설 — 좌측 학생 명단(번호·이름·완료/부분/결시 뱃지·합계 미리보기, ↑↓ 키보드 이동 — 입력 중 가드) + 우측 선택 학생 루브릭 카드(요소 단위 블록 D7, 수준 클릭 즉시 저장·재클릭 해제, 체크된 수준의 성취 설명 노출 D5, 요소별 특이사항 메모+총평 blur 저장 D6, 결시 토글 D8 — 체크 보존·입력 비활성·합계 제외, 합계/만점 푸터 D1). 도메인`rubricRules`에 채점 갱신 순수 함수 6종(createEmptyGrading/toggleMark/setAbsentStatus/setCriterionNote/setOverallFeedback/findGrading)+테스트 10건 추가(총 40) / `ManageRubrics.upsertGrading`((rubricId,studentId)당 1건 단일 저장 경로) / `useRubricStore` 채점 액션 4종. Phase 2 게이트 4/4: tsc 0 / lint 본 범위 0 errors / vitest 183 files 2366 passed / regression 35/35. **Phase 1**: 계획서 [docs/01-plan/features/rubric-grading.plan.md](docs/01-plan/features/rubric-grading.plan.md) Plan v1.1 (D1~D8 확정) Phase 1/4. **신규 9파일**: domain(`entities/Rubric.ts`— Rubric/RubricCriterion/RubricLevel(요소 종속 수준, D7)/RubricGrading(graded·partial·absent, D8) +`rules/rubricRules.ts`— 한도(반당 10·요소 1~10·수준 2~6)/검증/단순합계(D1, absent→null)/상태 유도/구조변경 영향 계산·정합화(FR-2 가드)/독립 복사(D2) + 테스트 30건) /`repositories/IRubricRepository`/ usecase`rubric/ManageRubrics`(한도·검증 강제 CRUD) / adapters(`JsonRubricRepository`('rubrics' 키) + `useRubricStore`+`Rubric/ClassRubricTab.tsx`(목록·카드·삭제 확인·한도 N/10·명단 없음 RosterEmptyState 패턴→명렬 관리 탭 CTA) + `Rubric/RubricBuilderModal.tsx`(요소 단위 블록 편집, 직전 요소 수준 복제, 실시간 만점, 채점 기록 존재 시 구조 변경 경고 단계, `flex-1 min-h-0`높이 상속)). **수정 5파일(전부 추가 전용)**:`ClassManagementPage.tsx`탭`rubric`(수행평가, 과제 수합 옆) / `container.ts`(rubricRepository+manageRubrics) / `syncRegistry.ts`#26 'rubrics' /`App.tsx` STORE_SUBSCRIBE_MAP 'rubrics' 1줄+import 1줄(⚠️ 다른 세션 수정 중 파일 — SyncSubscribers 메타테스트 (b)가 등재를 강제해 최소 추가만) / 본 파일. **게이트 4/4**: tsc 0 / lint 본 범위 0 errors(전체 1 error는 MemoFocus 기존 부채, 신규 경고는 uuid import 2건 — useSurveyStore와 동일 기존 패턴) / vitest 182 files 2348 passed(신규 rubricRules 30) / regression 35/35. sp-\* 투명도 수식(`bg-sp-accent/40`류) 신규 코드 0건. **남은 것(전 Phase 완료 후)**: 실기기 종합 확인 — 앱 재시작 → '수행평가' 탭 → 루브릭 생성(요소 3, 수준 구성 서로 다르게) → 채점(수준/메모/총평/결시/↑↓) → [엑셀 내보내기] 파일 열어 값·빈칸 확인 → [피드백 출력] 점수 숨김 PDF에서 점수가 정말 안 보이는지 + HWPX 한글에서 열기 → [다른 반에 복사]. 계획서 4 Phase 전체 구현 종료 — **다음 릴리즈 v2.1.2 묶음 예정**(사용자 확정 2026-06-13). Phase 1 실기기 확인은 사용자 완료(2026-06-12). 홍보 자산(Threads 6타래+카드뉴스 6장) `docs/release-notes-assets/rubric-grading/` 커밋 완료(`6959667e`).

- 🎨 **협업보드 G006 (PDCA-4) 내 템플릿 저장 완료 (2026-06-12, main 미커밋)** — 교사가 직접 그린 보드를 재사용 가능한 밑그림으로 보관: 보드 패널 [내 템플릿으로 저장](이름 입력 모달, 활성 세션이면 실시간 Y.Doc 인코딩·아니면 .ybin) → `userData/data/boards/templates/{tpl-xxx}.json`(요소 verbatim + versionSchema 0.17.6) → "새 보드 만들기" 다이얼로그의 **내 템플릿** 섹션(요소 수·날짜 카드 + hover 삭제 + 선택 삭제 시 blank 복귀)에서 골라 생성. 생성은 G005 시딩 경로 그대로 재사용 — 신규 `BoardSnapshotCodec`(스냅샷↔요소 추출·재시딩)로 패킹 로직을 일원화(BoardTemplateSeeder 가 코덱을 쓰도록 리팩토링). **핵심 원칙: 요소 id 재생성 금지(verbatim)** — 포스트잇 bound text 의 containerId/boundElements 가 요소 id 를 참조하므로 재생성 시 라벨-도형 연결이 깨짐(보드별 Y.Doc 분리라 같은 id 충돌 없음). 신규: UserTemplate 엔티티+sanitize / IUserTemplateRepo·IBoardSnapshotCodec 포트 / FileUserTemplateRepo(손상 파일 관용) / ManageUserTemplates(빈 보드 USER_TEMPLATE_EMPTY 거부·"내 템플릿 N" 자동 이름) / ManageBoard.createFromUserTemplate(보드명 비우면 템플릿명 승계) / useUserTemplateStore / IPC 3채널(user-template-list/save/delete) + create.userTemplateId. **검증**: tsc 0(본 범위) / lint 0 / vitest 신규 19(코덱 6+레포 5+유스케이스 8) + **전체 182파일 2348 passed 0 실패**(G005 때 flaky 였던 hot-path 도 이번엔 통과) / regression 35/35 / build-electron 재번들+심볼 grep 8 / **E2E 라운드트립**: 조별활동 스냅샷 → 코덱 추출(=내 템플릿 저장) → 재시딩(=내 템플릿으로 생성) → 실브라우저 렌더가 원본과 동일(`docs/03-analysis/collab-board-templates/user-roundtrip.png`, 페이지 에러 0). **다음**: 실기기 확인(보드에 그림 → [내 템플릿으로 저장] → 새 보드 → 내 템플릿 선택) → G005 와 함께 커밋(명시 path) → G007(PDCA-5 권한 3단계 + 나만보기 + boardRules SRP 분할).

- 🎨 **협업보드 G005 (PDCA-3) 템플릿 4종 완료 (2026-06-12, main 미커밋)** — 새 보드를 만들 때 학습 활동 밑그림을 골라 시작: "새 보드" → 템플릿 선택 다이얼로그(빈 보드/만다라트 9×9/조별 활동 6모둠/브레인스토밍 4분면/도형 다이어그램) → 잠긴 밑그림이 깔린 채 생성, 세션 시작 시 학생·교사 모두에게 그대로 보임. **핵심 설계 결정(ADR-012)**: plan AC-3.1이 미뤄둔 Teacher page ExcalidrawBinding 부착 여부를 **미부착 + 보드 생성 시점 main process Y.Doc 시딩(.ybin)**으로 확정 — SP-2가 확정한 y-excalidraw 2.0.12 저장 형식(`Y.Array<Y.Map{pos, el}>`)을 서버에서 직접 생성, 세션 시작 시 기존 initialState 경로가 그대로 전파(클라이언트 레이스 0). `pos` 호환 위해 **fractional-indexing 3.2.0**(클라이언트 CDN 동일 버전) 루트 의존 추가(esbuild 번들이라 packaged 안전). 4레이어 신규: domain(`BoardTemplate`+`boardTemplateRules` 4빌더+`IBoardTemplatePort`)/infra(`BoardTemplateSeeder` — Excalidraw 0.17.6 전 필드 명시 생성, 시딩 요소는 클라 restore() 미경유)/usecase(`ManageBoard.create(name, templateId)`)/adapter(`BoardNewBoardDialog` — 시딩될 캔버스의 토큰색 축소 미리보기, frontend-design 협업) + IPC·preload·global.d.ts·useBoardStore 배선 + `Board.templateId`(optional, 기존 보드 호환). **AC-3.3**: 학생 페이지 toolbar 교사 전용 "템플릿 🔒 잠금/해제"(locked 일괄 플립+version bump 전파, 토글 방향은 실제 요소 상태에서 유도) + **학생 선택 차단 가드 보강**(템플릿 요소는 잠금 해제 후에도 학생 선택 차단). 토큰 7종(`--sp-board-template-cell`+`group-{r,b,y,g,p,o}`) 라이트/다크 + 도메인 hex 동기 메타테스트(index.css 읽어 강제). **검증**: tsc 0(본 범위) / lint 0 / vitest 신규 33 + 전체 2254 passed(JsonInteractiveLessonRepository hot-path 1건은 전체 부하 한정 flaky — 단독 재실행 통과) / regression 34/34 / 인라인 JS 구문·마커 12종 PASS / build-electron 재번들+심볼 grep 14 / **실브라우저 E2E**(playwright-core+시스템 Chrome, 실제 YDocBoardServer 기동): 4템플릿 렌더·학생 동기화·교사 토글·이름모달 생략·학생측 토글 숨김 PASS — 스크린샷 5장 `docs/03-analysis/collab-board-templates/`. ⚠️ Playwright MCP는 전날 chrome 잔존 프로세스의 프로필 잠금으로 우회(별도 user-data-dir). **다음**: 실기기 확인(앱 재시작 → 새 보드 → 만다라트 → 세션 시작 → 학생 입장) → 커밋(명시 path) → G006(PDCA-4 내 템플릿 저장 — 동일 스냅샷 경로 재사용 가능).

- 🎨 **student-pages-design-refactor — Phase 1~3 구현 완료 (2026-06-12, main `2a8cde37`~`beb3e5fd` 커밋 7개)** — 학생/보호자 접속 페이지 8종 중 7종 디자인 통일 + P0 결함 수술. 감사 4건(`.omc/tmp/design-audit/`) → 분석(`docs/03-analysis/student-pages-design-audit.analysis.md`) → Plan v1.0 + 결정 4건(D1~D4) 사용자 승인 → 구현. **Phase 1(`2a8cde37`)**: `_studentPageChrome.ts`에 공용 셸 신설 — `--sps-*` 토큰(accent #3b82f6/카드 #1a2332/radius 12·8) + 상태화면 4종(SVG 체크 완료) + spsToast/spsSetPending + 줌 허용 viewport(WCAG 1.4.4) + Pretendard CDN+swap 폴백. **ADR-013: 기존 `--color-*`(DN-10, MultiSurvey v2 SSOT)는 불가침 — 네임스페이스 분리로 충돌 0.** 메타테스트 18건(`_studentPageChrome.shell.test.ts`) + REGRESSION #47. **Phase 2(`8466c7fb`~`9f3f2f64`, 페이지당 1커밋)**: 객관식(silent fail 토스트+교사색 휘도 대비 보정+ack 6s 복구)/주관식(placeholder 안내→토스트+카운터 aria-live)/워드클라우드(invalid 흔들림+토스트·연타 잠금·남은횟수 도트·칩 틴트 4색)/가치수직선(**끊김 화면 신설 P0**+연결 칩 최초 이식+role=slider 키보드+라운드 전환 안내)/신호등(**비색상 선택 표식 P0 — `--signal-color` 체크 원판**+position:fixed+100dvh로 음수마진 핵 제거+safe-area+말풍선 직각 제거). **복합 설문은 D3 발동으로 skip** — `liveMultiSurveyHTML.ts`+`liveMultiSurvey.ts`에 다른 세션(MultiSurvey v2 라이브 흐름) 미커밋 수정 확인, 셸 적용은 multisurvey-v2 PDCA에 위임. **Phase 3(`beb3e5fd`)**: PublicPageHeader/Footer 신설(앱 아이콘+개인정보 한 줄 "입력한 내용은 선생님에게만 전달됩니다") + check sp-\* 전면 이관(raw gray/blue 제거) + NETWORK_ERROR sentinel("설문 없음"/"PIN 오류" 오표시 해소)+OfflineNotice 재사용 + 접근성(role/aria-pressed/○예·×아니오) + submit 버그 2건("학년 반 7번" 빈값·10MB 초과 후 파일 재선택 무반응) + ChatWidget /submit·/check·/sign 숨김. **게이트**: tsc 0 / lint 0 / vitest 2276 passed(176 files) / regression 35/35 + 인라인 JS 구문·마커 검사(`.omc/tmp/check-student-html.mjs`) 5/5 + landing tsc 0+next build 통과. ⚠️ 함정: REGRESSION #18~21이 각 페이지 소스의 `[hidden]{display:none!important}` 리터럴을 요구 — 셸로 이동 금지. **남은 것**: ①Playwright 모바일(390×844) 스크린샷 세트 ②실기기 검증(도구 페이지 반영은 앱 재시작+도구 세션 재시작 필요) ③다음 릴리즈 묶음(v2.1.2) ④check PIN 보안 재설계(별도 보안 트랙). ADR-013은 DECISIONS.md에 추가됨(협업보드 ADR-012와 함께 미커밋 — 해당 세션 커밋 시 동반).

## Recently completed (다음 릴리즈 묶음 후보)

- 🕘 **컴시간 교사 시간표 자동 불러오기 (2026-07-02, main 미커밋)** — 나이스 API는 학급 시간표만 제공(교사 필드 없음)이라, 컴시간알리미 공개 데이터를 해석해 교사 시간표를 자동 구성하는 신기능. comcigan.ts 라이브러리가 컴시간 구조 변경으로 현재 고장 상태(2026-07-02 실측)여서 **자체 경량 파서를 직접 구현**(해석 공식: 수업 코드를 오른쪽 3자리 그룹으로 분해 — 교사 idx = 코드%1000, 과목 idx = floor(코드/1000)%1000%분리). **신규 4레이어**: domain(`entities/ComciganTimetable.ts` + `rules/comciganRules.ts` 순수함수 4종 + 테스트 10 — 온양여고 실측 픽스처) / port(`IComciganPort`) / infra(`comcigan/eucKr.ts` TextDecoder 역테이블 EUC-KR 인코더 + `comcigan/ComciganApiClient.ts` 주입식 transport·/st 라우트 정규식 추출 10분 캐시 + 테스트 11) / adapter(`Timetable/ComciganImportModal.tsx` 2스텝 위저드: 학교 검색(나이스 학교명 프리필)→교사 선택(자율/창체 더미 제외·이름 필터·주N시간·마스킹 안내)). **통신은 Electron main IPC 위임**(`electron/ipc/comcigan.ts` — comci.net은 CORS 미지원이라 렌더러 직접 fetch 불가, safeFetchBytes 경유 + 호스트 comci.net 고정 + 4MB/15s 제한) + preload `comcigan.fetchRaw` + global.d.ts 타입 + vite '/comcigan-api' dev 프록시. **적용은 기존 TeacherExcelPreviewModal 미리보기 흐름 재사용**(미리보기→확인 후 저장, undo 보존 — 비파괴). 실패(미등록 학교·구조 변경 SERVICE_CHANGED·오프라인) 시 친화 메시지 + 엑셀/수동 입력 폴백 안내 — 비공식 데이터원이라 부가기능으로 설계. **발견성 개선(사용자 지시, 같은 날)**: [컴시간에서 불러오기]를 TimetablePage 교사 탭 헤더(직접 편집 밖)로 이동, 양식 다운로드+엑셀 불러오기 세트는 직접 편집(TimetableEditor) 안에만 유지 — 페이지의 기존 미리보기·적용(handleExcelConfirm) 경로 재사용. **검증 게이트 4/4**: tsc 0 / lint 신규·변경 파일 0 errors(경고 3건은 기존 부채) / vitest 257 files 3227 passed(신규 21) / regression 38/38 + architect 리뷰 APPROVED(LOW 1건 주석 반영). **다음**: 실기기 확인(시간표 → 교사 시간표 탭 → [컴시간에서 불러오기] → 온양여자고등학교 → 본인 선택 → 미리보기 적용) → 다음 릴리즈(v2.2.6) 묶음 후보. ⚠️ 컴시간이 페이지 구조를 바꾸면 SERVICE_CHANGED로 표면화됨 — `comciganRules.ts`/`ComciganApiClient.ts` 정규식·공식만 갱신하면 복구.

- 📊 **학급 성적 살펴보기 + NEIS 전과목 성적 가져오기 — 담임 업무 읽기 전용 화면 (2026-06-18, main 미커밋)** — 담임이 다른 교과 점수를 직접 못 갖는 현실을 보완: NEIS 전과목 성적 일람표(xlsx)를 가져오면 학생별 성취도·석차·상담 신호를 본다. **읽기 전용** — 점수 수정·교과 세부 채점은 수업 관리 담당(화면 안내 문구 명시). **제1원칙 준수**: 학생 점수는 이 PC에만 저장(`container.ts` 주석 "로컬 전용, syncRegistry 제외" — Drive sync 비대상), 외부 전송 0. 계획: [docs/01-plan/features/grade-analysis.plan.md](docs/01-plan/features/grade-analysis.plan.md) Phase 6/8 (⚠️ 계획서 상태는 아직 **"검토 대기(pending approval)"** — 구현이 승인 전 선행됨, 계획상 제안 파일명 `homeroomGradeSignals.ts`는 실제 `transcriptAnalysis.ts`로 이탈). **신규 4레이어**: domain(`entities/ImportedTranscript.ts` + `rules/neisTranscriptImportRules.ts`(+테스트) + `services/transcriptAnalysis.ts`(+테스트, 요약·등급경계 근접 과목·취약 과목 신호) + `rules/gradeStandardRules.ts`에 `peopleToNextGrade`(석차 한 등급 상승 필요 인원, 기관 산출 등급 일치, +테스트)) / usecase(`transcript/ManageImportedTranscript.ts`(+테스트, studentKey upsert·upsertMany·clear)) / adapter(`stores/useTranscriptStore.ts` + `repositories/JsonImportedTranscriptRepository.ts` + `Homeroom/GradeOverview/HomeroomGradeOverviewTab.tsx`) / infra(`parse/NeisTranscriptExcelParser.ts`). **배선**: DI `container.ts`(transcriptRepository·manageImportedTranscript·parseTranscriptExcel export) + 담임 탭 `학급 성적`(📊) 추가(`HomeroomTabBar.tsx` `grades` + `HomeroomPage.tsx` 렌더) + 대시보드 위젯 `grade-summary`(📊, 개인 점수 미표시) 등록(`registry.ts` + `items/Grades.tsx`). 척도(rank5/rank9)는 학급 전체 석차등급 최대값으로 자동 추론(선택 학생만 보면 오분류). **검증 게이트(직접 실행, 2026-06-19)**: tsc 0 / 타깃 vitest 7파일 77 통과(examAllocation·transcript·gradeStandard·toolRegistration·registry) / 전체 vitest 2644 passed(2 실패는 JsonInteractiveLessonRepository 전체-부하 한정 flaky — 단독 재실행 18/18 통과) / regression 35/35 / lint 신규 파일 0건(error 1은 MemoFocus 기존 부채). **다음**: 실기기 확인(앱 재시작 → 담임 업무 → 학급 성적 → 엑셀 가져오기 → 요약·등급경계·취약 신호·과목별 표) → 계획서 승인/정정 → 커밋(⚠️ 명시 path — working tree에 동시 편집 중인 `scoringRubricParser.ts`(루브릭 가져오기, 타 세션) 동거).

- 🧮 **지필평가 배점 계산기 — 쌤도구 신규 도구 (2026-06-17, main 미커밋)** — 시험 출제 시 객관식+서답형(서술형/단답형) 문항별 배점을 만점에 맞춰 설계하는 도구. **학생 개인정보 0건**(문항 메타만) — 제1원칙(AI 0·로컬·학생데이터 외부전송 0) 가장 안전한 부류. **부동소수점 안전**: 배점 소수점 허용 → 모든 산술을 센티포인트(×100 정수)로 수행(`examAllocationRules`: 3.5×20=70.0 / 0.1×10=1.0 드리프트 0). **서답형 비율 검증**: (단답+서술)/만점, 교사가 목표비율 설정 시 미달 경고(학업성적관리규정, 고정값 금지). **균등배분**: step 단위 분배 + 나머지 앞 문항 결정론 흡수. 신규: domain(`entities/ExamPaper.ts` + `rules/examAllocationRules.ts` 순수함수 11종 + 테스트 25) / adapter(`Tools/ToolScoreAllocator.tsx` — sp-\* 토큰·단일 초안 localStorage 자동저장). 등록 5지점(Sidebar PageId / ToolsGrid TOOLS 🧮 / toolDefinitions / App 라우팅) + `toolRegistration.test.ts`에 라우팅 가드(App 소스 분기 검증) 추가. AnalyticsEvent ToolName에 'score-allocator' 1줄. 계획: [docs/01-plan/features/exam-score-allocator.plan.md](docs/01-plan/features/exam-score-allocator.plan.md). **검증 게이트 4/4**: tsc 0 / lint 0 / vitest 신규 29(도메인 25 + 등록 4) · 전체 2567 passed(유일 실패 FillFormFields는 무관 PDF 테스트의 전체-부하 5s 타임아웃 — 단독 재실행 10/10 통과) / regression-check 통과. **범위 밖(Phase 2/3)**: 다중 시험지 라이브러리(repository+DI) · 이원목적분류표 Excel/HWPX 출력 · 성취기준 EvaluationPlan 자동완성 · 듀얼모드 · 난이도/단원 분포. **다음**: 실기기 확인(앱 재시작 → 쌤도구 → 배점 계산기 → 빠른배분·합/잔여·서답형 비율) → 다음 릴리즈 묶음 → 성적 분석은 수업관리 별도 트랙(Phase 2).

- 📋 **MultiSurvey v2 이연 기능 5종 완결 (2026-06-12, main 미커밋)** — 2026-06-11 라이브 흐름 완결 때 후속으로 미뤄둔 항목 전부 구현(계획: `_workspace/plan-multisurvey-v2-deferred.md`). (1) **Share view(교실 모니터 화면) 연결**: 콘솔 [교실 화면 열기] → 별도 BrowserWindow(`?mode=msShare`, `electron/ipc/multiSurveyShare.ts` 신규) — liveSession은 메모리 전용이라 별도 창과 store 공유 불가 → 메인 윈도우가 phase/응답 변화마다 직렬화 스냅샷(`Share/shareSnapshot.ts`) IPC push + main process가 마지막 스냅샷 캐시 후 늦은 오픈/새로고침 시 did-finish-load 재전송(자동 넘김 OFF에서 빈 화면 방지). 입장코드 잔재 제거 — ShareEntryCodeBar/ShareLobbyScreen QR+URL 전용 전환. installNavigationGuard + open 핸들러 payload 런타임 가드. (2) **콘솔 타이머 실시간 카운트다운**: `useQuestionCountdown` 훅(1초 interval, onExpire 1회 ref 가드) → 0초 시 기존 reveal 전이 재사용. "자동 넘김 OFF 시 타이머 비표시" 결정 유지. (3) **빠른풀이/연속정답/랜덤 보너스**: domain `calcSessionScore()` 순수 함수(rng 주입식 — Math.random은 adapter 경계에서만) + store `questionOpenedAt`(메모리) + `appendResponse`에서 직전 연속 정답 누적 계산 후 scoreEarned 갱신(재제출 upsert 시 streak 불변 처리) → RoundResult/Podium은 scoreEarned 합산이라 무수정 자동 반영. **산식(설계서 미정의 → 제안값 적용, 사용자 확인 후보)**: 빠른풀이 = 남은시간 비례 최대 base의 +50% / 연속 = min(streak,5)×2점 / 랜덤 = 0~5점, 오답이면 보너스 전부 0. (4) **blank 초성 비교**: `normalizeHangulInitial()` domain 순수 함수(signatureNameSearch toChosung 로직 복제 — src/student import는 의존 역방향이라 복제가 정답) + isHangulInitial=true 시 공백 제거·초성 정규화 비교("ㅎㄱ"↔"한국"). (5) **STUDENT_WAVE/TOGGLE_FOCUS_MODE**: 학생 HTML(liveMultiSurveyHTML 전용 — 공용 셸 미오염) 아바타 탭 → WS `{type:'wave'}`(클라·서버 양측 2초 스로틀) → 콘솔 아바타 3초 pulse / 교사 👀 집중 모드 토글(displayOpts.teacherFocusMode ON일 때 노출) → 전 학생 WS broadcast 전면 디밍("선생님을 봐 주세요 👀"), 늦게 입장한 학생도 join 시 현재 상태 수신, ws.onclose 시 오버레이 해제. **검증 게이트 4/4 (2026-06-12)**: tsc 0 / lint 0 errors(MemoFocus 1건 기존 부채) / vitest 180 files 2309 passed(신규 ~75건: rules 28+score 11+wave 7+countdown 7+share 21) — JsonInteractiveLessonRepository hot-path 1건은 병렬 부하 한정 flaky, 단독 재실행 18/18 통과 / regression 35/35. guard 아키텍처 검증 PASS(7항목 — WARNING 1건 IPC 가드도 수정 반영). `node scripts/build-electron.mjs` 재번들 + 번들 grep(multi-survey-share 5 / toggle-focus-mode 1 / student-wave 1). **다음**: 실기기 수동 검증(2-모니터 Share 창 phase 전이 + 학생 wave/집중모드 + 보너스 점수 포디움 + 타이머 자동 넘김) → 커밋(⚠️ 명시 path 필수 — working tree에 협업보드 G005 board.ts/useBoardStore.ts 동거 중).

- 📌 **memo-classroom-share 구현 완료 — 메모 교실 공유 (2026-06-11, main 미커밋)** — 교사가 포스트잇 N개를 골라 보드로 묶으면 고정 링크(`ssampin.com/memo/{fileId}`)에서 학생이 실시간(5초 폴링)으로 읽는 신기능. **저장소는 Supabase가 아니라 선생님 개인 Google Drive**(정보 유출 우려 사용자 결정, ADR-011) — 쌤핀 서버 무저장, anyone-with-link reader 권한으로 읽기 전용 구조 보장, 공유 중지 시 영구 삭제. 기존 `drive.file` 스코프로 충분(OAuth 재심사 불필요). 4레이어 신규: domain(entities 2+port+rules)/usecases 3종/infra(GoogleDriveClient 메서드 2개 추가+MemoShareDriveClient)/adapters(useMemoShareStore — useMemoStore subscribe 관찰+debounce 1.5s, MemoShareModal, 배지) + landing 7파일(`/memo/[id]` 미니멀 그리드·라이트/다크 명도 반전·Web Audio 차임·PWA 동적 manifest+SW). PDCA 문서: plan v0.3 + design v0.2 + `_workspace/plan.md` v2(SC-1~10 성공 기준). **검증**: 게이트 4/4(tsc 0 / 신규 파일 lint 0 / vitest 170 files 2161 passed / regression 34/34) + guard 아키텍처 검증 PASS(7항목) + Playwright 모의 E2E(렌더·7초 내 실시간 반영·수정 반영·404 안내·라이트/다크 스크린샷 → `docs/03-analysis/memo-classroom-share/`). **개선 루프 2건**: 챗봇 버튼 `/memo` 경로 숨김(ChatWidget pathname 가드) + 공유 중지 화면의 "다시 연결 중" 상태 점 제거. ⚠️ 함정 기록: sw-memo.js가 dev에서 옛 청크를 cache-first로 잡아 코드 수정이 안 보임(prod는 해시 URL이라 무관) — dev 검증 시 SW unregister 필요. **남은 것**: ① GCP 브라우저 API 키 발급(Drive API 한정+referrer 제한)+Vercel `NEXT_PUBLIC_GOOGLE_DRIVE_API_KEY` 등록(사용자 1회, 미등록 시 페이지가 설정 오류 화면으로 안전) ② 실기기 E2E(Electron 앱 공유→실 Drive→교실 페이지) ③ deletedMemoNotice가 모달 안에서만 보임 — 토스트 승격 후보 ④ 동기화 큐 메모리 전용(재시작 직전 변경분 push 누락 가능, 다음 수정 때 자연 복구). ~~landing/.env.local에 dummy 키 잔존~~ → 실키 교체 완료. **2차 추가(같은 날, 실사용 피드백 3건)**: ① 개인정보 경고·삭제 확인·동기화 칩의 amber 저대비 → 공용 `Notice`/sp 토큰으로 교체 ② "페이지 설정 미완료" 원인 = **ssampin.com 서빙 프로젝트는 Vercel `ssampin`인데 키를 옛 `landing` 프로젝트에 등록했던 것** → `ssampin`에 재등록(printf로 줄바꿈 혼입 방지, ⚠️ `echo |` 파이프는 trailing \n 들어감) ③ **공유 중 보드 포스트잇 추가/삭제 편집**: store `updateBoardMemos`(desired 구성 맵 → 동기화 큐 직렬화, 같은 fileId PATCH라 링크·QR 불변) + 모달 편집 모드(체크박스 그리드 재사용). 게이트 재통과 4/4(vitest 2166). manifest 서버 fetch에 Referer 명시(키 referrer 제한 대응).

- 🛡️ **Dependabot 취약점 12건 일괄 처리 (2026-06-11)** — v2.0.x security-hardening 잔여 패시브("npm audit 게이트 승격") 일부 해소. **루트**: shell-quote 1.8.3→1.8.4(critical, concurrently가 exact pin → `overrides` 필요) + tmp 0.2.5→0.2.7(high, `npm audit fix`) + uuid 8.3.2→11.1.1(moderate, @blocknote/exceljs 경유 → `overrides` — 취약점은 v3/v5/v6+buf 전용이라 v4만 쓰는 양쪽 모두 무영향, Excel/Xlsx exporter 테스트로 실증) / **file-type(moderate)은 수정판 없음** — jimp 0.22(nut-js)가 16.x exact pin + 17+는 ESM-only라 강제 시 스티커 자동 붙여넣기 파손 → GitHub alert #21 `tolerable_risk` dismiss(입력이 로컬 전용). **landing**: next 16.1.6→16.2.9(high 1+moderate 다수, 동일 마이너) + 중첩 postcss 8.4.31→8.5.15(`overrides`) → `npm audit` 0건 + tsc 0 + next build 통과. **spike(s1-cdn-poc)**: ws 8.21.0 lockfile-only 갱신. **사고 1건 복구**: 클린 재설치 중 실행 중인 Electron dev 앱이 `electron/dist` 잠금 → rm 중단 → exceljs 중첩 bluebird 손상(테스트 4파일 로드 실패)으로 발현 → exceljs 서브트리 재설치로 정상화. ⚠️ 교훈: **클린 재설치 전 Electron dev 앱 종료 확인 필수**. 검증: tsc 0 / lint 0 errors / vitest 164 files 2097 passed / regression 34/34 / build-electron OK / landing build OK. 남은 audit: 루트 moderate 7건 전부 file-type 사슬(수정 불가, dismiss 처리).

- 🧩 **위젯 모드 카드 크기 조절 — 대시보드와 동일 그립 이식 (2026-06-11, main `0fd3f31` 커밋)** — Widget.tsx(Electron 위젯 창) 카드 셀에 리사이즈 그립 3종(우측·하단·코너) 이식, useDashboardConfig 공유로 설정 일원화. 핸들 3종에 **transform: scale() 보정** 추가(분할 레이아웃 split 0.85 / quad 0.7에서도 드래그→칸 변환 정확) + WidgetVerticalResizeHandle 줄 높이·간격 하드코딩(80/16) 제거 → 그리드 computed style 직접 읽기(위젯 모드 gap 12px 대응). sidebar-right(1열)는 가로 조절 자동 비활성. 검증 게이트 4/4 + Playwright `#widget` 실브라우저(코너 드래그 2칸×5행→3칸×6행 저장, scale 0.85에서 시각거리 1칸분 = 정확히 ±1칸). ⚠️ 위젯 모드 첫 진입 코치 투어가 hover/드래그를 가리므로 자동화 검증 시 건너뛰기 필요.

- 🧩 **위젯 리사이즈 발견성 개선 — 호버 즉시 그립 + 코너 핸들 (2026-06-11, main `107782c` 커밋)** — 사용자 피드백 "상하 조절 방법 발견이 너무 어렵다". (1) 리사이즈 그립(우측·하단) **항상 mount + 카드 호버만으로 즉시 표시**(group-hover CSS) — 숨은 quadrant dwell 의존 제거(dwell은 ⋮/✕ pill 전용 존치). (2) **우하단 ㄴ자 코너 핸들 신설**(`WidgetCornerResizeHandle.tsx`) — 대각선 드래그로 가로+세로 동시 조절, 프리뷰 "넓게 3칸 × 6행". (3) 그립을 카드 테두리에 걸치게 배치(-right/-bottom-1.5)로 콘텐츠 가림 최소화 + sp-accent 색 + 60%→hover 100% opacity. (4) ⚠️ **전역 함정 발견: sp-\* 색 토큰은 Tailwind 투명도 수식(`bg-sp-accent/40` 등)이 무효** — tailwind.config 색상이 `var(--sp-*)` 원시값이라 `<alpha-value>` 미지원 → 클래스 자체가 미생성되어 **조용히 투명**이 됨. 기존 그립이 안 보였던 진짜 원인. 코드베이스에 `bg-sp-accent/N` 9건(5파일) 동일 잠재 문제 잔존 — 별도 정리 후보. frontend-design 스킬 협업. 검증 게이트 4/4(tsc 0 / eslint 0 / vitest 2097 / regression 34) + Playwright 실브라우저(그립 opacity 0→1, 우측/하단/코너 드래그 저장, 카드 클릭 모달 비간섭).

- 🧩 **대시보드 위젯 크기 조절 회귀 fix (2026-06-11, main `260f2d0` 커밋)** — 사용자 신고 "위젯 카드 크기 편집이 안 됨". 근본 원인: v2.0.8 widget-modal 개편(`4442207`)에서 편집 모드 → quadrant dwell 호버(우상단 60×60 + 300ms)로 바뀌면서, 핸들 표시 후 모서리 zone을 벗어나면 **100ms 만에 핸들이 unmount** — 우측(가로)/하단(세로) 리사이즈 핸들까지 마우스가 도달할 수 없어 크기 편집이 사실상 불능이었음(⋮/✕ pill에만 유지 처리가 있었고 리사이즈 핸들에는 누락). 수정: 핸들 표시 후에는 카드 내부에 마우스가 있는 동안 유지 + 카드 전체 이탈 시 150ms 후 숨김 + 리사이즈 드래그 중(`isResizing`)에는 카드 밖에서도 유지. 표시 조건(quadrant dwell)은 그대로. 변경 3파일: `SortableWidget.tsx` / `WidgetResizeHandle.tsx` / `WidgetVerticalResizeHandle.tsx`(`onDraggingChange` prop 추가). 검증 게이트 4/4: tsc 0 / eslint 변경 파일 0 / vitest 2097 passed (164 files) / regression 34/34 + **Playwright 실브라우저 검증**(quadrant dwell→핸들 표시→가장자리 이동 후 유지→드래그 colSpan 2→3(653→986px)·rowSpan 5→8 localStorage 저장 확인). ⚠️ 부수 사건: 커밋 중 lint-staged .bin 링크 깨짐 발견 → npm install 중 실행 중이던 dev Electron 5개가 파일 잠금으로 EBUSY 중단 → electron 프로세스 종료 후 `npm install` 재실행으로 node_modules 완전 복구(lockfile 변경 없음).

- ✅ **챗봇 장애(6/3~6/11) 복구 + 대시보드 대화 원문 전체 보기 (2026-06-11, main `4922621` push 완료)** — (1) **장애 진단·복구 완료**: 6/3 11:09 KST부터 챗봇 전면 다운(`ssampin-chat` 500). 근본 원인: Gemini `GOOGLE_API_KEY` 무효화(`API_KEY_INVALID`) — 로컬 `.env` 키 SHA256 = Supabase secret digest 일치로 배포 키 확정. git 히스토리에 키 커밋 이력 0건(저장소 유출 아님). 대화 저장이 답변 성공 후에만 실행되어 장애 기간(8일) 대화 기록 전무. 사용자가 새 키 발급 → 양 모델(generateContent/embedContent) 200 사전 검증 → `supabase secrets set` + 로컬 `.env` 갱신 → E2E 검증(isTest POST 200 + 한글 질문/답변 DB 저장 + sources 확인, 디버그 행 삭제). 같은 secret을 쓰는 ssampin-embed(KB ingest)도 일괄 복구. ⚠️ 교훈: 외부 키 단일 장애점 — 8일간 조용히 다운돼도 감지 수단 없음, 헬스체크/실패 로그 저장은 추후 개선 후보. (2) **대시보드 개선(완료·배포)**: 관리자 Analytics "챗봇 대화 원문" — 답변 300자 잘림 제거 → 질문/답변 말풍선 전문 표시 + 메시지별 시각 + 참조 문서(sources) 칩, 조회 200→1000건, 세션 30개 제한 → "더 보기" +30 페이지네이션. 검증: landing tsc 0 + next build 통과, Vercel 자동 배포.

- 📋 **MultiSurvey v2 라이브 흐름 완결 — 배포 가능 상태 (2026-06-11, main 미커밋)** — Phase C까지 메이커만 연결돼 있던 결정적 갭 3종 해소. (1) **라이브 흐름 연결**: 메이커 "설문(퀴즈) 시작" → `LiveConsoleContainer` 신설이 학생 접속 서버(`live-multi-survey:start` stepMode=true) 기동 + 터널·숏코드(LiveSessionClient) 발급 + `TeacherConsole` 렌더. phase 전이 시 학생 페이지 IPC 동기화(lobby→activate / open→reveal / revealed·round_result→advance·end-session / podium→stop). roster→students, 학생 답변→`liveBridge.buildResponseFromLiveAnswer`(정답 판정·점수는 domain rules)→responses upsert. StrictMode 이중 mount 안전(liveId 단위 기동/정리 단일 effect). (2) **v2 세션 영속화**: `useMultiSurveyV2Store` partialize에 sessions+selectedSessionId 추가 — 앱 재시작 시 작성한 설문 소실 해소. 메이커 헤더에 세션 선택 select + "+ 새 설문" + 삭제(2단계 확인, `--sp-error` 토큰 신규 — index.css 3개 테마 블록, v2 학생 폼들이 이미 참조하던 미정의 변수 잠재 버그 해소). (3) **no-op 버튼 해소**: 저장→"저장됨 ✓" 플래시(자동 영속 확인 피드백), 리포트 버튼 제거(미구현 dead UI), 시작 버튼 `validateSession` 가드(실패 사유 툴팁). (4) **'게임' 표기 → '설문(퀴즈)'**: "게임 시작"→"설문(퀴즈) 시작", 토글 그룹 부제 "게임 메카닉"→"설문(퀴즈) 메카닉" (사용자 지시 2026-06-11). (5) **IPC 확장**: `student-answered` 이벤트에 `answer` 원본 payload 추가(additive — v1 UI 무영향) + preload/global.d.ts 타입. 신규 `src/adapters/multiSurvey/live/liveBridge.ts`(9종 문항→학생 HTML 4종 다운매핑: ox→O/X single-choice, multiple→multi-choice, short/blank/description→text — 정답 정보 학생 페이지 유출 0) + 단위 테스트 12건. 검증 게이트: tsc 0 / lint 0 errors / vitest 163 files 2090 passed(FillFormFields 1건 flaky — 재실행 전체 통과 확인) / regression 34/34 / migration-roundtrip 5단계 ✓ / check-flag-usage 3/3 / check-hex 0건 / check-sp-coverage 1215. electron main 재번들 완료(`node scripts/build-electron.mjs` — dev watch 함정 회피). **남은 이연 항목(후속 PDCA)**: Share view(교실 모니터 별도 창) 미연결, STUDENT_WAVE·TOGGLE_FOCUS_MODE IPC 미등록, 콘솔 타이머 실시간 카운트다운(현재 정적 표시), 빠른풀이/연속정답/랜덤 보너스 점수(calcSessionScore), blank 초성 비교. **다음**: 실기기 C.6 시나리오 수동 검증(교사 시작→학생 QR 입장→답변→정답 공개→포디움) → 커밋. **(추가 UX 결정 2건, 같은 날 사용자 지시)** ① 시작 버튼 비활성 사유를 툴팁→버튼 옆 상시 표시 텍스트로 승격(`startBlockerMessage` — "N번 문항의 내용/정답을 입력해주세요" 교사용 문구, validateSession 폴백). ② **자동 넘김 OFF 시 타이머 전면 비표시**(편집기 스테퍼·문항 칩 ⏱·미리보기 ⏱·콘솔 문항 메타·콘솔 TimerBar 5곳 — 교사 주도 진행에선 무의미). ③ **입장 코드 폐기 — QR+링크만**(LobbyView EntryCodeDisplay 제거+URL 20px 승격, ConsoleHeader 코드 표시 제거, EntryCodeDisplay.tsx 삭제, 숏코드 등록은 짧은 주소 발급용으로 유지). 재검증: tsc 0 / lint 0 errors / regression 34/34 / hex 0 / sp-coverage 1208.

- 🎨 **협업보드 PDCA-1/2 실작동 리팩토링 — "고도화가 잘 안됨" 근본 원인 5건 수술 (2026-06-11, main 미커밋)** — 변경 파일 2개: `src/infrastructure/board/generateBoardHTML.ts`(학생/교사 보드 페이지) + `src/adapters/components/Tools/Board/BoardQRCard.tsx`. (1) **(치명) revert 권한 가드 → 선택 차단 가드**: 이전 가드는 다른 학생의 정상 편집(원격 변경)도 "남의 요소 수정"으로 오판해 강제로 되돌리고 그 되돌림을 다시 전체에 전파 — 2명만 접속해도 서로 되돌리는 충돌 + 안내 토스트 도배로 협업 자체가 불능이었음. 요소를 수정하려면 먼저 선택해야 한다는 점을 이용해, 남의 요소를 선택하는 순간 선택만 해제(로컬 상태라 전파 없음)하는 방식으로 교체. (2) **(치명) 내가 만든 것도 못 고치던 잠김 해소**: 작성자 표시가 스티커에만 붙어 일반 도형·텍스트는 만든 본인조차 수정 불가였음 → 내가 새로 만든 모든 요소에 finalize 시점 작성자 태깅(+version bump 로 동기화 보장, 원격 요소 오태깅 방지 로직 포함). (3) **(UX) 스티커가 진짜 포스트잇으로**: 텍스트 요소 배경색은 Excalidraw 가 그리지 않아 색이 안 보였음 → 작성 완료 시 색 사각형 + 내부 텍스트(⭐작성자 첫 줄)로 자동 변환, 연속 작성 지원. (4) **(미완 → 완료) 도형 12종 버튼 활성화**: 누르면 콘솔 로그만 찍히던 placeholder 를 전부 실제 동작으로 — 도구형 8종(직선·화살표·사각형·둥근사각형·원·마름모·텍스트·양방향 화살표) + 스탬프형 4종(삼각형·오각형·블록 화살표·꺾인 화살표: 화면 중앙 즉시 추가 후 바로 끌어 배치). 누름 상태가 실제 활성 도구와 자동 동기화(ESC 대응). (5) **(UX) 교사가 보드에 들어갈 길 신설**: 교사 화면은 QR/접속자 목록뿐 캔버스 진입 불가였음 → 세션 카드에 **[보드 열기]** 버튼 추가, 기본 브라우저에서 `?role=teacher`로 이름 입력 없이 '선생님' 즉시 입장 + 전체 편집 권한(폐쇄 교실 클라이언트 신뢰 — Plan ADR 정합). **검증 게이트 4/4**: tsc 0 / eslint 변경 파일 0 / vitest 162 files 2078 passed 9 skipped / regression 34/34. 추가로 인라인 학생 페이지 JS 는 tsc 가 못 보므로 esbuild 번들→HTML 생성→`node --check` 구문·마커·잔재 검사 신설(.omc/tmp/check-board-html.mjs) ALL PASS. `node scripts/build-electron.mjs` 재번들 + 신규 심볼 3종 grep 확인(electron dev watch 함정 회피). omc 장부 `.omc/ultragoal/collab-board-rb-parity/goal-ledger.md` G002~G004 complete 갱신(이전 세션 누락 소급). **다음**: 실기기 2-브라우저 수동 시나리오(스티커→타 학생 선택 차단→도형 12종→교사 보드 열기) → 커밋 → G005(PDCA-3 템플릿 4종). **알려진 한계**: 지우개 도구(E)는 선택 없이 삭제 가능(권한 가드 우회) — PDCA-5 권한 단계에서 재검토. **(추가 fix, 같은 날)** 사용자 신고 "도구 창 겹침" — 좌측 고정이던 스티커/도형 패널이 Excalidraw 메뉴·속성 패널(불투명도/레이어)을 가림 → 우측 상단(`top:64px; right:12px`)으로 이동 + 🧰 접기/펼치기 버튼 + max-height 스크롤. **(추가 fix 2, 같은 날)** 사용자 신고 "스티커 클릭해도 안 만들어짐" — "색 선택→text 도구→입력 완료 시 변환" 4단계 모드는 (a) 클릭 즉시 피드백 0 (b) Excalidraw 가 텍스트 편집 중 activeTool 복귀 시 모드 풀림 허점 → **색 클릭 즉시 화면 중앙에 포스트잇 생성**(도형 스탬프와 동일 패턴, 계단식 offset, '⭐작성자\n메모를 입력하세요' 프리필, 생성 직후 선택 + 더블클릭 편집 toast 안내)으로 단순화. activeStickerColor 모드 머신 전부 제거. 인라인 JS 구문검사·lint 0·regression 34/34·build-electron 재번들 확인. ⚠️ 반영하려면 앱 재시작 + 보드 세션 재시작 필요(HTML 은 세션 시작 시 1회 생성).

- ✍️ **서명받기(sigv2) 도구 전면 리팩토링 — 5단계 마법사 UX + 수업반 명렬 + 임포트 열 매핑 + 404 정정 (2026-06-11, main `18f16e3`)** — 사용자 신고 5건 전부 해소. (1) **404 근본 원인 + 링크 도메인 결정**: 링크는 `https://ssampin.com/sign/{code}` 유지(사용자 선호 — 짧고 브랜드 일치), `landing/vercel.json`에 `/sign/:path* → https://m.ssampin.com/sign/:path*` 307 redirect 추가로 해소. 실제 페이지는 모바일 번들(`src/mobile/main.tsx`)이 처리. `.env.example` + `docs/signature-deployment-checklist.md` sigv2 기준 전면 재작성(기존 문서는 폐기된 2C 구조 기준이었음). 추가: 임포트 시 매핑 안 된 기존 열 정리 옵션(가져온 양식 ↔ 열 편집 동기화) + 열 편집 순서 변경(위/아래) 버튼. (2) **단계 UX**: 한 화면 일괄 설정 → `기본 정보 → 명단 구성 → 공개 설정 → 공유·현황 → 등록부 생성` 5단계 마법사(이전/다음 + 클릭 가능한 인디케이터 + 공개 후 ①~③ 잠금). (3) **수업반 명렬**: `useTeachingClassStore` 연동 — 담임반 버튼 옆에 수업반별 버튼(활성 학생만, 소속=학생별 학년-반 폴백 수업반명, 연번=출석번호). (4) **붙여넣기/CSV ↔ 열 편집 양방향 연동**: `parseRosterGrid → suggestImportTargets → 매핑 미리보기 UI(RosterImportMapping) → applyImportMapping` — 칸별 대상 열 선택 + '새 열로 추가' 시 열 편집기에 자동 생성 + 중복 매핑 가드 + 인라인 편집 명단 표(RosterTable). (5) **숨은 버그 3건**: 접근 코드가 서버로 전송되지 않아 accessCodeEnabled 시 publish 400 (ISignaturePort에 accessCode 추가 + 클라이언트 전달), 세션 비영속으로 앱 재시작 시 adminKey 유실(localStorage 영속 + 공개 시점 명단 스냅샷째 보관), 도구 재진입 시 members 빈 배열로 등록부가 빈 시트로 생성되던 문제(세션 스냅샷 기준 내보내기로 정정). 구조: 1,443줄 단일 파일 → `ToolSignatureRoster.tsx`(마법사) + `SignatureRoster/{signatureRosterLogic.ts, RosterImportMapping.tsx, RosterTable.tsx, SessionPanels.tsx}`. 검증 게이트 4/4: tsc 0 / lint 본 작업 범위 0 errors 0 warnings(MemoFocus 1 error는 다른 세션 부채) / vitest 162 files 2076 passed(신규 13건 포함, Notice 메타테스트 amber 가드 위반 1건 수정) / regression 34/34. 다음: 커밋·푸시 → m.ssampin.com 재배포 확인 → 실기기 QR 시나리오(공개→서명→현황→시트/Excel) 수동 검증.

- 📋 **MultiSurvey v2 G004 Phase C C.4 완료 — v1 템플릿 추출 + opt-in 이벤트 로깅 (2026-05-30)** — Phase C C.0/C.1/C.3 직후 잔여 C.4를 1 파일 5 영역 수술적 편집으로 완결. (1) `useV1MultiSurveyData()` 빈 배열 stub → `useToolTemplateStore`의 `toolType==='multi-survey'` 템플릿을 V1Survey shape(id/title/questions/submissions=[]/isOpen=false/createdAt epoch ms)로 변환 — `Date.parse(template.createdAt)` ISO 8601 → epoch ms + `useEffect` 자동 load + `useMemo` 변환 캐시. (2) `useAnalytics().trackRaw` 채널 도입 — AnalyticsEvent enum(보호 파일) 비-확장 원칙으로 4종 신규 이벤트(`multi_survey_v2_opt_in`, `multi_survey_v2_opt_out`, `multi_survey_v2_migration_completed`, `multi_survey_v2_migration_failed`) 게재. Phase D 합격선 95% (Q5 ADR-005 폴백 90% 포함) 측정의 분자 입력. (3) 마이그레이션 useEffect 보강 — `v1Sessions.length===0` 시 ref 미세팅 → 템플릿 비동기 로드 후 재진입 시 자동 재시도 가능. runMigration 결과를 success/failed 이벤트로 로깅. (4) `handleOptIn`/`handleRollbackToV1` 콜백에 클릭 시점 트래킹 추가 — V2OptInBanner의 inline 콜백을 참조로 교체. 검증: tsc 0 errors / lint 0 errors / check-flag-usage 3/3 (추가 호출 없음) / check-hex-hardcoding 0건 / check-sp-coverage 1175건 / migration-roundtrip 29/29 / 보호 파일 10종 GUARD-CLEAN (useAnalytics는 useSettingsStore _읽기만_ — 본 파일은 비-수정). 다음: C.2 정성 게이트 시각 검증(데스크톱 실 화면) → C.6 사용자 테스트 5 시나리오 → C.5 릴리즈 워크플로우 → G004 complete checkpoint.

- 📋 **MultiSurvey v2 G004 Phase C C.0 + C.1 + C.3 진입 완료 (ADR-010 발행, 2026-05-30)** — G003 Phase B(55 신규 파일) 직후 Phase C 진입. (1) **C.0 Phase B 잔여 통합 완료**: `MultiSurveyToolEntry.tsx` 신규 작성 (`useRealtimeToolFlag` 3번째이자 마지막 호출 위치 — check-flag-usage 3/3 PASS), `MakerLayout` 자동 진입 + 빈 세션 1개 자동 생성 + V2OptInBanner(flag OFF 시) + 이전 도구로 복귀 버튼(flag ON 시) + MigrationReportModal `api` prop 주입 + 자동 마이그레이션 트리거 useRef로 1회 보장. `toolRegistry.ts:76` + `App.tsx:353` 두 진입점 `ToolMultiSurvey` → `MultiSurveyToolEntry` 교체. v1 데이터 추출은 `useV1MultiSurveyData()` 빈 배열 stub(TODO C.4-followup으로 분리). (2) **C.1 미감 정량 게이트 자동화 3종**: ADR-010 발행 — sp-\* ratio ±20% 게이트 폐기 + 비-fallback HEX 0건 / sp-\* 바인딩 ≥500 / frontend-design S1/S2 0건 3종으로 대체. `scripts/check-hex-hardcoding.mjs`(화이트리스트 4종: var fallback + qrcode color + [Ff]allback 식별자 + \_studentPageChrome.ts) + `scripts/check-sp-coverage.mjs`(Phase B baseline 582 → 임계 500) 신규. 실측: HEX 0건 PASS / sp-\* 1175건 PASS(임계 2.35배). (3) **C.3 Percy 미도입 ADR 발행**: `.omc/specs/adr-percy-non-adoption.md` 1인 OSS 운영 비용/효과 + Phase D 합격선 미달 시 재오픈 트리거 4종 명시. (4) **Open Questions 갱신**: Q4(ADR-010으로 해소) + Q5(폴백 합격선 A 채택 — 90/99.5/0 + 90일 + 결재) + Q7(권장 5건 전부 채택). 검증: tsc 0 errors / lint 본 작업 범위 0 errors(MemoFocus 1건은 다른 세션 부채) / check-flag-usage 3/3 / check-hex-hardcoding 0건 / check-sp-coverage 1175 / 보호 파일 10종 GUARD-CLEAN. 다음(다른 세션 또는 본 G004 후속): C.2 정성 게이트 시각 검증(데스크톱 실 화면 + frontend-design 협업) → C.4 v1 데이터 추출(useToolTemplateStore 연동) + opt-in 이벤트 로깅 → C.5 릴리즈 워크플로우 8단계 → C.6 사용자 테스트 5 시나리오 → G004 complete checkpoint.

- 🍱 **점심 위치 1급 도메인 승격 + 표 내 인라인 위·아래 버튼** (ADR-009 / C안 + Phase 2 통합, 2026-05-29) — 사용자 피드백 "3교시 후 점심으로 옮기는 단일 액션이 없음"에 대응. Settings에 `lunchAfterPeriod?: number` 추가, PeriodTab 점심 행에 [↑][↓] 버튼 인라인 배치, lazy 마이그레이션, 3단 폴백(`lunchAfterPeriod` → `lunchStart/End` → 30분 갭). 데스크톱만(모바일은 Phase 3 이연 — 시간표 그리드 화면 자체가 없음). 도메인 함수 5개 + presenter 3단 폴백 + PeriodTab UI 갱신 + 학급/교사 시간표 양쪽 동일 lunchIndex 공유 검증 완료. 검증 게이트 4/4 통과: tsc 0 errors / lint 0 errors / vitest 2021 passed (166 files, 10 skipped) / regression 41/41.

## Current Version

**v2.0.9 (2026-05-26 출시 진행 — 핫픽스 릴리즈, v2.0.8 OAuth 사고 정정).** v2.0.8 빌드 사고(`.env` 누락으로 `VITE_GOOGLE_CLIENT_ID` 가 빈 채로 박혀 구글 캘린더·할 일·드라이브·로그인 전체 깨짐 — 사용자 신고 2026-05-26)를 정정하고 빌드 가드 추가로 재발 차단.

**v2.0.9 묶음에 포함된 변경 사항 (1 Critical fix + 1 신기능 + 회귀 fix 5건 묶음 + UX 개선 4건 + 빌드 가드)**:

1. 🔧 **Critical: 구글 캘린더·할 일·드라이브 연결 복구 (v2.0.8 OAuth client_id 빈값 빌드 사고 정정)** — `vite.config.ts` / `vite.mobile.config.ts` 에 production 모드 OAuth 키 빈값 시 즉시 throw 하는 가드 추가. 검증: .env 백업 후 빌드 시도 → "프로덕션 빌드 중단" 에러로 정상 차단 확인. 사용자 PC 의 v2.0.7~v2.0.8 설치본 `app.asar` 에 `990268309712` 0건 박혀있던 사실을 grep 으로 확정.
2. ✨ **담임 메모장 출결 세부 입력 추가** (`5067245`) — 카드 안에서 출결 사유까지 한 번에 기록.
3. 🪟 **바탕화면 위젯 모달 입력창 포커스 안정화 (회귀 5건 일괄)** — `1d79f4c`/`0c12659`/`a752c05`/`0286a73`/`a58b262` — 모달 열 때 위젯 축소·입력 포커스 race·클릭 시 입력 풀림·detach race·fallback 경로 정정.
4. 🛡️ **출결 자동저장 안전망 + UX 폴리시** (`237de6d`) — 입력 도중 끊겨도 마지막 입력 보존 + 저장 상태 시각 표시.
5. 🔁 **출결 워크플로우 단순화** (`f12b32b`) — 자주 쓰는 동선 우선 + 중복 확인 제거 + 키보드 완주 가능.
6. ♿ **학생 기록 뷰 접근성 개선** (`2d7bbbf`) — Tab 키 탐색 + 스크린 리더 호환성.
7. 🪟 **Windows 보호 경고 안내 애니메이션 개선** (`e00e85e`) — 첫 실행 시 SmartScreen 안내 부드럽게.

**검증 게이트 4/4 통과 (2026-05-26)**: tsc 0 errors / lint 0 errors (118 warnings 기존 부채) / vitest 1809 passed (9 skipped) — phase5-ux-cleanup JSX 줄바꿈 회귀 1건 fix(`이 좌석으로 수업 기록하기` string literal 명시) / regression 28/28.

**빌드 가드 동작 검증 통과**: `.env` 백업 후 `npx vite build --mode production` 시도 → `[vite] 프로덕션 빌드 중단: VITE_GOOGLE_CLIENT_ID, VITE_GOOGLE_CLIENT_SECRET 가 비어있습니다.` 에러로 즉시 종료 + .env 자동 복원 확인.

**제외 (다른 묶음 예정 — v2.0.9에서 빠짐)**:

- 학급규칙(ClassroomAgreement) 도구 신규 — 도메인+UI+IPC+테스트 전부 미커밋, 다음 릴리즈로 이연 (사용자 결정 2026-05-26)
- RealtimeWallTabConfig 엔티티 — v2.1.0 멀티탭 묶음 예정
- XlsxExporter 인프라 + 테스트 — 별도 PDCA
- public/landing/, bin/, scripts/gen-card-image.mjs — 빌드/배포 도구류 별도 정리

**다음 단계**: Windows 5단계 분리 빌드 (CLAUDE.md §6) + grep 검증 (`990268309712` 박힘 확인) + macOS GHA + GitHub 릴리즈 + 사용자 PC 재설치.

---

**v2.0.8 (2026-05-23 빌드 사고 — 실제 사용 불가)**: `release-notes.json` v2.0.8 블록 14개는 등록됐고 git/태그/installer 도 발급됐으나, 빌드 시점 `.env` 누락으로 `VITE_GOOGLE_CLIENT_ID` 가 빈 문자열로 박혀 모든 OAuth 가 깨졌음. 이 사고는 v2.0.9 핫픽스 릴리즈로 정정. 사용자에게 노출된 v2.0.8 의 위젯 UX 개선 항목들은 v2.0.9 에도 그대로 포함되므로 v2.0.9 가 첫 사용 가능 버전.

**v2.0.8 묶음에 포함됐던 변경 사항 (위젯 모드 UX 대수술 — v2.0.9에도 함께 포함)**:

1. ✅ **ssampin-widget-inline-ux + widget-expanded-editors** — 위젯 카드 클릭 → 큰 창(모달) 한 개로 통합 + 인라인 CRUD. 할 일·메모·미니 캘린더·다가오는 일정·D-Day + Phase 4 담임 메모장·북마크·즐겨찾기 확장 편집기.
2. ✅ **widget-mode-discovery** — 헤더 모드 인디케이터 칩 + 1회 코치 투어 + 우클릭 컨텍스트 메뉴 모드 섹션 + 설정 → 위젯 탭 모드 섹션 상단 승격 + ModalCoordinator priority 2종 추가(WIDGET_MODE_FALLBACK=2.5, WIDGET_MODE_COACH=5.5).
3. ✅ **native-desktop-left-edge-resize-fix Phase 7-D 2차** — 사용자 신고 "바탕화면 모드 왼쪽 테두리 드래그 시 위젯 사라짐" 결정적 회귀 차단. `moveAndResizeWidgetSync` 신규 헬퍼(SWP_ASYNCWINDOWPOS 제외 sync 변형) + `computeResizeBounds` 순수 함수 추출 + 5 메타테스트. ADR-008.
4. ✅ **위젯 모드 ESC + 모달 버튼 + 모드 전환 + 휠 방향 + 미니 캘린더 split 핫픽스 묶음** — 일반 모드 ESC(window.focus + capture 단계 keydown) + 바탕화면 모드 ESC(globalShortcut IPC 폴백) + WidgetSettingsPanel ESC + 모달 readOnly 가드 부작용 해소 + 모드 전환 bounds 강제 복원 + 좁은 폭 헤더 3열 그리드 + 휠 부호 blink convention SSOT + MiniCalendarExpanded md breakpoint 세로 스택 + desktop-organize 모달 진입 차단.
5. ✅ **widget-wheel-direction-fix** — `computeWheelDeltas(rawDelta, axis)` SSOT 헬퍼 + 메타테스트 16건. ADR-007.
6. ✅ **아이콘 모드 픽셀아트 캐릭터** — PNG 렌더링 연결 + 캐시 무효화.
7. ✅ **student-records rules-of-hooks 위반 해소** — wrapper 분기 패턴.

**검증 게이트 4/4 통과 (2026-05-23)**: tsc 0 errors / lint 0 errors (120 warnings 기존 부채) / vitest 1723 passed (9 skipped) / regression 24/24.

**제외 (다른 묶음 예정)**:

- 협업보드 PDCA-1 Step 1.0~1.4 (코드는 main 잔류, release notes 미노출 — v2.1.0 묶음 또는 별도 fast follow)
- 실시간 담벼락 v2.0 (다른 세션 미커밋 — stash 보호 후 stash@{0}에 보존, 그 세션 마무리 시 자체 release)
- 복합 유형 설문 v2 renewal (Phase A 진입 대기)

---

**v2.0.7 (2026-05-22 출시 완료) 묶음 변경 사항 (4 PDCA + 보안 패치)**:

1. ✅ **자리배치 신규 기능 3종** — 히스토리(저장/복원/비교·이전 자리 피하기) + 이름 학습 모드(전체화면 카드 플립) + 우연을 가장한 배치(사전 설계 → 셔플 위장). 자유 배치 모드 Phase 1~5a(ㄷ자형·시험 대형·모둠 + PDF 출력). 47 신규 테스트.
2. ✅ **roster-sample-data-removal Phase 1+2+3** — Match Rate 99.0% PASS. SAMPLE_STUDENTS 35명 자동 채움 영구 제거 + 6중 안전 가드(A·B·C·D·E·F·G) 마이그레이션 + 9개 화면 EmptyState 가드 + amber 경고 배너 + 사이드바 빨간 점 + 마이그레이션 토스트. Dev 도구 `npm run electron:dev:fresh` 추가.
3. ✅ **notification-modal-stacking-fix Phase 0~4** — Match Rate 97% PASS. 일정 알림 X 안 눌리던 핫픽스 + 공용 Modal 마이그레이션 + ModalCoordinator 우선순위 큐(7단계) + 6개 모달 큐 등록 + REGRESSION 9→17.
4. ✅ **realtime-tool-student-page-health Phase 0+1+2** — Match Rate 98% PASS. 4개 학생 페이지(워드클라우드·주관식·객관식·복합) 우상단 연결 상태 칩 + submit 침묵 실패 차단 + KB Q&A 3건 + REGRESSION 17→22.
5. ✅ **보안 패치** — ws + protobufjs 알려진 취약점 패치(PR #73).

검증 게이트 4/4 통과 (2026-05-22): tsc 0 errors / lint 0 errors / vitest 1566/1566 / regression 24/24.

## Completed (최근)

- 🟢 **v2.0.9 핫픽스 릴리즈 진행 중 (2026-05-26, v2.0.8 OAuth 사고 정정)** — 사용자 신고: 데스크톱 앱에서 "구글 계정 연결" 시 브라우저에 "액세스 차단됨: 승인 오류 / Missing required parameter: client_id / 400 invalid_request" 표시. 진단: v2.0.8 설치본 `app.asar` 에 `990268309712` 0건 박혀있음 — codex 릴리즈 작업 중 `.env` 누락으로 vite `loadEnv` 가 `VITE_GOOGLE_CLIENT_ID` 를 빈 채로 dist 에 박은 결정적 사고. **해결**: (1) `vite.config.ts` / `vite.mobile.config.ts` 에 production 모드 OAuth 키 빈값 시 즉시 throw 가드 (위 §C-1·C-2·C-3 plan) — `.env` 백업 후 빌드 시도 → "프로덕션 빌드 중단" 에러로 정상 차단 확인. (2) `release-notes.json` 에 v2.0.9 블록(highlights 6 + changes 7) 최상단 추가, v2.0.8 블록은 보존. (3) `scripts/ingest-chatbot-qa.mjs` v2.0.9 Q&A 4건 추가 (구글 연결 핫픽스 안내·담임 메모장 출결 세부·위젯 모달 입력 fix·출결 자동저장/접근성 묶음). (4) 6곳 버전 갱신(package.json·landing config/layout·sidebar·mobile settings/more). (5) 검증 게이트 4/4 통과(tsc 0 / lint 0 / vitest 1809 / regression 28 + JSX 줄바꿈 회귀 1건 fix). **포함 커밋**(May 23 18:00 이후 main): `5067245`(메모장 출결 세부) + `1d79f4c`/`0c12659`/`a752c05`/`0286a73`/`a58b262`(바탕화면 위젯 모달 회귀 5건) + `f12b32b`(출결 워크플로우 단순화) + `2d7bbbf`(학생 기록뷰 접근성) + `237de6d`(자동저장 안전망) + `e00e85e`(Windows 보호 경고 안내). **제외**: 학급규칙(ClassroomAgreement) + RealtimeWallTabConfig + XlsxExporter + public/landing — stash 보호 후 v2.0.9 빌드 완료 시 복원. **다음**: Windows 5단계 분리 빌드 + grep 검증 + macOS GHA + GitHub 릴리즈 + 사용자 PC 재설치 + KB ingest + 노션 가이드 갱신. [Plan v0.1](docs/01-plan/features/v209-hotfix-release.plan.md)

- 🟢 **v2.0.8 묶음 릴리즈 완료 (2026-05-23, tag `v2.0.8`, main `b1fe8b5`, 16 commits) — 빌드 사고로 사용 불가** — ⚠️ `.env` 누락으로 OAuth client_id 빈 채 배포되어 모든 구글 연동 깨짐(v2.0.9 핫픽스 참조). 코드 자체는 정상이었음. 위젯 모드 UX 대수술 + 바탕화면 모드 안정화. ssampin-widget-inline-ux(G001~G011) + widget-expanded-editors Phase 1A·1B·2A·2B·4 + widget-mode-discovery + native-desktop Phase 7-D 2차 fix(ADR-008) + 위젯 ESC/모달버튼/모드전환/휠방향/미니캘린더 split 핫픽스 묶음 + widget-wheel-direction-fix(ADR-007) + 아이콘 모드 픽셀아트 + student-records hooks. 검증 게이트 4/4 통과. Windows 빌드 5단계 분리 실행 (EXIT 127 회피 재확인). macOS GHA 7분 13초. 10 URL 302 전부 통과. KB ingest + 노션 가이드 갱신 사용자 직접 실행 대기.

- 🟢 **native-desktop-left-edge-resize-fix Phase 7-D 2차 fix 완료 (2026-05-23, main `0526ed6`)** — 사용자 신고 결정적 회귀 차단. SetWindowPos sync 변형 채택 + `computeResizeBounds` 순수 함수 추출 + 5 메타테스트. ADR-008. v2.0.8 묶음 포함. 사용자 수동 검증 통과.

- 🟢 **widget-wheel-direction-fix PDCA 완료 (2026-05-23, main 미커밋, 사용자 수동 검증 통과)** — 사용자 신고 "바탕화면 위젯 모드에서 마우스 휠 상하 스크롤이 일반 윈도우와 반대 방향" 핫픽스. 부호 정책을 manager inline `-delta` → 순수 helper `computeWheelDeltas(rawDelta, axis)`(SSOT)로 추출 + blink `WebMouseWheelEvent` 컨벤션 채택(Win32 raw 부호 보존). 신규 메타테스트 16건이 회귀 차단. **진단 과정 헛돔 1.5h**: 1차 fix(`+rawDelta`)와 2차 정정(`-rawDelta`)이 사용자 인스턴스에 한 번도 도달하지 못했음을 `dist-electron/main.js` mtime + `computeWheelDeltas` grep으로 확정 — `scripts/electron-dev.mjs`가 `electron/` 폴더 변경을 watch하지 않는 dev 함정 재발(2026-05-21 realtime-tool-student-page-health에도 동일 함정 기록되어 있었음. PROGRESS.md 미독으로 재발견). `node scripts/build-electron.mjs` 명시 빌드 + dev 재시작으로 blink convention 정답 확정. 검증 게이트: tsc 0(본 PDCA 변경분) / lint 0 / vitest focused 78/78 / regression 24/24. ADR-007. **Follow-up**: `electron-dev.mjs`에 `electron/` watch + 자동 rebuild + electron 재실행 (별도 작은 PDCA 권장). [DECISIONS.md ADR-007](DECISIONS.md)

- 🟢 **ssampin-widget-inline-ux + widget-expanded-editors PDCA 완료 (2026-05-22, main 미커밋)** — 위젯 카드 클릭 → 큰 창(모달) 한 개로 통합 + 그 안에서 추가/수정/삭제까지 가능. 두 PDCA 종결.
  - **ssampin-widget-inline-ux (G001~G011)**: WidgetModal(createPortal+ESC+✕+backdrop+autoSave) / ModalCoordinator `onPreempt`(시스템 모달 발생 시 자동 저장) / `useFocusTrap`(직접 구현, focus-trap-react 무사용) / PIN_FEATURE_MAP 추출 / registry 22→21 위젯(`modalSize`/`modalMode`/`inplaceCapable`/`requiresExplicitCancel` 4 필드) / WidgetCard 클릭→모달 / SortableWidget quadrant dwell 호버 핸들 / DashboardHeader 📋·🎨 두 버튼 / WidgetGrid `isEditMode` 분기 완전 제거 / Settings panel `initialTab` prop / Electron Widget.tsx 헤더 동일 두 버튼 + read-only shim(AC17 Phase 2 이연) / 마이크로 인플레이스 편집 + 5초 Undo(Todo/Memo/DDayCounter, `durationMs=5000` 명시) / MemoFocus 위젯 deprecate / AC4·AC11·AC12·AC14·AC20 메타테스트 추가 / AC11 hitbox 8건(today-class·meal·dday-counter·favorite-tools·image-sticker-2/3/4) 일괄 수정 + mini-calendar는 카드 사이즈 제약상 SKIP_AUDIT.
  - **widget-expanded-editors (사용자 UX 신고 해소)**: 카드 클릭 후 두 겹 팝업이 뜨던 문제 해결. Phase 1A 할 일 — `TodoEditor` 추출 + `TodoPopup` 삭제 + 모달에서 추가/수정/삭제. Phase 1B 메모 — `MemoEditor` 추출 + `MemoDetailPopup`은 portal wrapper로 리팩터(MemoPage 호환 유지) + 모달에서 한 줄 추가/그리드/인라인 편집 패널. Phase 2A 미니 캘린더 — `MiniCalendarExpanded` 신규(큰 캘린더 좌 + 일정 패널 우 + QuickEventForm + 5초 Undo). Phase 2B 다가오는 일정 — `DashboardEvents` 확장 뷰에서 `EditableEventRow` + `EventsQuickForm` 인라인 CRUD + showAll portal은 compact 모드에서만.
  - **검증 게이트**: tsc 0 / eslint 0(PDCA 파일 한정) / vitest 1711 pass / 9 skipped / 0 fail / regression 24/24.
  - **AC17 Phase 2 이연**: Electron 데스크톱 위젯 ↔ 메인 앱 양방향 IPC sync는 별도 ralplan(`widget:data:*` 채널 설계).
  - **남은 후속 작업**: G011 sub-agent 보고 중 권고된 `QuickEventForm` DRY-up (MiniCalendarExpanded ↔ DashboardEvents 공유 추출) — 후속 리팩터 PDCA 가능. PROGRESS.md/노션 가이드 사용자 안내 — 사용자 결정.

- ⚠️ **realtimeWall v2.0 — 다른 AI 세션이 작업 중(미커밋)** — `src/domain/entities/RealtimeWall.ts`/`RealtimeWallTabConfig.ts`/`useRealtimeWall*`, `src/domain/services/`, `src/shared/wsProtocol/realtimeWall.ts` + 2 테스트 파일. "멀티탭 보드 + WS 프로토콜 2.0, schemaVersion='2.0', 메타테스트 MT-1/2/3" 별도 Plan 진행 중. `wsProtocol/realtimeWall.test.ts:21-22` 의 `ServerToClientWallMessageSchema`/`ClientToServerWallMessageSchema` unused import 2건 때문에 `npx tsc --noEmit` 전체 실행 시 TS6133 빨간 줄이 잠시 보임 — 그 세션이 마무리하면 자연 해소. 본 세션은 CLAUDE.md "다른 세션 파일 건드리지 말 것" 규칙대로 손대지 않음.

- 🟢 **v2.0.6 묶음 릴리즈 완료 (2026-05-20, tag `v2.0.6`)** — 21커밋 풀세트 통합. multi-date-attendance(#60) + consultation Phase 1+2+3(#59,#61) + update-notification-controls(#57) + modal-scroll-overflow-fix(#58) + Notice 공용(#55) + Phase 2 마이그레이션·amber 가독성(#62). 검증 게이트 4/4 통과. 10 URL 302 (v2.0.6 + latest 각 5종). [Memory](.claude/projects/e--github-ssampin/memory/project_v206_bundled_release.md)
- 🔴 **seating-constraints-and-cluster-fix 핫픽스 (사용자 신고 2026-05-20, main)** — 격자 → 모둠 비연동 모드에서 학생이 "알 수 없음" 표시 + 모든 학생이 미배정에 잔존 + 자리 바꾸기 누르면 GroupShuffleOverlay 무한 로딩되던 회귀 차단. 4-Phase: (1) `sanitizeGroups` 순수 함수, (2) `sanitizeSeating` 가 groups 정합화, (3) `changeLayout`/load 시 빈 모둠 자동 복구, (4) `shuffleGroupSeating` 격자 폴백 + Overlay 0-cell 안전망 + `confirmRandomize` groupCount 보정. 검증 게이트 4/4: tsc 0 / lint 0 / test 1457 / regression 9. [Plan](docs/01-plan/features/seating-constraints-and-cluster-fix.plan.md)
- 🟢 **multi-date-attendance Do 단계 완료 (2026-05-20, feature/multi-date-attendance)** — 사용자 피드백 "여러 날짜 출결 일괄 입력" 대응. 3-Phase 모두 구현 + 4단계 검증 게이트 통과. 신규 `calendarUtils` (8 함수 31 unit) + `MultiDatePicker` (single/range/multi 3-mode, 29 unit) + 18단계 변경 + 4 메타테스트 파일(40 케이스). 1304/1304 tests (baseline +74), 9/9 regression. 다음: `/pdca analyze multi-date-attendance`. [Plan](docs/01-plan/features/multi-date-attendance.plan.md) · [Design](docs/02-design/features/multi-date-attendance.design.md)
- 🔴 modal-scroll-overflow-fix 핫픽스 (사용자 신고, 2026-05-19): 13개 모달 wrapping div 에 `flex-1 min-h-0` 추가 + 회귀 차단 메타테스트. Match Rate 97%, 1156/1156 tests. [Report](docs/04-report/features/modal-scroll-overflow-fix.report.md)
- v2.0.5 릴리스: 설문 실시간 답변 확인 + 학생 페이지 fix + 5도구 난수 + 모바일 UX
- PDCA Report 4건 정착: security-hardening, tool-randomness, roster-data-consistency
- 5개 도구 난수 다양성 강화 (secureRandom + anti-repeat)
- 위젯 우측 사이드 레이아웃 프리셋 (Ctrl+5)
- 모바일 UX 개선 다수 (SW 자동 새로고침, safe-area, 스와이프 제거)
- 하네스 엔지니어링 세팅 (CLAUDE.md 리팩터링, 도메인 규칙 분리, 세션 프로토콜)

## In Progress

- 🔒 **위젯 타일 PIN 잠금 노출 fix (2026-07-01, main 미커밋)** — 사용자 신고(챗봇 로그): "메모 위젯만 잠금 설정하여 위젯 내용이 노출되지 않도록". 근본 원인: 기존 PIN 잠금(설정>보안, `memo`도 잠금 대상)이 **클릭-확장 모달(WidgetModal)에만** DashboardPinGuard를 걸고, 상시 노출되는 **카드 타일 본문(`WidgetCard`의 `<definition.component/>`)은 무방비**였음 — 대시보드·바탕화면 위젯 모드 둘 다 같은 WidgetCard를 써서 타일에 메모가 그대로 보임. **fix**: `WidgetCard`가 `PIN_FEATURE_MAP[id]` 매핑 위젯의 카드 본문도 DashboardPinGuard로 감싸 잠금 시 "잠금됨·클릭하여 열기" 카드 노출(잠금 상태에선 모달 열림 래퍼 자체가 렌더 안 됨). DashboardPinGuard 잠금 카드는 셀 높이 채우도록 h-full 정리. 신규 회귀 테스트 3건(`WidgetCard.pinLock.test.tsx`: 잠금→내용 숨김·해제→표시·미매핑 위젯 무영향). 단일 PIN·자동잠금 기존 인프라 재사용(신규 저장소/설정 0). 잠금은 기능 단위라 `memo` 체크 시 메모 페이지도 함께 잠김(기존 동작 유지). **게이트 4/4**: tsc 0 / lint 0(변경 파일) / vitest 255파일 3206 passed / regression 38/38. 남음: 실기기 시각 확인(앱 재시작→설정>보안 PIN 설정+메모 체크→대시보드/바탕화면 위젯에서 메모 타일 잠금 카드 확인).

- 🟢 **record-evidence-excel-import 완료 (2026-06-24, main 미커밋)** — 쌤핀에 기록을 안 해둔 교사용 **관찰 기록 엑셀 일괄 등록**. 잘 만든 양식 다운로드(명단 사전채움 + 숨김 식별키 + 안내 시트) → 작성 → 업로드로 근거 일괄 등록(학생 개별/반 전체). **유형 분류는 엑셀에 안 받고 업로드 후 '미분류' 탭에서** 인라인 토글로 지정(US-1 재사용). 한 칸 줄바꿈=별도 근거. 오매칭 방지(숨김 studentRef→번호→유일성명, 실패 시 임의배정 없이 오류 보고), 부분 성공+오류 행 리포트. 신규 `EvidenceExcel.ts`(infra, exceljs)·`importEvidenceFromExcel.ts`(usecase 순수+테스트 6)·RecordEvidenceView 툴바. **검증 4/4**: tsc 0 / lint 0 errors / vitest 251파일·3183 passed·0 failed / regression 38/38. **한계**: 재업로드 중복(후속 dedup), 점수는 수동입력이라 자동차단 없음(안내). [ADR-016](DECISIONS.md)
- 🟢 **record-evidence-improvements 4건 완료 (2026-06-24, main 미커밋)** — 근거 자료 사용성 개선: (1) 근거 행 **유형 인라인 토글**(수정 모드 없이 즉시 분류) + 영역 0개도 사라지지 않게 **'미분류' 탭** 보존, (2) **학급 전체 일괄 끌어오기**(현재 출처를 모든 학생에 자동 추가, sourceId 멱등 dedup, `addMany` 1회 저장), (3) **출처 확장** — 과제물(Submission)·교사 저장 파일/학생 제출물(ObservationAttachment)·수행평가(RubricGrading 요소별 수준명+피드백)·성적(GradeAnalysis 성취도 A~E + 교사 서술)을 질적 정보로 import, (4) 초안 행 **근거 준비도 배지**(근거 N건·최근일·'AI 작성 가능'·'검토 필요'). **성적/점수 정책=길 A**: 원점수·배점·석차·환산점 숫자는 근거에 절대 미포함, 단 **성취도 등급(A~E)은 생기부 기재 항목이라 포함**(브릿지 정책 일치). 순수 변환함수 `evidenceImport.ts`+회귀 가드 11건. **검증 게이트 4/4**: tsc 0 / lint 0 errors / vitest 250파일·3177 passed·0 failed / regression 38/38. **한계**: Drive 과제수합 import 는 in-memory 로드분만(후속). 길 B(점수 교사전용칸)·브릿지 sourceType parity 는 후속. [ADR-015](DECISIONS.md)
- 🟢 **record-evidence-subpage Phase 1 완료 (2026-06-24, main 미커밋)** — "생기부 초안 페이지 안 '근거 자료' 서브페이지" 요구 구현. 교사가 학생별로 생기부 작성 근거를 등록/수정/삭제하고 생기부 유형(자율·진로·행특·과목세특·개인세특·동아리·교과학습발달 = RecordArea 7종)별로 분류하며, 흩어진 데이터(담임 누가기록·수업반 관찰기록)를 끌어와(import) 모은다. 신규 `RecordEvidence` 엔티티 + 수직 슬라이스(`IRecordEvidenceRepository` / `JsonRecordEvidenceRepository`[record-evidence.json] / `useRecordEvidenceStore` / DI `recordEvidenceRepository`) + `RecordDraftView` 초안↔근거 자료 모드 토글 + 신규 `RecordEvidenceView`(담임·수업반 공용 master-detail, headless). 기존 `RecordDraft.basisObservationIds`(AI 인용 provenance)와 역할 분리 — 라벨로 구분. 끌어오기 연결 검증: 담임=StudentRecord.studentId=Student.id / 수업반=ObservationRecord.studentId=studentKey(ClassRecordInputView.tsx:661). **검증 게이트 4/4**: tsc 0 / lint 0 / vitest 249파일·3164 passed·0 failed / regression 38/38. 신규 테스트 7/7(`useRecordEvidenceStore.test.ts`). **Phase 2 완료(2026-06-24)**: 별도 저장소 `ssampin-ai-bridge`에 읽기 전용 도구 `get_record_evidence`(core `recordEvidence`+`io.readRecordEvidence` / mcp `getRecordEvidence`+server 등록, area 필터·allowRecordWrite 게이트·deidentify·audit) 추가 + `write_record_draft` 안내문 보강 + esbuild 번들 `electron/ai-bridge/index.mjs` 재생성 → AI가 근거 자료 기반으로 영역별 초안 작성. 쓰기 계약 SSOT 불변. **검증**: 브릿지 build OK + 테스트 552 통과(core 290/cli 47/mcp 215, 신규 PII-0 동급생 실명 마스킹 포함) + 번들 node --check OK + ssampin regression 38/38(본체 무영향). [ADR-014](DECISIONS.md) · [Plan](docs/01-plan/features/record-evidence-subpage.plan.md) · [Design](docs/02-design/features/record-evidence-subpage.design.md)
- 🟢 **realtime-tool-student-page-health PDCA 완료 + 수동 검증 통과 (2026-05-21, main 미커밋)** — 사용자 신고 "워드클라우드·주관식 설문 학생이 응답해도 0명·응답 미수신" 진단 결과 처방. 5단계 흐름 진단으로 (a) v2.0.4 이하 학생 페이지 누적 상태 노출 + (b) WS 미연결 침묵 실패 식별. Phase 0: KB Q&A 3건 추가 (5단계 진단 / 연결 끊김 표시 / 학교 Wi-Fi 화이트리스트 요청 템플릿). Phase 1: 신규 공용 모듈 `electron/ipc/_studentPageChrome.ts` (145줄, 3 export) + 4개 학생 페이지(워드클라우드·주관식·객관식·복합)에 우상단 연결 상태 칩(4상태 색+텍스트, role=status aria-live=polite, 펄스 애니메이션, safe-area-inset 가드) + submit silent no-op 차단. Phase 2: regression-grep-check 신규 5건 (#18~#22). 검증 게이트 4/4: tsc 0 / lint 0 / test 1510 / regression 22/22 (17→22). gap-detector 98% PASS. **사용자 수동 검증 완료 (2026-05-21)**: 4개 도구 학생 페이지 6 시나리오 (정상/제출/끊김/재연결/dim/SE 위치) + 챗봇 KB Q&A 모두 정상 동작 확인. **빌드 노트**: `npm run electron:dev` 는 main process(electron/\*) 코드를 watch 하지 않음 — 학생 페이지 HTML 변경 시 `node scripts/build-electron.mjs` 재실행 + electron 재시작 필수. **다음: git commit + v2.0.7 묶음 릴리즈 (notification-modal-stacking-fix + roster-sample-data-removal 동반)**. [Plan v1.1](docs/01-plan/features/realtime-tool-student-page-health.plan.md) · [Design v1.0](docs/02-design/features/realtime-tool-student-page-health.design.md) · [Analysis](docs/03-analysis/realtime-tool-student-page-health.analysis.md) · [Report](docs/04-report/features/realtime-tool-student-page-health.report.md)
- 🟢 **notification-modal-stacking-fix PDCA 완료 — Match Rate 97% PASS (2026-05-21, main 5 commits)** — 사용자 신고 "처음 일정 알림이 떠 있을 때 X 안 눌리고 창을 껐다 켜야 누름" 핵심 해소. Phase 0(`4136527`) 핫픽스 + Phase 1(`396b5b4`) EventPopup Modal 베이스 마이그레이션 + Phase 2(`3a9b3a9`) ModalCoordinator 우선순위 큐 인프라 신설 + Phase 3(`50f6c6b`) 6개 모달 큐 등록 + SharePromptOverlay Modal 통합 + Phase 4(`003eb1a`) 메타테스트 18건 + REGRESSION 9→17. 검증 게이트 4/4: tsc 0 / lint 0 / test 1503 (+46) / regression 17/17. **다음: v2.0.7 릴리즈 (CLAUDE.md 8단계 워크플로우)**. [Plan v1.1](docs/01-plan/features/notification-modal-stacking-fix.plan.md) · [Design v1.1](docs/02-design/features/notification-modal-stacking-fix.design.md) · [Report](docs/04-report/features/notification-modal-stacking-fix.report.md)
- **multi-date-attendance Check 완료 (2026-05-20, Match Rate 96.7% PASS)** — `feature/multi-date-attendance` 브랜치 6 commits + 분석 보고서 1건. gap-detector 결과 모든 HIGH 항목 100%, LOW 갭 3건 cosmetic. Iterate 불필요. 다음: `/pdca report multi-date-attendance`. [Analysis](docs/03-analysis/multi-date-attendance.analysis.md)
- **freestyle-seating Phase 1~5a + UX 종합 개선 14건 + PDF 출력 완료 (2026-05-20, main 단일 워킹트리)** — Playwright MCP 실사용 점검으로 결함 발견·즉시 수정 누적:
  1. 컨테이너 4:3→16:10 + max-height (viewport 잘림 해소)
  2. 다이얼로그 제목 중복 제거 (srOnlyTitle)
  3. 모둠 카드 groupId 색상 외곽선+배경
  4. ㄷ자형 좌·우 반경 안쪽 조정
  5. 자유 모드에서 「연동」 버튼 숨김
  6. 편집 안내 메시지 freestyle 전용 (다중 선택 설명 포함)
  7. 회전된 책상 가로/세로 swap (좌·우 이름 잘림 해소)
  8. 모둠 내부 cardInnerDx/Dy 최소값 보장 (아바타 겹침 차단)
  9. Figma 스타일 **다중 선택·이동** (선택 박스 드래그 + Shift+클릭 + ESC + `moveMultipleFreestyleDesks` 액션 + 「N개 선택됨」 안내 칩)
  10. 다이얼로그 카드 3종→2종 축소 (모둠형 제거)
  11. **「시험 대형」 신규 type** (rows 대체) + 학번 순 정렬 + 좌↔우 방향 선택 토글
  12. 책상에 **학번 표시** (격자 모드 SeatCard 와 동일 시각 규칙: 학번+출석 dot+이름)
  13. 컨테이너 내부 작은 교탁 제거 (외부 「[ 교 탁 ]」 헤더와 중복 해소)
  14. 시험 대형 **column-major 배치** (1번 1열 1행, 2번 1열 2행, ... 한 열 차면 다음 열로)
  15. **PDF 출력 완전 구현** — `exportFreestyleSeatingToPdf` 신설, 정규화 좌표→A4 매핑, 회전 텍스트, 모둠 색상, 우측 명렬표. 내보내기 메뉴 PDF/Excel/HWPX 3종 노출.

  **검증 게이트 4/4 통과**: tsc 0 errors, lint 0 errors, test 1457/1457, regression 9/9. Playwright 다운로드 검증: `%PDF-1.7` 매직 / 2.23MB / application/pdf. Phase 5b(제약조건 마이그레이션) + Phase 6(Tier 2/3 프리셋)은 별도 PDCA. [Plan](docs/01-plan/features/freestyle-seating.plan.md) · [Design v0.2.1](docs/02-design/features/freestyle-seating.design.md)

## Blocked

- dlekthf0109@naver.com 회신 — 사용자 행동 필요 (Claude 발송 불가)
- ~~v2.0.6 핫픽스 묶음 릴리즈~~ ✅ 2026-05-20 출시 완료

## Next

- 🆕 **collab-board-rb-parity** (Spike 완료, PDCA-1 진입 대기) — 외부 참고 도구 RB(Reference Board)의 협업 캔버스 수준으로 협업보드 고도화. **Stage 1+2+Spike 완료**: deep-interview 8라운드(ambiguity 100→24%) + omc-plan consensus 3 iter (Architect 9/10·Critic 9/10) + **PDCA-0.5 Risk-First Spike** (2026-05-22, 3 worker team, production code merge 0, 18분 wall-clock). **Spike 결과**: SP-1 PASS(customData R2 fallback retire) / SP-2 PASS(81 element 단일 updateScene + teacher binding caveat) / SP-3 CONDITIONAL(33→50ms 보수화). AC-1.5/3.1/6.1/6.5 cascade revision 적용. 다음: PDCA-1 진입 사용자 승인 → `/pdca design collab-board-rb-parity` 또는 스티커 메모 + toolbar scaffold 직접 구현. [Plan v1.0+Spike](docs/01-plan/features/collab-board-rb-parity.plan.md) · [Spec](.omc/specs/deep-interview-collab-board.md) · [Spike Synthesis](.omc/spikes/synthesis-report.md)
- \_workspace/plan.md: 서식관리 Phase 1 MVP
- interactive-slides 기능 (docs/01-plan/features/interactive-slides.plan.md)
- PDCA 문서 구조 활용한 체계적 기능 개발
