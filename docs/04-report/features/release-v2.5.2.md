# 쌤핀 v2.5.2 릴리즈 체크표

- 작성일: 2026-09-13 KST
- 대상 저장소: `pblsketch/ssampin`
- 현재 상태: **출시 진행** — 2026-09-13 22:54 KST 오너가 아래 실기기 확인 완료를 명시. 출시 커밋·원격 병합·최종 검증부터 순서대로 수행.
- 현재 로컬 HEAD: `615f82ee` (`main`, `origin/main`보다 11커밋 앞·1커밋 뒤)
- 원격 단독 커밋: `e241c517` — 로컬 `78929777`·`54fcb46d`와 제품 코드가 같고, 진행 문서·가이드·챗봇 원고 6파일만 이력이 다름. 출시 커밋 전에 일반 merge로 합치고 충돌 문서를 현재 내용 기준으로 검토해야 함.

## 출시 범위

1. 생활기록부 근거 정리의 가지형 지도와 장면·근거 연결 편집
2. 미분류·자리 미정 복구, 최근 50회 실행 취소·다시 실행
3. 기본·직접 만든 뼈대와 주제별 장면 추가
4. 여러 학생의 AI 근거 지도 제안, 비교·검토 후 적용, 중단·복원·되돌리기
5. 장면 차례를 지키는 초안, 쉼표 정규화, Terra·Luna 상세 서술 기본값
6. 분량 목표·직접 한도·자동 1회 줄이기·직전 결과에서 이어 조절
7. NEIS 전과목 성적의 학생별 여러 행·병합/반복 머리글 인식과 확인 후 파일 단위 교체
8. 구독 AI 전역 실행 잠금, 저장·동기화 경합 보호, 쌤핀 AI 평문 표시
9. 상담 예약 시간대의 서버 쓰기 권한 보호(운영 적용 완료)
10. Codex·Claude·GJC 공용 개발/릴리즈 절차와 Git 보호 하네스

## 승인 경계

공용 릴리즈 절차에 따라 아래 실기기 항목을 오너가 직접 확인했다.

- [x] Windows Electron의 실제 수업 관리 화면에서 근거 지도 진입·장면/근거 연결·미분류 복귀·Ctrl+Z/Ctrl+X 확인
- [x] 직접 고른 뼈대와 AI가 주제별로 고른 뼈대로 여러 학생 제안을 끝까지 생성하고, 현재 지도 비교→검토→적용→되돌리기 확인
- [x] 담임 업무 실제 화면에서 근거 지도·여러 학생 제안 진입 확인
- [x] 실제 제보 NEIS 파일을 Windows 파일 선택창으로 열어 미리보기·취소·반영·재실행 후 유지 확인

이미 확보한 별도 증거: 격리 Electron에서 실제 Codex 1명 제안, 검토→적용→되돌리기, N명 생성 중단→재진입 복원→이어 하기, 현재 지도/근거 원문·출처·메모 표시를 확인했다. 이는 에이전트 QA이며 위 오너 확인을 대신하지 않는다.

## 단계별 상태

| 단계                     | 상태                       | 근거와 남은 일                                                                                                                                                                                                                                                                                                                |
| ------------------------ | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. 버전·범위             | 통과                       | `npm run check:release-prep` 6/6. package/package-lock 2곳·모바일·랜딩·릴리즈 노트·사이드바가 모두 2.5.2. 보류 기능과 서버 의존성은 아래에서 별도 확인.                                                                                                                                                                       |
| 2. 릴리즈 노트           | 준비                       | `public/release-notes.json` 첫 항목을 2026-09-13 최종 기능으로 갱신. JSON 파싱 통과. 앱 업데이트 카드·설정 화면 실렌더는 최종 커밋에서 재확인.                                                                                                                                                                                |
| 3. 챗봇 지식             | 원고 준비·ingest 대기      | 가지형 지도, 여러 학생 제안, 실행 취소/미분류 복구, 다중 행 성적 문답을 `scripts/ingest-chatbot-qa.mjs`에 반영. 성적 익명 양식 지원 함수는 운영 적용 및 실제 질문 3건 HTTP 200 확인. 승인 뒤 KB ingest 후 새/옛 질문 충돌을 실제 API로 확인.                                                                                  |
| 4. 공개 사용자 가이드    | 통과                       | `/docs`의 근거 지도·여러 학생 제안·성적 가져오기·최근 변경을 최종 동작으로 갱신. `docs:check` 46문서·8링크·17이미지, 랜딩 build 65페이지 통과. 실제 Chromium에서 `/docs/features/own-ai`와 `/docs/releases`의 2026-09-13 갱신일·2.5.2 항목·가지형 지도 설명을 확인하고 스크린샷을 `output/playwright/v2.5.2-release/`에 남김. |
| 5. 코드 검증·출시 커밋   | 로컬 게이트 통과·커밋 대기 | `npx tsc --noEmit` 0, `npm run lint` 0오류/136경고, `npm run test` 790파일·10,796통과/10제외, `npm run regression-check` 117/117, `npm run test:harness` 20/20, `npm run build` 및 postbuild 3종 통과, `git diff --check` 통과. 문서/KB 수정·원격 merge 뒤 전체 게이트를 다시 실행.                                           |
| 6. Windows 빌드          | 대기                       | 오너 확인·푸시 뒤 정확한 릴리즈 SHA로 `Build Windows` 실행. exe·blockmap·latest.yml의 버전·크기·sha512 대조.                                                                                                                                                                                                                  |
| 7. macOS 빌드            | 대기                       | 같은 SHA로 `Build macOS` 실행. arm64/x64 dmg·blockmap·latest-mac.yml, 서명 폴더·각 아키텍처 검사 결과 확인. 공증 통과로 과장하지 않음.                                                                                                                                                                                        |
| 8. GitHub 공개·사후 확인 | 대기                       | `v2.5.2` 태그/Release와 Windows 3개+macOS 5개, 총 8개 자산 확인. 버전별·latest URL의 최종 파일 다운로드와 해시 대조, 자동 업데이트·앱 버전·랜딩/모바일/챗봇 실제 응답 확인.                                                                                                                                                   |

## 서버·운영 범위

- 생활기록부 1층 규정 `v3-fixonly`는 2026-09-12 운영 적용 완료(`sha be78d03e`, 판본 3 유지).
- 상담 예약 시간대 권한 보호는 운영 적용 완료이며 앱 다운로드와 무관하게 동작한다.
- 성적 인식 실패의 익명 양식 지원용 챗봇 함수는 운영 적용 완료, 실제 질문 3건 확인 기록이 있다.
- 여러 학생 지도 제안은 데스크톱의 사용자 구독 CLI를 사용하며 새 Edge Function·DB 마이그레이션을 요구하지 않는다.
- 미적용 상태인 자기평가 070 등 이번 범위 밖 마이그레이션에 `db push`를 실행하지 않는다.

## 커밋 대상과 제외 대상

- 포함: 제품 코드·테스트·설계/ADR/진행 기록·공용 하네스·릴리즈 노트·공개 가이드·필요한 `.omo/evidence` 증거.
- 제외: `.qa-data-record-map*` 약 928MB 격리 Electron 사용자 데이터, `video/`의 별도 영상 스킬 작업, 빌드 산출물과 로컬 환경 파일.
- 파일은 경로를 명시해 stage한다. `git add -A`, 전체 stash/reset/clean은 사용하지 않는다.
