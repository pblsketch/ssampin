# v2.4.5 릴리즈 노트 초안 — 상담·설문 접근 권한 차단 (오너 GO 대기)

> ## ✅ 2026-08-25 — 릴리즈 노트를 `public/release-notes.json` 에 **반영 완료**
>
> 아래 영역별 초안들을 **하나로 합쳐 `versions[0]` 에 넣었다**(highlights 5 · changes 18).
> 이 문서는 이제 "무엇을 쓸까"가 아니라 **"쓴 것을 왜 그렇게 썼나 + 무엇이 남았나"** 의 기록이다.
>
> **날짜는 `2026-08-25` 로 넣어 두었다 — 실제 릴리즈일이 다르면 고칠 것.**
> 버전 번호 6곳(SOP 1단계)은 **아직 안 올렸다.** 릴리즈 노트만 먼저 쓴 상태다.
>
> ### 초안에 없다가 이번에 새로 편입한 것 (v2.4.4 이후 커밋 47건 전수 대조)
>
> 이 문서에는 상담·설문 / 쌤핀 AI / 원클릭업무포털 세 갈래만 있었는데, 실제로는
> **그 밖에도 사용자에게 보이는 변경이 여덟 갈래** 더 쌓여 있었다. 전부 노트에 넣었다.
>
> | 영역                 | 커밋                  | 노트 제목                                                   |
> | -------------------- | --------------------- | ----------------------------------------------------------- |
> | macOS 설치 차단      | `7c206a30` 외         | 맥에서 설치가 막히던 문제를 해결했어요                      |
> | 동기화 — 삭제 표식   | `87fe7943`            | 다른 기기에서 지운 표시가 방금 다시 넣은 자료를 지우던 문제 |
> | 동기화 — 전수점검    | `0872d6f4` `c9231d19` | 새 기기에서 학생 사진이 안 내려오던 문제 등                 |
> | 모바일 입력 소실     | `07bcdb32`            | 휴대폰에서 적던 내용이 동기화 때문에 사라지던 문제          |
> | 모바일 정렬          | `87ad2860`            | 컴퓨터에서 정한 순서가 휴대폰에 반영되지 않던 문제          |
> | 관찰 슬롯            | `65dc52df` `ee83b92b` | 관찰 기록에 "어떤 장면인가"를 탭 한 번으로                  |
> | 생기부 금지항목·잠금 | `54a5aa88` `3f42cba0` | 적으면 안 되는 내용 경고 / 확정 초안 덮어쓰기 차단          |
> | 모둠 균등 배분       | `343e381c`            | 모둠 편성 인원이 고르게 나뉘어요                            |
> | 쿨메신저 쪽지함      | `fcf0ab2a`            | 쪽지함을 읽지 못하던 문제 (실험실)                          |
> | 설치 파일 감량       | `c20dd569` `98f01a31` | 설치 파일이 가벼워졌어요                                    |
> | 쌤핀 AI 하루 한도    | `3f9f7f6f`            | 하루에 쓸 수 있는 양을 늘렸어요                             |
>
> ### 일부러 **넣지 않은** 것
>
> - **관리자 대시보드 속도·지표**(`b1551101` `95fa6cf3`) — 운영자용 화면이다. 선생님 눈에
>   보이는 변화가 없으므로 릴리즈 노트 항목이 아니다.
> - **홈페이지 챗봇 임베딩 이전**(`9a72c618` `56ae176c`) — 서버 쪽 배포라 **이미 적용돼
>   동작 중**이다. 앱 릴리즈 노트에 넣으면 "이번에 고쳐졌다"로 읽혀 시점이 어긋난다.
> - **문서 수정 커밋**(`c42c652b` `8a62199c` `342f5fa6` 등) — 코드 변경이 아니다.
> - **universal(통합) DMG** — `d32dbe98` 로 되돌렸다. 칩별 2파일이 그대로다.
>   ⛔ 노트·안내 어디에도 "통합 파일" 을 쓰지 말 것.
>
> ### 문체 게이트 (`docs/release-notes-assets/RELEASE-NOTES-WRITING-STYLE.md`)
>
> 작성 후 대조해 두 건을 고쳤다. 다음에 항목을 추가할 때도 같은 검사를 할 것.
>
> - ✨ 는 **장식 이모지라 금지**(§4)였다 → 🤖 로 교체
> - 실험실 4항목에 슬롯 3(UI 경로)이 없었다 → `[설정 > 실험실 기능 > …]` 추가
> - 길이: highlights 32~50자, description 최장 448자 (상한 500 이내)
>
> ### 검증 (2026-08-25)
>
> - `npx vitest run src/usecases/releaseNotes src/adapters/components/common` → **통과**
> - `npm run regression-check` → **51/51 통과**
> - JSON 파싱·`ChangeType` 4종(`new|fix|improve|change`) 일치 확인, diff `+105 -0`(추가만)
>
> ### 남은 SOP 단계 (이번 작업 범위 밖)
>
> 1. 버전 번호 6곳 + `package-lock.json` 2줄
> 2. 챗봇 KB ingest — **⚠️ `GOOGLE_API_KEY` 만료 상태라 지금은 실패한다**(오너 조치 대기)
> 3. `/docs` 가이드 — `landing/src/content/docs.ts` 의 **"지원 기준 버전 v2.4.4"**(2442줄)와
>    v2.4.5 기능 목록·`lastUpdated` 갱신 필요
>    5~8. 커밋 → Windows/macOS 빌드 → GitHub 릴리즈

> 버전 번호는 잠정이다. 확정되면 파일명과 아래 `version` 을 함께 바꿀 것.
> 범위: 상담·설문 접근 권한 조이기 (커밋 `4e5aac90`). 같은 릴리즈에 담기는 생기부·쌤핀 AI
> 항목은 그쪽 작업자가 따로 채운다.

---

## 🔴 이 초안을 쓰기 전에 — 지금이 어느 단계인가

이 작업은 **DB 마이그레이션 4개를 순서대로 적용**하는 중이다. 릴리즈 노트 문구가
**적용 단계에 따라 달라진다.**

| 마이그레이션                   | 상태 (2026-08-25 기준)              |
| ------------------------------ | ----------------------------------- |
| 057 숏링크 RPC 3종             | ✅ 운영 적용                        |
| 058 숏링크 표 읽기 차단        | ✅ 운영 적용 — 첫 구멍 닫힘         |
| 059 예약 취소 RPC              | ✅ 운영 적용                        |
| **060 예약·응답 표 읽기 차단** | ⏸ **미적용 — 이 릴리즈가 선행조건** |

**이 릴리즈의 목적이 060 의 선행조건을 만드는 것이다.** 새 앱이 퍼져야 060 을 올릴 수 있다.
상세 순서·확인·롤백은 저장소 밖 비공개 런북에 있다.

### ⛔ 060 적용 전에는 쓰면 안 되는 표현

아직 열려 있는 문을 가리키는 문장이 된다.

- ❌ "권한 없는 요청은 다른 사람의 자료에 닿지 못합니다" — **지금은 사실이 아니다**
- ❌ "완전히 차단", "모두 봉쇄"
- ❌ 예약자 이름·연락처·설문 응답이 노출됐다는 서술
- ❌ 표 이름, 행 수, 요청 방법 등 재현 가능한 정보

### ✅ 060 적용 후에 바꿔 넣을 문장

060 이 올라가고 확인이 끝나면 다음 릴리즈에서 이렇게 쓸 수 있다.

> 권한 없는 요청은 다른 사람의 자료에 닿지 못하도록 완전히 차단했어요.

같은 시점에 **개인정보 방침 제14조 "접근 통제" 문구도 되돌린다.** 2026-08-14 에 사실이
아니어서 낮춰 적어 둔 상태다. 같은 문장이 `docs/edzip/학운위-심의자료.md` ·
`docs/edzip/에듀집-등록-초안.md` 에도 있으니 함께 볼 것.

---

## public/release-notes.json 에 넣을 항목 (그대로 붙여넣기)

`versions[0].changes` 에 추가한다.

```json
{
  "type": "fix",
  "title": "상담 예약을 취소하면 그 시간대가 확실히 다시 열려요",
  "description": "예약 삭제와 시간대 복구를 한 번에 처리하도록 바로잡았습니다.\n\n· 중간에 연결이 끊겨도 \"예약만 사라지고 시간대는 막힌 채\"로 남지 않아요\n· 아무도 잡을 수 없던 유령 시간대가 더 생기지 않습니다\n· 이미 취소된 예약을 다시 누르면 사유가 또렷하게 떠요\n\n상담 주간에 취소된 자리를 다시 받을 때 헛돌지 않아요."
},
{
  "type": "fix",
  "title": "학생·학부모 자료에 닿는 서버 통로를 한 단계 더 조였어요",
  "description": "필요한 만큼만 답하도록 조회 방식을 바꾸고, 공유 링크 주소가 목록으로 훑어지는 길을 봉쇄했습니다.\n\n· 조회는 \"누구의 것인지\" 확인한 뒤 그만큼만 돌려줍니다\n· 기존에 보내신 링크와 인쇄하신 QR은 그대로 쓰실 수 있어요\n· 예약·응답을 받는 학생·학부모 화면은 달라지는 것이 없습니다\n\n선생님이 따로 하실 일은 없어요."
}
```

`highlights` 에 한 줄 넣는다면:

```json
"🔒 학생·학부모 자료에 닿는 서버 통로를 한 단계 더 조였어요"
```

**"기존 링크와 QR은 그대로"** 는 빼지 말 것. 보안 문구를 보면 선생님들이 가장 먼저 하는
걱정이 "내가 뿌린 링크 다시 만들어야 하나"다. 미리 답해 두면 문의가 줄어든다.

---

## 쌤핀 AI — 이 릴리즈에 담기는 것 (쌤핀 AI 작업자 작성)

범위: 커밋 `fb636fd5` `3ffdd791` `ae05079c` `8495f4c6` `a626230b` `5c23a6a2` `60118242`
`535f77e1` `e05e958c` `ce763653` `6502e3e7` `2caeda04` (전부 미출시).

**★v2.4.4 와 무엇이 다른지 헷갈리기 쉽다.** v2.4.4 에서 이미 나간 것은 *조회 + 할 일·일정·
메모·진도·즐겨찾기·노트 쓰기*다. 이번에 **새로 열리는 것은 학생에게 닿는 3종(출결·관찰·
채점)** 이고, 나머지는 그 3종이 실제로 동작하게 만드는 수정이다. 릴리즈 노트에 "할 일 등록이
새로 됩니다"라고 쓰면 안 된다 — 이미 되던 것이다.

### `versions[0].changes` 에 추가

```json
{
  "type": "feat",
  "title": "쌤핀 AI가 출결·관찰 기록·루브릭 채점까지 적어 줘요",
  "description": "말로 부탁하면 무엇을 저장할지 카드로 먼저 보여 주고, [실행]을 누를 때만 저장됩니다.\n\n· \"내일 2교시 3번 결석 처리해줘\" — 담임 학급은 교시를 말하지 않으면 조회부터 종례까지 하루 전체로 적어요\n· \"1학년 7반 7번 관찰 기록 남겨줘\" — 선생님이 말한 문장을 그대로 옮깁니다. AI가 지어내지 않아요\n· \"만점으로 표시해줘\" — 평가 요소 전부를 가장 높은 수준으로 체크합니다\n\n학생 이름은 여전히 밖으로 나가지 않아요. AI에게는 임시 표시만 가고, 실제 이름을 찾는 일은 선생님 컴퓨터에서 합니다."
},{
  "type": "feat",
  "title": "공문에 할 일이 여러 개면 건마다 나눠서 담아요",
  "description": "제출·배부·회의가 한 글에 섞여 있어도 각각 따로 만들어 줍니다.\n\n· 카드 한 장에 무엇무엇이 저장되는지 전부 보이고, [실행] 한 번으로 다 저장돼요\n· 건마다 마감일과 시각이 따로 붙습니다\n\n메신저로 받은 안내문을 그대로 붙여 넣고 \"할 일에 추가해줘\"라고 하시면 됩니다."
},{
  "type": "fix",
  "title": "쌤핀 AI에 공문을 보낼 때 날짜가 지워지던 문제를 고쳤어요",
  "description": "제출 기한이나 회의 일시가 생년월일로 잘못 인식돼 가려지던 것을 바로잡았습니다.\n\n· 이제 날짜가 그대로 전달돼 AI가 기한을 제대로 읽어요\n· 연락처·주민등록번호를 막는 것은 그대로입니다\n\n안내문을 붙여 넣고 물어보실 때 답이 훨씬 정확해집니다."
}
```

### `highlights` 에 넣는다면

```json
"✨ 쌤핀 AI가 출결·관찰 기록·루브릭 채점까지 적어 줘요 (확인 버튼을 누를 때만 저장)"
```

**"[실행]을 누를 때만 저장"** 은 빼지 말 것. 학생 데이터에 AI가 손댄다는 말은 그 자체로
불안을 준다. 저장 권한이 선생님에게 있다는 사실을 제목·본문 양쪽에 남긴다.

**생기부를 언급하지 말 것.** 생기부 초안 생성은 하지 않는다는 것이 이 기능의 선이고
(ADR-072), 릴리즈 노트에서 굳이 꺼내면 "곧 될 것 같다"로 읽힌다.

## 릴리즈 전 실기기 확인 (필수)

- [ ] **상담 예약 취소를 실제로 해 볼 것.** 예약이 목록에서 사라지고, **그 시간대가 다시
      "예약 가능"으로 돌아오는지** 확인. ← 삭제와 복구가 한 트랜잭션으로 묶였는지 보는 지점
- [ ] 이미 취소된 예약을 한 번 더 취소 → "이미 취소되었거나 찾을 수 없는 예약입니다" 안내
- [ ] 커스텀 이름으로 숏링크 만들기 → 중복 이름이면 "이미 사용 중인 링크입니다"
- [ ] 기존 상담 숏링크·QR 을 눌러 예약 페이지가 정상으로 열리는지

### 쌤핀 AI (오너 확인 완료 — 2026-08-25)

- [x] "내일 2교시 3번 결석 처리해줘" → 미리보기 카드 + [실행] 로 저장
- [x] 하루 결석에 **조회·종례까지** 함께 찍히는지 (출결부 화면에서 눈으로)
- [x] "1학년 7반" 으로 말해도 수업반 "1-7" 을 찾는지
- [x] 번호가 겹치는 수업반에서 번호만 말하면 이름을 되묻는지
- [x] 공문 붙여넣기 → 「나갈 문장」에 **날짜가 그대로** 남는지
- [x] 공문의 할 일 3건이 "할 일 추가 — 3건" 한 카드로 뜨는지
- [ ] ⚠️ **관찰 기록은 교과 수업반 전용**이다. 담임 학급에 남기려 하면 거절한다 —
      의도된 동작이지만 문의가 올 수 있으니 알고 있을 것

---

## 원클릭업무포털 — 업무 바로 열기 (2026-08-25 추가)

저작자(청완초 온영범 선생님)께서 우리가 보낸 PR 2건을 **v0.1.15 에 반영**하셨다
(2026-08-25 배포). 그래서 쌤핀이 "프로그램 띄우기"에서 **"그 업무 화면까지 열기"**로 올라간다.

### `versions[0].changes` 에 추가한다

```json
{
  "type": "improve",
  "title": "원클릭업무포털에서 복무·출장·기안을 바로 열 수 있어요",
  "description": "쌤도구의 원클릭업무포털 카드를 누르면 어떤 업무를 열지 고르실 수 있습니다.\n\n· 나이스 · 복무 · 출장 · 에듀파인 · 기안 · 품의 여섯 가지\n· 프로그램이 이미 켜져 있어도 고르신 화면으로 바로 넘어가요\n· 예전처럼 프로그램만 여는 것도 그대로 됩니다\n\n원클릭업무포털은 청완초등학교 온영범 선생님이 만드신 별개의 무료 프로그램이며, 쌤핀은 실행만 도와드립니다."
}
```

### 문구에서 지킬 것

- **"쌤핀이 만든 기능이 아니다"를 빼지 말 것.** 문제가 생기면 저작자가 아니라 쌤핀으로 문의가
  오고, 좋으면 만드신 분이 아니라 쌤핀이 칭찬받는다. 둘 다 잘못된 상황이다.
- ❌ "나이스가 빨라져요" 류 — 쌤핀은 그 프로그램을 실행할 뿐 나이스에 관여하지 않는다.
- ❌ 설치 파일을 쌤핀이 배포한다고 읽힐 문장. 배포처는 `https://원클릭업무포털.com` 뿐이다.

### 릴리즈 전 실기기 확인 (필수)

- [ ] **v0.1.15 이상**에서 쌤도구 카드 → 업무 고르기 모달 → 6개 각각이 실제로 그 화면을 여는지
- [ ] 프로그램이 **이미 켜져 있을 때** 업무를 고르면 → 창이 두 개 뜨지 않고 떠 있던 창이
      그 화면으로 바뀌는지 ← 저쪽 `SingleInstanceCoordinator` 가 요청을 넘겨받는 지점
- [ ] **구버전(v0.1.14 이하)**이 깔린 PC → 업무 목록이 **안 보이고** 예전처럼 실행만 되는지
- [ ] 아직 설치 안 된 PC → 설치 안내 모달 (동작 변화 없음)
- [ ] **옆핀 즐겨찾기 위젯**에서도 업무 고르기가 뜨는지 ← 위젯 창에는 토스트가 없어 모달로만
      안내된다

## 릴리즈 후 (060 을 위해)

- [ ] 관리자 분석 화면에서 **구버전 비율** 확인 — 그 숫자가 곧 "060 적용 시 예약 취소가
      한 번 막히는 선생님 비율"이다
- [ ] 충분히 퍼진 뒤 060 적용 (런북 6단계). ⚠️ `--include-all` 이 필요하고 그 플래그는
      다른 대기 마이그레이션까지 함께 올린다 — 런북 절차를 그대로 따를 것

---

## 새로 편입한 영역 — 문구에서 지킬 것 · 릴리즈 전 확인 (2026-08-25 추가)

### 🍎 macOS 설치 차단

**노트에 "이전 파일을 받아 두셨다면 새로 내려받아 설치해 주세요"를 남겼다.** 서명이 없던
설치본은 앱 내 업데이트로 고쳐지지 않는다 — 그 앱을 열 수 없으니 업데이트도 못 받는다.
받은 사람이 스스로 다시 받아야 한다는 사실을 노트가 말해 주지 않으면 아무도 모른다.

- ⛔ "통합(universal) 파일" 을 쓰지 말 것 — `d32dbe98` 로 되돌렸다. **칩별 2파일이 맞다**
- ⛔ "공증(notarize)했다" 로 읽힐 문장 금지 — 무료 임시 서명(ad-hoc)이라 공증은 안 됐다.
  [그래도 열기] 한 단계는 여전히 필요하다
- [ ] 실기기: 새 DMG 를 내려받아 **터미널 없이** 설치되는지 (M칩 · 가능하면 Intel 도)
- [ ] `_CodeSignature` 게이트가 CI 에서 실제로 도는지 (빠지면 빌드가 실패해야 한다)

### 🛟 동기화 3종

가장 무거운 항목이다. **데이터가 사라졌던 경험**을 건드리므로 문구가 과하면 "그동안 내
자료가 날아가고 있었나" 로 읽히고, 약하면 다시 넣어 보라는 신호가 안 간다.

- [ ] 실기기: PC 에서 학생 사진 삭제 → 다른 PC 에서 **표식 받기 전에** 같은 학생 사진 다시
      넣기 → 동기화 2회 → **다시 넣은 사진이 살아 있는지** ← 이번 수정의 핵심
- [ ] 새 기기에 설치 → 첫 동기화에서 **학생 사진이 실제로 내려오는지**(0장이면 회귀)
- [ ] 휴대폰에서 진도 입력 중 앱 전환 후 복귀 → **적던 내용이 남아 있는지**
- [ ] PC 에서 수업반 순서 재배치 → 휴대폰 수업 탭·학생 탭·진도·출결 통계 **6곳 모두** 반영

### 🔎 관찰 슬롯 · 생기부

- ⛔ **"생기부 초안을 AI 가 써 준다" 로 읽힐 문장 금지**(ADR-072). 슬롯은 _입력_ 기능이다
- ⛔ 금지항목 경고를 "자동으로 걸러 준다" 로 쓰지 말 것 — **막지 않고 알려만 준다**(오너 결정).
  걸러 준다고 쓰면 선생님이 검토를 건너뛴다
- [ ] 실기기: 학생 A 에 슬롯을 켜고 → 학생 B 로 넘어갔을 때 **A 의 칩이 남아 있지 않은지**
      ← `ee83b92b` D1(b), 남의 장면이 붙던 결함
- [ ] 담임 누가기록에서 학생 여러 명 일괄 저장 시에도 같은지 (D2)

### 👥 모둠 · 🧊 쿨메신저 · 📦 설치 파일

- [ ] 21명 4모둠 → **6-5-5-5**, 21명 5모둠 → **5-4-4-4-4** (1명 모둠이 안 나오는지)
- [ ] 자리 배치 모둠에서 **학생이 빠지지 않는지**(21명·4모둠·최대 5명)
- [ ] 쿨메신저: 오너 PC 에 쿨메신저가 없어 **실기기 확인이 안 된다** — 릴리즈 노트에
      "해결했다" 로 단정해 두었으므로, 같은 신고가 또 오면 진단 로그
      (`userData/coolmessenger-diag.log`)를 먼저 받을 것
- [ ] 설치 파일 감량은 **빌드 후 실제 용량을 확인**하고 노트를 유지할지 정할 것.
      줄지 않았으면 그 항목을 뺀다 (수치는 일부러 안 적었다)
