# Architect 재검토 r2 — school-year-archive.plan.md v2

> 판정: **APPROVE (조건부)** · 2026-08-06 · 착수 전 수정 3건(NEW-1·2·8) + 착수 중 정리 5건(NEW-3·4·5·6·7). **8건 반영 시 재검토 없이 착수 가능.**
> r1 수정 요구(Synthesis 7 + PV HIGH 3)는 전부 실질 해소 확인. v2 신규 앵커 실측 전건 정확. S1.4 재작성(열거 vs 해석 기준)은 v1보다 명백히 정확.

## 착수 전 필수 (HIGH)

### NEW-1. ADR-033이 기대는 완화책("보관 직후 업로드")이 어느 스토리·AC에도 없다 (고아 완화책)

- "5분"은 고정값이 아님 — `src/App.tsx:956-958` 주기는 `autoSyncIntervalMin` **사용자 설정**이고 `!enabled`면 동기화 자체가 꺼짐 → 위험 창 무한 가능.
- 수정: **S1.2에 "`archiveClass`/`unarchiveClass` 성공 후 동기화 업로드 트리거(비활성 시 스킵)" 스토리·AC 추가.** ADR-033 Drivers ③ 문구 정정("다음 업로드 — 주기는 사용자 설정·비활성 가능"). 완화책→요구사항 격상. (레코드 병합 NO 결론 자체는 유지)

### NEW-2. term 파생 규칙 미명시 — `date` vs `createdAt`이 학기 경계에서 다른 답

- 3도메인 전부 두 필드 보유(`Attendance.ts:34/42`, `Observation.ts:6/10`, `StudentRecord.ts:68/69`). 8/10 수업을 9/2에 기록하면 date→2026-1, createdAt→2026-2로 갈림 — 가드가 작동하는 유일한 시점(학기 경계)에 모호성.
- 수정: **파생은 3도메인 전부 `date`(사건 발생일)로 고정**, createdAt 사용 금지를 ADR-034 Consequences·S2.2에 명시. AC 추가: "8/10 수업을 9/2에 기록한 레코드의 term = 2026-1".

### NEW-8. ADR-034가 PENDING인데 본문은 확정 서술 + 확정 조건이 falsifiable하지 않음

- 수정: **"APPROVED(방향) — P2 착수 시 선결 검증 4건"**으로 전환: ①build\* 경유 불변식 실측 100%(봉투 메타테스트 — NEW-5 이동분 활용) ②date 파생 결정 가능(이미 확인) ③merge 필터와 툼스톤 비교 분리 유지 ④레코드 증가분 실측.

## 착수 중 정리 (MEDIUM~LOW)

- **NEW-5**: 봉투 메타테스트를 P1 S1.6에서 **P2 S2.2로 이동** + 범위를 병합 3도메인 봉투(`{records:...}`)로 한정(내 r1 권고의 과잉 적용 정정 — P1은 봉투를 안 건드리고, `ManageTeachingClasses.reorder` 신설이 10번째 봉투 리터럴을 만들어 룰 즉시 예외 발생).
- **NEW-4**: P1 추정 4~6일은 낙관 — 순증(+브릿지 가드·3중 방어·긍정형 메타테스트 신설·reorder 유즈케이스·술어 3지점) 반영해 **6~8일로 정정**(NEW-5 반영 시 5~7일). `attendanceAutosave.ts`가 미커밋 상태라 S1.3이 그 위에 얹힘.
- **NEW-3**: S2.2→S2.1 강제 선행은 과잉 교정(레코드 단위 확정으로 S2.1에 epoch 의존 0) — **"S2.2 ∥ S2.1 병행, 단 S2.4 착수 전 둘 다 완료+ADR-034 확정"**으로 완화, §5 그래프 수정.
- **NEW-6**: 축소 게이트 ↔ 긍정형 메타테스트 상호작용 미정의 — 축소 절차 명문화(MUST_FILTER_FILES 제거+사유 주석 `// P1.1 이월 — YYYY-MM-DD 축소 게이트`+PROGRESS.md 기록). 안 하면 D-5 압박 속 "테스트를 고쳐 통과"라는 최악 선택 유발.
- **NEW-7**: "아카이브 Drive 동기화 = P4" 결정이 각주로 강등됨 — **ADR-036 승격**(P2 출시 조건 1의 근거이므로).

## 사소한 정정

- `ClassAttendanceStatsView.tsx` 실제 경로는 `src/mobile/components/Class/`(pages/ 아님).
- §5 그래프에서 S2.2 가지가 S1.5에 매달린 것처럼 보임 — S2.2 의존은 S0.1.

## 유지 권고 (잘 된 것)

S1.2 order 규칙(max+1) · §13.2 자기 정정 표 · §12-A 5문항 · §9 P1 롤백 절차 · S1.0 스토리 승격+폐기 금지.

## 원칙 위반

r1 HIGH 3건 전부 해소. v2 신규 = NEW-1 1건(MEDIUM, "위험 수용의 근거 완화책은 스토리·AC를 가져야 한다"). NEW-2는 위반이 아니라 명세 공백.
