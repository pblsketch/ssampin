판정: REJECTED

치명 결함 1건(런타임에서 그물 ③이 정상 도구 결과를 100% 차단), 그것을 숨긴 테스트 공백 1건, 그리고 "일부만 고쳐 남은 자기모순" 6곳 때문입니다. 코드 품질 자체는 높고 US-001~003, 005~006은 기준을 만족합니다.

---

## 치명 1건 — 그물 ③이 모든 정상 도구 결과를 차단한다

**`E:\github\ssampin\src\domain\rules\assertNoPii.ts:28`** — `birth: true`

`domain/privacy/maskRules.ts:37`의 birth 정규식은 `(?<!\d)\d{4}[-./]\d{1,2}[-./]\d{1,2}(?!\d)` 입니다. 실측했습니다.

```
"2026-08-21"            -> [ '2026-08-21' ]
"2026-08-25"            -> [ '2026-08-25' ]
"2026-08-01 ~ 2026-08-21" -> [ '2026-08-01', '2026-08-21' ]
```

이번에 만든 집계 함수 5종 중 **3종이 이 모양의 문자열을 반드시 내보냅니다**:

| 함수                  | 필드                                | 파일:줄                                                                     |
| --------------------- | ----------------------------------- | --------------------------------------------------------------------------- |
| `summarizeAttendance` | `date: '2026-08-21'`                | `E:\github\ssampin\src\usecases\assist\summaries\summarizeAttendance.ts:58` |
| `summarizeTodos`      | `due: '2026-08-25'`                 | `E:\github\ssampin\src\usecases\assist\summaries\summarizeTodos.ts:43`      |
| `summarizeRecords`    | `period: '2026-08-01 ~ 2026-08-21'` | `E:\github\ssampin\src\usecases\assist\summaries\summarizeRecords.ts:54`    |

경로가 끊기지 않습니다. `checkOutboundValue` → 문자열 분기(`assertNoPii.ts:88`) → `checkOutboundText` → `detectPatterns`(`assertNoPii.ts:61`) → `spans.length > 0` → `{ blocked: true }`(`assertNoPii.ts:62`). 그물 ③은 경고가 아니라 **차단**이라고 스스로 명시했으므로(`assertNoPii.ts:42`), Phase 2/3에서 배선하는 순간 **출결 요약·할 일·기록 통계가 단 한 건도 나가지 못합니다.**

이건 설계 문서가 가장 크게 경고한 실패 모양 그대로입니다 — _"오탐이 기능을 죽인다. 안 쓰이는 안전장치는 안전하지 않다"_(input-guard.design.md §1.2). 그 경고를 근거로 입력층은 경고 전용으로 내렸는데, 정작 **하드 차단 층에 그 오탐이 남았습니다.**

권고: `birth` 를 자유 텍스트 필드(`hasFreeText: true`)에만 켜거나, 도구 스키마상 날짜 필드로 선언된 값은 관문에서 제외하십시오. 등급표는 이미 `hasFreeText` 를 갖고 있으니 배선 지점이 이미 있습니다.

---

## 높음 2건

**① 계획서 Phase 1 인수 조건 중 "함정 픽스처로 전 도구 실행" 테스트가 없다 — 그래서 위 치명 결함이 안 잡혔다**
계획서 §4.4 그물 ④(`in-app-chatbot-zen.plan.md:414-420`)와 Phase 1 인수 조건(`:1262`)은 `runWithFixture(tool)` → `assertNoPii(result, FIXTURE_ROSTER)` 를 요구합니다. 실제 계약 테스트(`E:\github\ssampin\src\domain\services\__tests__\assistToolRegistry.contract.test.ts`)는 레지스트리 **메타데이터만** 순회하고 도구 결과를 관문에 통과시키지 않습니다. 집계 함수 테스트도, 관문 테스트도, 서로를 부르지 않습니다. 세 층이 각자 초록불인데 **이어 붙이면 빨간불**입니다. 이 저장소가 반복해서 겪은 "게이트 4종 전부 초록인데 결함이 살아 있음"의 재현입니다.

**② "오탐 0건" 테스트 픽스처가 실제 출력 모양을 피해 갔다**
`E:\github\ssampin\src\domain\rules\__tests__\assertNoPii.test.ts:15-19` 의 정상 문자열은 `'3학년 2반은 27명 출석했습니다.'`, `'이번 달 기록 34건…'` — **날짜가 하나도 없습니다.** 반대로 `:55-67` 의 중첩 픽스처에는 `due: '2026-08-25'` 가 있지만 이름 때문에 `blocked: true` 를 기대하므로, 날짜가 유발한 차단이 이름의 차단에 가려집니다. 통과하지만 아무것도 증명하지 못하는 배치입니다. US-004 마지막 기준("오탐 0건 검증 테스트")은 **형식만 충족**입니다.

---

## 중간 4건

**③ `sanitizeToolResult` 는 얕은 화이트리스트다 — `list_classes`·`get_my_todos`·`get_records_stats` 에서는 그물 ②가 사실상 작동하지 않는다**
`E:\github\ssampin\src\domain\services\sanitizeToolResult.ts:42` 가 `rebuilt[field] = value` 로 **참조를 그대로 복사**합니다. 레지스트리의 `resultFields` 는 최상위 키만 담습니다 — `['classes']`(`assistToolRegistry.ts:44`), `['items']`(`:62`), `byCategory`(`:52`). 계획서 §4.2 화이트리스트는 `{ classes: [{ id, name, grade, classNum }] }`, `{ items: [{ title, due, done }] }` 처럼 **중첩 필드 단위**로 적혀 있는데, 코드는 그 깊이를 강제하지 않습니다. 즉 누가 `{ items: store.todos }` 를 그대로 넘기면 `subTasks`·`googleTaskId` 등이 통째로 나갑니다 — 이 파일의 주석(`:8-10`)이 스스로 경고한 "엔티티가 커지면 조용히 샌다" 시나리오가 depth 1에서 그대로 성립합니다. 그리고 `sanitizeToolResult.test.ts:72-82` 는 이 약한 동작을 **잡는 게 아니라 고정합니다.** 지금은 집계 함수가 객체를 직접 짓기 때문에 안전하지만, 그건 그물이 아니라 우연입니다.

**④ `assertNoPii` 가 놓치는 경로 2개 (질문하신 항목)**

- **객체 키**: `assertNoPii.ts:97` 이 `Object.values(node)` 만 훑습니다. `{ '김지훈': 3 }` 처럼 **키에 든 이름은 검사되지 않습니다.** 지금 `byCategory` 가 배열 형태라 안전하지만, 맵 형태 집계가 하나만 추가돼도 뚫립니다.
- **숫자 필드**: `:88` 이 `typeof node === 'string'` 만 처리하므로 숫자에 든 값(예: `1099998888`)은 검사 대상이 아닙니다.
- 중첩 배열·중첩 객체는 정상 재귀합니다(`:92-98`). 이 부분은 확인했고 문제없습니다.

**⑤ `summarizeAttendance` 가 학생을 조용히 잃는다**
`E:\github\ssampin\src\usecases\assist\summaries\summarizeAttendance.ts:42-48` 이 `classAbsence`(수업 결과)를 세지만 반환(`:57-65`)에서 **버립니다.** `sick` 은 항상 하드코딩 0(`:64`)입니다. 스키마는 §4.2.2와 맞지만, 모델은 "질병 결석 0명"이라고 **사실과 다르게** 답하고 인원 합이 학급 인원과 안 맞습니다. 계획서 P5("모델이 죽어도 숫자는 남는다")의 전제는 그 숫자가 맞다는 것입니다.

**⑥ `SKIP_IF_NEARBY` 가 이름과 다르게 동작하고, 경고를 아예 삭제한다**
`E:\github\ssampin\src\domain\rules\screenAssistInput.ts:88,133,137` — 상수 이름은 NEARBY(인접)인데 구현은 `text.includes('예방')` **문장 전역 검사**이고, `continue` 로 finding 을 **추가조차 안 합니다**(심각도 강등이 아님). 그래서 _"자살 시도한 학생이 있는데, 예방 교육 자료 있나요?"_ → **경고가 0건**입니다. 설계 §2.3(`input-guard.design.md:122`)의 규칙은 `자살예방교육`·`자해예방` 처럼 **"붙으면"** 입니다. PRD US-005 문구("함께 있으면")로는 통과하지만 설계 정본과는 어긋납니다.

---

## 문서 자기모순 — "일부만 고침"이 6곳 남았다 (지목하신 유형)

같은 주장을 하는 다른 자리를 못 찾은 것입니다. 워크로그 마지막 줄이 스스로 적어 둔 재발 방지 항목이 이번에도 재발했습니다.

| #   | 위치                                                        | 지금 적혀 있는 것                                                                | 문제                                                                                                             |
| --- | ----------------------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 1   | `in-app-chatbot-zen.plan.md:1266` **(Phase 1 인수 조건)**   | "`screenAssistInput` 이 3등급 키워드에서 **차단**을 반환하고"                    | 이번에 만든 물건과 정반대. §6.2 만 고치고 같은 문서의 인수 조건은 그대로                                         |
| 2   | 같은 파일 `:1265`                                           | "`assertNoPii` … **반환값이 아니라 예외로 흐름을 끊는다**"                       | 구현은 결과 객체를 반환. 파일명 `assertNoPii` 인데 `assert*` 함수도 throw 도 없다                                |
| 3   | 같은 파일 `:893` vs `:895`                                  | `:893` "차단 상태로 바뀐다 — **보내기 버튼 비활성**" / `:895` "**차단 폐기**"    | **한 섹션(§5.7.4) 안에서 정면 충돌.** `:895` 가 이번에 새로 추가된 문장이므로 편집이 닿았는데도 `:893`을 안 고침 |
| 4   | 같은 파일 `:1619` **(§ 후퇴 금지선)**                       | "**'경고로 완화하자'는 변경은 이 문서를 근거로 거부한다**"                       | 오너 결정을 미래 세션이 되돌리라고 지시하는 문장이 살아 있다. 6곳 중 가장 위험                                   |
| 5   | 같은 파일 `:53`, `:135`, `:1413`, `:1478`, `:1489`, `:1491` | "3등급 차단은 선택이 아니라 필수", "3등급 표현 차단", 테스트 계획의 "→ **차단**" | 원안 표시 밖 영역인데 전부 미갱신                                                                                |
| 6   | `DECISIONS.md:1102`                                         | "…**3등급 차단은 필수가 된다**"                                                  | 같은 ADR `:1068` 은 `~~취소선~~`+보정 블록으로 제대로 고쳐 놓고, 34줄 아래 같은 주장은 그대로                    |

**키워드 개수 — 코드는 맞고 설계 문서가 자기 표를 잘못 설명합니다.**
코드 32개(주의 30·심각 2)를 설계 §3.1/§3.2 표와 **단어 단위로 하나씩 대조했고 완전 일치**합니다(가정 10 · 건강 9 · 안전 8 · 상담 3 · 심각 2). 그런데 `ssampin-ai.input-guard.design.md:130` 은 _"아래 표는 개념을 묶어 24개로 적었다"_ 라고 하는데 **아래 표에는 32개가 개별로 나열돼 있습니다**(그리고 `:136` 은 스스로 "구현 30개"라고 적습니다). 24개 개념표는 이 문서가 아니라 계획서 §5.7.3-a(24행)에 있습니다. 덤으로 `:202` 는 "경고 대상 **24개** + 심각 2개"(=26)라 `:130`의 24(=22+2)와도 안 맞습니다. **코드는 무죄, 문서 3곳이 서로 다른 숫자를 말합니다.**

---

## US-001 ~ US-008 판정

- **US-001 충족** — `AssistTool.ts` 전문 74줄에 import 문 0건 확인. `grade` 비-optional, `outbound` 존재, `ALLOWED_GRADES=[1]` + ADR-061 결정7 주석(`:49-57`), `declare const modelSafeBrand: unique symbol`(`:64`), `isGradeAllowed`(`:60`).
- **US-002 충족** — 1등급 5종만 등록, 전원 `grade`+`outbound` 명시, 계약 테스트가 순회·중복·화이트리스트 비어있음까지 검증.
- **US-003 충족(단, 얕음)** — `delete`·rest 미사용을 grep 으로 확인(0건), 함정 픽스처·미래 필드·누락 허용 전부 있음. 기준은 만족하나 중첩 미강제(중간 ③).
- **US-004 미충족** — "정상 문자열에서 오탐 0건" 기준이 실질적으로 깨집니다(치명 ①·높음 ②). 나머지 기준(패턴 탐지·명단 주입·차단 판정·주석·원문 미포함·함정 픽스처)은 충족.
- **US-005 충족** — 반환 타입에 `blocked`/`blocking`/`allowed` 없음을 테스트가 고정(`screenAssistInput.test.ts:17-25`), 파일 전체에 전송 여부를 결정하는 코드 0건(전문 171줄 확인). offset 은 `text.slice(start,end) === word` 로 정확성이 검증됩니다(`:46`). 한국어 라벨 5종 모두 화면에 그대로 쓸 수 있는 문구. 설계 §2.3 대비 "예방" 예외 동작 편차만 남음(중간 ⑥).
- **US-006 충족(주의)** — 5종 전부 존재, import 는 `@domain/entities/Attendance` 타입 하나뿐(스토어·adapters·infrastructure 0건, grep 확인). 반환 스키마는 §4.2 화이트리스트와 필드 단위 일치, `summarizeTodos` 는 `title` 을 가공 없이 보존하고 §4.4 그물 ③ 경고 주석이 파일 상단·함수 위 두 곳에 있음(`:6`, `:30`) — §4.2.2와 일치합니다. 단 출결 수치 정확도 문제(중간 ⑤).
- **US-007 부분 미충족** — ①②③은 워크로그 기록대로 성립합니다. **④는 성립하지 않습니다**: 레지스트리에 2등급 도구가 하나도 없으므로 `ALLOWED_GRADES=[1,2]` 로 바꿔도 새로 열리는 도구가 없고, `contract.test.ts:27` 의 `toEqual([1])` 이 **상수가 바뀌었다는 사실만** 실패로 알립니다. 워크로그의 _"어떤 도구가 새로 열리는지 드러냄"_ 은 코드가 뒷받침하지 않는 과장입니다.
- **US-008 미충족** — 게이트 4종·worklog·§6.2 갱신·타 세션 파일 무변경(assist 산출물이 전부 `??` untracked 임을 `git status` 로 확인)은 충족. 그러나 스토리 제목의 **"문서 정합"** 이 깨졌습니다 — §6.2 만 고치고 같은 문서의 Phase 1 인수 조건·§5.7.4·§11·§10 테스트 계획과 `DECISIONS.md:1102` 가 반대로 남았습니다.

---

## 나머지 확인 항목 (문제 없음)

- **`any`·타입 우회**: 신규 8개 소스 파일 전체 grep — `any` 0건, `@ts-ignore`/`@ts-expect-error` 0건. `as` 는 `sanitizeToolResult.ts:47` 의 브랜드 부착 1건뿐이며 이는 브랜드 타입의 정상적인 유일 생성 지점입니다. 테스트의 `as AssistToolDef` 는 테스트 코드입니다.
- **`ModelSafe<T>` 우회 가능성**: `modelSafeBrand` 를 export 하지 않아 외부에서 브랜드를 정직하게 만들 수 없습니다. 다만 브랜드 타입의 본질적 한계로 `x as ModelSafe<T>` 는 어디서든 씁니다 — 이를 막을 lint 규칙이 없고, **Phase 1에는 `ModelSafe` 를 요구하는 소비자(중계 호출 시그니처)가 아직 없어 컴파일 타임 강제 지점이 0개**입니다. US-001 기준상 문제는 아니지만, 그물 ②가 "타입 시스템이 거부한다"고 말할 수 있으려면 Phase 2에서 `sendToRelay(payload: ModelSafe<T>)` 가 생겨야 성립합니다.
- **domain 순수성 판정**: `assertNoPii` 가 `domain/privacy` 를 import 하는 것은 **허용**입니다. `docs/architecture-rules.md:18` 의 "domain → 아무것도 import 안 함"은 §12 의존성 규칙 표 안에 있어 **바깥 레이어를 import 하지 않는다**는 뜻이고, 실제로 `src/domain/**` 288개 파일이 레이어 내부 import 를 하고 있습니다(`Attendance.ts`·`Seating.ts` 등). 계획서 §6.1도 `domain/privacy/*` 를 "기존 자산, 수정하지 않고 사용"으로 명시했습니다. 정규식을 두 벌 만들지 않은 판단은 옳습니다 — 다만 그 재사용이 치명 ①(`birth` 규칙을 그대로 켠 것)을 불러왔으므로, **재사용하되 설정은 이 관문의 맥락에서 다시 골랐어야** 했습니다.

---

## 되돌리는 데 필요한 최소 작업

1. `assertNoPii.ts:28` `birth` 를 자유 텍스트 전용으로 좁히고, **집계 함수 5종의 실제 출력을 관문에 통과시키는 통합 테스트**를 추가(치명 ① + 높음 ①②를 한 번에 닫습니다). 이 테스트가 먼저 빨간불이 나는지 확인한 뒤 고치십시오 — 이 저장소의 "그물 실증" 원칙 그대로입니다.
2. 문서 6곳 일괄 정정. `grep -n "차단" in-app-chatbot-zen.plan.md` 로 전수 확인하고, 원안 보존 구간과 현행 서술 구간을 명시적으로 갈라 주십시오. 특히 `:1619` 는 오너 결정을 뒤집으라는 지시라 우선순위가 높습니다.
3. `sanitizeToolResult` 중첩 화이트리스트 여부는 **오너 판단 사항**으로 올리십시오. 지금 얕게 두려면 `resultFields` 가 최상위만 강제한다는 사실을 레지스트리 주석에 적어 다음 사람이 오해하지 않게 해야 합니다.
