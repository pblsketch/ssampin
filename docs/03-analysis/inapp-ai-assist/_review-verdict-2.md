## 판정: REJECTED — 19건 중 16건 완전 수정, 3건 부분(#12·#16·#19), 새로 생긴 결함 5건(중간 3·낮음 2)

치명·높음 7건(#1~#7)은 전부 실제로 고쳐졌고 **ADR-061 결정 1~7과 충돌하는 새 문장은 발견되지 않았다.** 그러나 ①계획서 안에 "검증됨"이라는 **사실이 아닌 안전 판정이 그대로 남아 있고**(#12 절반만 수정), ②오너 설명서가 **"19건 전부 반영"이라고 스스로 잘못 보고**하며, ③새로 넣은 절이 **엉뚱한 장(章) 안에 들어가 있다**(#19). 이 셋은 각각 "그물이 초록불인데 결함이 살아 있다" 유형이라 통과시키지 않는다. 수정 범위는 4곳뿐이다.

---

## 1. 19건 개별 판정

| #      | 판정                       | 근거 (파일:줄)                                                                                                                                                                                                                                                                |
| ------ | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 치명 | **고쳐짐**                 | `plan.md:878-889` — "근본 해결책은 없다 + 3등급 차단·제안 칩·정직한 고지 3가지가 필수"로 재작성. `plan.md:1577-1578`(부검 4)도 같은 취지로 교체                                                                                                                               |
| 2      | **고쳐짐**                 | `plan.md:215-216` — "나중에 판단이 바뀔 수 있는 유일한 축" 사라지고 **"전환 지점에 등급 정책은 포함되지 않는다"** 명시                                                                                                                                                        |
| 3      | **고쳐짐**                 | `plan.md:1186-1188` — 업스테이지 교육팀 문의로 교체, 물을 것 2가지(①무료 권한 범위 ②약관 제9조) 명시, `help@anoma.ly`는 취소선 + 폐기 사유                                                                                                                                    |
| 4      | **고쳐짐**(단 새 결함 2건) | `plan.md:1131-1146` 완료 배너 / `plan.md:1590` R3 해소 / `plan.md:1316` 상한을 **50 왕복**으로 확정. → 아래 N4·N5 참조                                                                                                                                                        |
| 5      | **고쳐짐**                 | `plan.md:444` "**같은 무료 권한 위에서 돈다**(ADR-048, 콘솔 청구 $0)" + `:445` 데이터 성격 차이 행 신설                                                                                                                                                                       |
| 6      | **고쳐짐**                 | `design.md:736` "**즉시 전송한다. 입력창을 거치지 않는다**" + 왜 채워 넣기가 1층 방어를 무너뜨리는지까지                                                                                                                                                                      |
| 7      | **고쳐짐**                 | `plan.md:15` "✅ 본문 갱신 완료" / `owner-brief.md:347` "본문(§1~§12·§14) 갱신 완료, §13만 역사 보존". 본문에 Zen 전제 잔재 0건(§13·경로명·비교 수치 제외)                                                                                                                    |
| 8      | **고쳐짐**                 | `owner-brief.md:258-265` — $10.9 → $5 → $3.3 → $2 아래 순, "위에서 아래로 갈수록 싸집니다"                                                                                                                                                                                    |
| 9      | **고쳐짐**                 | `owner-brief.md:302` — "**입력창에 사정을 풀어서 쓴다**(가정사·건강)"으로 정정                                                                                                                                                                                                |
| 10     | **고쳐짐**                 | `design.md:77` `font-sp-semibold`. 실제 유효 클래스임을 `tailwind.config.js:93`에서 확인                                                                                                                                                                                      |
| 11     | **고쳐짐**(렌더 결함)      | `design.md:585-591` placeholder 선언 + FAQ.tsx 인용 원칙 + 담아야 할 사실 3가지. → N8                                                                                                                                                                                         |
| 12     | **부분 ❌**                | `plan.md:245`는 "스키마만 재사용, 집계 함수는 신규"로 고쳐졌으나 **`plan.md:288`이 그대로다** — `\| get_weekly_summary \| 브릿지 스키마 그대로 \| 브릿지에서 egress 테스트로 이미 강제됨 \| ✅ 검증됨 \|`. 같은 문서 안에서 245와 288이 정면 충돌하고, **하필 안전 판정표**다 |
| 13     | **고쳐짐**(실측 일치)      | `plan.md:66·136·999`, `owner-brief.md:17` 모두 18/19. 실제 `supabase/functions/` 20개 중 `_assets`·`_shared` 제외 = **18개** 확인                                                                                                                                             |
| 14     | **고쳐짐**(실측 일치)      | `plan.md:521` → `src/App.tsx:1318`이 실제 `<CommandPalette>` 마운트 줄. 함께 쓰인 `:1241`(flex row) `:1243`(Sidebar) `:1255`(본문)도 전부 실제와 일치                                                                                                                         |
| 15     | **고쳐짐**                 | `design.md:69` — "HEX 표기를 한 번도 쓰지 않는다". 실제 `grep -E '#[0-9a-fA-F]{3,6}'` **0건** 확인                                                                                                                                                                            |
| 16     | **부분 ❌**                | `plan.md:1704`는 R17로 교체됨. 그러나 **`plan.md:1705`가 아직 R14를 가리킨다** — R14는 "3등급 차단이 빡빡하다는 압력"(`:1601`)이고, "자매 계획과 공유 자산 소유권"은 **R18**(`:1605`)이다. 두 어긋남 중 하나만 고쳤다                                                         |
| 17     | **고쳐짐**(제목 모순)      | `owner-brief.md:14-24` 용어 9개. 단 **추가된 건 7개가 아니라 6개**이고, 제목이 아직 "**용어 세 개만**"(`:12`) → N3                                                                                                                                                            |
| 18     | **고쳐짐**                 | `owner-brief.md:94-107` §3.2-a 신설. 계획서 `:100`·설계 `:598`과 서술 일치                                                                                                                                                                                                    |
| 19     | **부분 ❌**                | 내용은 `owner-brief.md:283-291`에 있으나 **위치가 틀렸다** — §5.3(`:269-279`)이 끝난 뒤, `## 6`(`:295`) 앞에 `### 4.8`이 놓여 **"돈 이야기" 장 안에 4.8이 들어가 있다**                                                                                                       |

---

## 2. 새로 생긴 문제

| #   | 심각도   | 문제                                                                                                                                                                                                                                               | 근거                                       |
| --- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| N1  | **중간** | (#12와 동일) 안전 판정표가 **아직 만들지도 않은 함수를 "✅ 검증됨"으로 선언**한다. 이 프로젝트에서 반복된 "그물이 초록불인데 결함이 살아 있음" 유형                                                                                                | `plan.md:288` vs `plan.md:245`             |
| N2  | **중간** | 오너 설명서가 "**19건 지적, 전부 반영**"이라고 보고한다. #12·#16이 남아 있으므로 **사실이 아니다.** 오너가 이 줄을 근거로 판단한다                                                                                                                 | `owner-brief.md:348`                       |
| N3  | **중간** | 제목 "0. 먼저, **용어 세 개만**" 아래에 용어가 **9개**다. 오너용 문서에서 첫 화면부터 말이 안 맞는다                                                                                                                                               | `owner-brief.md:12` vs `:14-24`            |
| N4  | 낮음     | §8.1 표가 **깨져 있다** — 머리글 행이 두 번 들어가 `\| 규칙 \| 값 \| 근거 \|`와 `\| --- \|`가 본문 행으로 렌더된다(이번 수정으로 새 표를 옛 머리글 아래에 붙인 흔적)                                                                               | `plan.md:1310-1313`                        |
| N5  | 낮음     | 근거문이 "**여유를 두고 건다**"인데 값은 **물리 상한 그대로**다(공급자 100요청/분 ÷ 2 = 50왕복). 여유가 0이다. 또 ADR-061 결정 4로 **키를 분리했으므로 "분당 100"이 키 단위인지 계정 단위인지**가 이 값의 전제인데 문서에 확인 항목이 없다         | `plan.md:1316`                             |
| N6  | 낮음     | 설계 문서 §11은 **일일 429("내일 다시 열려요")만** 다룬다. #4 수정으로 **월 예산이 1차 제동 장치로 승격**됐는데(`plan.md:1317-1320`, `:1043` `degraded:"budget"`은 200), 월 예산 소진 상태의 화면이 없다 → 그때 "내일 다시 열려요"는 거짓말이 된다 | `design.md:636-680` vs `plan.md:1043·1317` |
| N7  | 낮음     | #11로 넣은 placeholder 경고가 **코드펜스(557~607) 안에** 있어 인용구가 아니라 ASCII 그림의 일부로 렌더된다. 경고가 도면 한가운데 박혀 눈에 덜 띈다                                                                                                 | `design.md:557·585-591·607`                |
| N8  | 낮음     | 제안 칩 **개수가 문서마다 다르다** — 설계는 "**3개.** 4개 이상은 고르는 부담"(`design.md:735`), 계획서는 4개(`plan.md:769`), 새로 쓴 오너 §4.8도 4개(`owner-brief.md:287`). #19가 계획서 쪽 숫자를 그대로 옮기며 설계와의 불일치를 하나 더 늘렸다  | 3개 문서                                   |
| N9  | 사소     | 오너 설명서의 "캐시까지 걸리면 **$2 아래**"는 계획서·ADR-061 어디에도 없는 파생 수치다(ADR은 "$10.9 시나리오에서 캐시 시 월 $5 이하"까지만 실측 근거)                                                                                              | `owner-brief.md:263`                       |
| N10 | 사소     | §13을 "역사 보존"이라 선언해 놓고 **일부만 갱신**했다 — `:1704`는 R17·업스테이지로 갱신, `:1703`은 `help@anoma.ly` 유지. 보존 규칙이 반쪽이라 다음 사람이 어디까지 손대도 되는지 알 수 없다                                                        | `plan.md:1700-1705`                        |

---

## 3. 요청한 교차 검증 결과 (전부 통과)

- **ADR-061 결정 1~7 충돌**: 없음. 결정 2·6(무료 원칙·유료 비상구) → `plan.md:1373`·`:1756`, 결정 3(월 상한) → `:1317`, 결정 4(키 분리) → `:1058-1066`, 결정 5(코드에 박지 않음) → `:26-28`·`:1360`, 결정 7(영구 경계·2등급 미제작·학습 고지·3등급 필수) → `:1362-1370`·`:296-299`·`:914`. 남은 "유료" 언급은 전부 **비상구 맥락**이라 결정 6·7과 어긋나지 않는다.
- **§8.1 "분당 50왕복" vs §8.2·§6.6.1·§10**: 모순 없음. §8.2는 왕복 수가 아니라 대화 단위 예산이고, §6.6.1의 429/`degraded` 분기와도 층위가 다르다. 단 N5·N6 참조.
- **비용 숫자**: 세 문서 모두 월 **$3~11** 일치(`plan.md:23·1373·1756`, `owner-brief.md:58·276`), 문항당 $0.000281 일치. 캐시 $5는 ADR 범위 안(N9의 $2만 근거 밖).
- **실측 ② 상태**: 세 문서 어디에도 "미완료" 잔재 없음.
- **오너 §3.2-a·§4.8 내용 정합성**: 국외이전 서술은 계획서 `:100`·설계 `:598`과 일치. 칩 동작(즉시 전송)도 설계 `:736`과 일치(개수만 N8).
- **검증 가능한 사실 재확인**: Edge Function 18개 ✅ / `App.tsx:1318` ✅ / `font-sp-semibold` 실존 ✅ / 설계 HEX 0건 ✅ / ADR-039·048·051·052·056·061 6건 전부 실재 ✅ / `node scripts/regression-grep-check.mjs` → **Total 43 | Passed 43 | Failed 0** ✅ (오너 설명서 `:350` 주장 실측 확인)

---

## 4. APPROVED로 넘기기 위해 필요한 수정 — 4곳

1. `E:\github\ssampin\docs\01-plan\features\in-app-chatbot-zen.plan.md:288` — `get_weekly_summary` 행의 "브릿지에서 egress 테스트로 이미 강제됨 / ✅ 검증됨"을 245줄과 같은 취지(**스키마만 참조, 집계 함수는 신규 → 검증 대상**)로 교체.
2. 같은 파일 `:1705` — `R14` → `R18`.
3. 같은 파일 `:1310-1313` — 중복 머리글 행 2줄 삭제(표 복구). 겸사 `:1316` 근거문을 값과 맞추거나(50 → 40 등) "물리 상한과 같다"로 정직하게 고칠 것.
4. `E:\github\ssampin\docs\01-plan\features\inapp-ai-assist.owner-brief.md` — ①`### 4.8`(283-291)을 `### 4.7`(241-245) 뒤·`## 5`(249) 앞으로 이동 ②`:12` 제목을 "용어 아홉 개" 등으로 ③`:348` "전부 반영"을 실제 반영 결과로 정정.

N6(월 예산 소진 화면)·N8(칩 개수)은 Phase 3 착수 전까지 정리하면 되는 항목이라 이번 통과 조건에 넣지 않았다.
